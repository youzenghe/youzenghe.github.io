(function(){
  const detail = {
  "id": 12,
  "content": "<h1>Spring 全家桶面试八股精华</h1>\n      <blockquote>整理自小林coding 6.Spring面试篇</blockquote>\n      <blockquote>涵盖 Spring 核心 / Spring MVC / Spring Boot / MyBatis / Spring Cloud</blockquote>\n      <blockquote>你简历两个项目都强依赖 Spring Boot + MyBatis，<strong>这本是面试 占比最高的一本</strong></blockquote>\n      <hr>\n      <h2>目录</h2>\n      <ul><li><a href=\"#一spring-核心25-题\">一、Spring 核心（25 题）</a></li><li><a href=\"#二spring-bean12-题\">二、Spring Bean（12 题）</a></li><li><a href=\"#三spring-事务10-题\">三、Spring 事务（10 题）</a></li><li><a href=\"#四spring-mvc10-题\">四、Spring MVC（10 题）</a></li><li><a href=\"#五spring-boot15-题\">五、Spring Boot（15 题）</a></li><li><a href=\"#六mybatis--mybatis-plus15-题\">六、MyBatis / MyBatis-Plus（15 题）</a></li><li><a href=\"#七spring-cloud选看8-题\">七、Spring Cloud（选看，8 题）</a></li><li><a href=\"#八高频追问串讲\">八、高频追问串讲</a></li></ul>\n      <hr>\n      <h1>一、Spring 核心（25 题）</h1>\n      <h2>1. Spring 是什么 / 有哪些模块 ⭐</h2>\n      <ul><li><strong>是什么</strong>：一个轻量级 Java EE 开发框架，<strong>核心是 IOC 和 AOP</strong>。</li><li><strong>核心模块</strong>：</li><li>Core Container（Beans、Core、Context、SpEL）</li><li>AOP</li><li>Data Access（JDBC、ORM、Transaction）</li><li>Web（Spring MVC、WebFlux）</li><li>Test</li></ul>\n      <h2>2. IOC 是什么 ⭐⭐⭐</h2>\n      <ul><li><strong>全名</strong>：Inversion of Control，<strong>控制反转</strong>。</li><li><strong>是什么</strong>：原本由程序员 <code>new</code> 对象，现在交给 Spring 容器创建、管理依赖关系。</li><li><strong>目的</strong>：解耦，让对象之间不再直接依赖。</li><li><strong>DI（依赖注入）是 IOC 的实现方式</strong>：容器通过构造器/Setter/字段注入依赖。</li></ul>\n      <h2>3. AOP 是什么 ⭐⭐⭐</h2>\n      <ul><li><strong>全名</strong>：Aspect Oriented Programming，<strong>面向切面编程</strong>。</li><li><strong>解决什么</strong>：把横切关注点（日志、事务、权限、限流、缓存）从业务代码抽离。</li><li><strong>OOP 是纵向继承复用，AOP 是横向切面复用</strong>。</li></ul>\n      <h2>4. AOP 核心概念 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>概念</th><th>解释</th></tr></thead><tbody><tr><td><strong>Aspect 切面</strong></td><td>横切逻辑的封装（如日志切面）</td></tr><tr><td><strong>Join point 连接点</strong></td><td>可以被切入的位置（方法执行点）</td></tr><tr><td><strong>Pointcut 切点</strong></td><td>表达式，匹配哪些连接点要切入</td></tr><tr><td><strong>Advice 通知</strong></td><td>切入后做什么（before/after/around）</td></tr><tr><td><strong>Target 目标对象</strong></td><td>被代理的对象</td></tr><tr><td><strong>Weaving 织入</strong></td><td>把切面织入目标对象的过程</td></tr></tbody></table></div>\n      <h2>5. Advice 通知有哪几种</h2>\n      <ul><li><code>@Before</code>：方法执行前</li><li><code>@After</code>：方法执行后（finally，无论异常）</li><li><code>@AfterReturning</code>：正常返回后</li><li><code>@AfterThrowing</code>：抛异常后</li><li><code>@Around</code>：环绕（最强大，可控制是否执行原方法）</li></ul>\n      <h2>6. Spring AOP 实现原理 ⭐⭐⭐（必考）</h2>\n      <p><strong>动态代理</strong>：</p>\n      <ul><li><strong>接口</strong> → JDK 动态代理（<code>java.lang.reflect.Proxy</code>）</li><li><strong>无接口</strong> → CGLib 代理（继承目标类生成子类）</li></ul>\n      <p><strong>注意</strong>：Spring Boot 2.x 后<strong>默认全用 CGLib</strong>（即使有接口）。</p>\n      <h2>7. JDK 动态代理 vs CGLib</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>JDK 代理</th><th>CGLib</th></tr></thead><tbody><tr><td>前提</td><td>必须有接口</td><td>无要求</td></tr><tr><td>原理</td><td>反射生成实现类</td><td>字节码生成子类</td></tr><tr><td>性能</td><td>创建快、执行稍慢</td><td>创建慢、执行快</td></tr><tr><td>限制</td><td>无</td><td><strong>不能代理 final 类/方法</strong></td></tr></tbody></table></div>\n      <h2>8. Spring AOP vs AspectJ AOP</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>Spring AOP</th><th>AspectJ</th></tr></thead><tbody><tr><td>织入时机</td><td><strong>运行时</strong>（动态代理）</td><td><strong>编译期 / 类加载期</strong>（字节码增强）</td></tr><tr><td>性能</td><td>略低</td><td>高</td></tr><tr><td>功能</td><td>只能拦截方法</td><td>方法、字段、构造器都能</td></tr><tr><td>难度</td><td>简单</td><td>复杂</td></tr></tbody></table></div>\n      <h2>9. AOP 失效场景 ⭐⭐⭐</h2>\n      <ol><li><strong>同类内部方法调用</strong>：<code>this.xxx()</code> 不走代理（this 是原对象不是代理对象）</li><li><strong>private / final / static 方法</strong>：不能被代理</li><li><strong>方法异常被吞掉</strong>：异常通知拿不到</li><li><strong>AOP 注解未被 @ComponentScan 扫描</strong></li></ol>\n      <p><strong>解决同类调用</strong>：</p>\n      <ul><li>注入自己：<code>@Autowired private XxxService self;</code> 调 <code>self.xxx()</code></li><li><code>AopContext.currentProxy()</code> 拿当前代理</li><li>拆方法到另一个 Bean</li></ul>\n      <h2>10. Spring IOC 容器初始化流程（简化版）</h2>\n      <ol><li>读配置（XML / 注解 / Java Config）</li><li>解析为 <strong>BeanDefinition</strong> 注册到容器</li><li>实例化 Bean、依赖注入、初始化（生命周期回调）</li><li>放入单例池供后续使用</li></ol>\n      <h2>11. BeanFactory vs ApplicationContext</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>BeanFactory</th><th>ApplicationContext</th></tr></thead><tbody><tr><td>加载</td><td><strong>懒加载</strong></td><td><strong>预加载</strong>（启动时全初始化）</td></tr><tr><td>功能</td><td>基础 IOC</td><td>IOC + AOP + 事件 + 国际化</td></tr><tr><td>使用</td><td>很少直接用</td><td>实际开发用它</td></tr></tbody></table></div>\n      <h2>12. ApplicationContext 常用实现</h2>\n      <ul><li><code>ClassPathXmlApplicationContext</code>：读 classpath XML</li><li><code>FileSystemXmlApplicationContext</code>：读文件系统 XML</li><li><code>AnnotationConfigApplicationContext</code>：基于注解</li><li><code>AnnotationConfigServletWebServerApplicationContext</code>：Spring Boot 默认</li></ul>\n      <h2>13. 依赖注入的 3 种方式 ⭐</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">// 1. 构造器注入（推荐！）\n@Service\npublic class UserService {\n    private final UserRepository repo;\n    public UserService(UserRepository repo) {\n        this.repo = repo;\n    }\n}\n\n// 2. Setter 注入\n@Autowired\npublic void setRepo(UserRepository repo) { this.repo = repo; }\n\n// 3. 字段注入（不推荐！）\n@Autowired\nprivate UserRepository repo;</code></pre></figure>\n      <p><strong>为什么推荐构造器注入</strong>：</p>\n      <ol><li>强制依赖明确（缺依赖直接启动失败）</li><li>字段可设 final（不可变）</li><li>单元测试方便（直接 new）</li><li>避免循环依赖隐藏问题</li></ol>\n      <h2>14. @Autowired vs @Resource ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>@Autowired</th><th>@Resource</th></tr></thead><tbody><tr><td>来源</td><td>Spring</td><td>JDK</td></tr><tr><td>默认装配</td><td>byType</td><td>byName</td></tr><tr><td>多实现</td><td>需配合 @Qualifier</td><td>直接按 name</td></tr><tr><td>必需性</td><td>默认 required=true</td><td>没有 required 属性</td></tr></tbody></table></div>\n      <h2>15. @Autowired 注入找不到怎么办</h2>\n      <ul><li>加 <code>required = false</code></li><li>或用 <code>@Nullable</code></li><li>或加 <code>@Qualifier(&quot;具体beanName&quot;)</code> 指定</li></ul>\n      <h2>16. @Component / @Service / @Repository / @Controller 区别</h2>\n      <p><strong>功能完全相同</strong>，只是语义区分：</p>\n      <ul><li><code>@Component</code>：通用</li><li><code>@Service</code>：业务层</li><li><code>@Repository</code>：DAO 层（<strong>额外把数据库异常转为 Spring DataAccessException</strong>）</li><li><code>@Controller</code>：表现层</li></ul>\n      <h2>17. @Configuration 和 @Component 区别 ⭐</h2>\n      <ul><li>都能注册 Bean，但 <code>@Configuration</code> 类是<strong>代理类</strong>，<code>@Bean</code> 方法之间互相调用会被代理拦截，保证单例</li><li><code>@Component</code> 类不被代理，方法调用每次都新建对象</li></ul>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Configuration\npublic class Config {\n    @Bean public A a() { return new A(); }\n    @Bean public B b() { return new B(a()); }  // 这里调 a() 走代理，返回单例\n}</code></pre></figure>\n      <h2>18. @Bean 和 @Component 区别</h2>\n      <ul><li><code>@Component</code>：<strong>类</strong>上注解，自动扫描注册</li><li><code>@Bean</code>：<strong>方法</strong>上注解，<strong>手动</strong>注册（适合第三方类、需要复杂初始化的类）</li></ul>\n      <h2>19. Spring 循环依赖 ⭐⭐⭐（必考）</h2>\n      <p><strong>3 级缓存</strong>：</p>\n      <ul><li>一级 <code>singletonObjects</code>：完整 Bean</li><li>二级 <code>earlySingletonObjects</code>：半成品 Bean（已实例化未填属性）</li><li>三级 <code>singletonFactories</code>：Bean 工厂（懒生成代理）</li></ul>\n      <p><strong>解决步骤</strong>：</p>\n      <ol><li>A 实例化（半成品），放三级缓存</li><li>A 注入 B → 去创建 B</li><li>B 实例化，注入 A → 从三级缓存拿 A 的工厂 → 升到二级缓存</li><li>B 创建完成，A 继续填充属性 → A 创建完成</li></ol>\n      <p><strong>只解决单例 + setter/字段注入</strong>，<strong>构造器注入 / prototype 解决不了</strong>。</p>\n      <h2>20. 为什么需要三级缓存而不是两级</h2>\n      <ul><li>没有 AOP 时两级就够</li><li><strong>有 AOP 时</strong>：B 引用的 A 必须是代理对象，三级缓存的工厂用于<strong>懒生成代理</strong>（避免每个 Bean 都提前生成代理）</li></ul>\n      <h2>21. Spring 用了哪些设计模式 ⭐</h2>\n      <ul><li><strong>工厂模式</strong>：BeanFactory</li><li><strong>单例模式</strong>：Bean 默认单例</li><li><strong>代理模式</strong>：AOP</li><li><strong>模板模式</strong>：JdbcTemplate、RestTemplate</li><li><strong>观察者模式</strong>：ApplicationEvent</li><li><strong>适配器模式</strong>：HandlerAdapter</li><li><strong>装饰器模式</strong>：BeanWrapper</li><li><strong>责任链模式</strong>：拦截器链</li></ul>\n      <h2>22. SpringMVC 中 @Controller 和 @RestController 区别</h2>\n      <ul><li><code>@Controller</code>：返回视图名</li><li><code>@RestController</code> = <code>@Controller</code> + <code>@ResponseBody</code>：返回数据（JSON），常用于 REST API</li></ul>\n      <h2>23. Spring 事件机制</h2>\n      <ul><li>自定义事件继承 <code>ApplicationEvent</code></li><li>发布：<code>ApplicationContext.publishEvent(event)</code></li><li>监听：<code>@EventListener</code> 或实现 <code>ApplicationListener</code></li><li><strong>同步执行</strong>，要异步加 <code>@Async</code></li></ul>\n      <h2>24. Spring 启动流程（简化）</h2>\n      <ol><li>创建 ApplicationContext</li><li>加载配置文件 / 扫描注解</li><li>注册 BeanDefinition</li><li>调用 BeanFactoryPostProcessor（修改 BeanDefinition）</li><li>实例化所有单例 Bean → 注入依赖 → 调用 BeanPostProcessor → 初始化</li><li>容器就绪，发布 ContextRefreshedEvent</li></ol>\n      <h2>25. Spring 中的常见扩展点</h2>\n      <ul><li><code>BeanFactoryPostProcessor</code>：修改 BeanDefinition</li><li><code>BeanPostProcessor</code>：修改 Bean 实例（AOP 就是用这个）</li><li><code>InitializingBean / DisposableBean</code>：生命周期回调</li><li><code>ApplicationListener</code>：事件监听</li><li><code>Aware</code> 系列：让 Bean 感知容器（BeanNameAware、ApplicationContextAware）</li></ul>\n      <hr>\n      <h1>二、Spring Bean（12 题）</h1>\n      <h2>1. Bean 的生命周期 ⭐⭐⭐（必背）</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 实例化（调用构造器）\n2. 属性填充（依赖注入）\n3. Aware 系列回调（BeanNameAware → BeanFactoryAware → ApplicationContextAware）\n4. BeanPostProcessor.postProcessBeforeInitialization\n5. @PostConstruct\n6. InitializingBean.afterPropertiesSet()\n7. 自定义 init-method\n8. BeanPostProcessor.postProcessAfterInitialization  ← AOP 在这里生成代理\n9. ▼▼▼ Bean 可用 ▼▼▼\n10. @PreDestroy\n11. DisposableBean.destroy()\n12. 自定义 destroy-method</code></pre></figure>\n      <h2>2. Bean 的作用域 Scope</h2>\n      <div class=\"md-table\"><table><thead><tr><th>作用域</th><th>说明</th></tr></thead><tbody><tr><td><strong>singleton</strong>（默认）</td><td>单例，整个容器一个</td></tr><tr><td><strong>prototype</strong></td><td>每次获取都新建</td></tr><tr><td><strong>request</strong></td><td>Web 环境，每个 HTTP 请求一个</td></tr><tr><td><strong>session</strong></td><td>Web 环境，每个会话一个</td></tr><tr><td><strong>application</strong></td><td>Web 环境，整个 ServletContext 一个</td></tr><tr><td><strong>websocket</strong></td><td>每个 WebSocket 一个</td></tr></tbody></table></div>\n      <h2>3. Bean 默认为什么单例</h2>\n      <ul><li><strong>性能</strong>：每次 new 开销大</li><li><strong>共享</strong>：无状态 Bean 单例足够</li><li>内存少</li></ul>\n      <h2>4. Spring 中的 Bean 线程安全吗 ⭐⭐⭐</h2>\n      <ul><li><strong>取决于 Bean 是否有可变状态</strong></li><li>单例 + 有可变成员变量 → <strong>线程不安全</strong>（如 Controller 里放业务字段）</li><li>解决：</li></ul>\n      <ol><li>改为 prototype</li><li>用 ThreadLocal 存共享变量</li><li>使用无状态设计（推荐）</li></ol>\n      <h2>5. Bean 注册的几种方式</h2>\n      <ol><li><code>@ComponentScan</code> + <code>@Component</code></li><li><code>@Configuration</code> + <code>@Bean</code></li><li>XML <code>&lt;bean&gt;</code></li><li><code>@Import</code> 导入配置类 / ImportSelector</li><li><code>FactoryBean</code> 接口</li></ol>\n      <h2>6. FactoryBean 和 BeanFactory 区别</h2>\n      <ul><li><strong>BeanFactory</strong>：IOC 容器</li><li><strong>FactoryBean</strong>：一个特殊的 Bean，<strong>用于生产其他 Bean</strong></li><li><code>getObject()</code> 返回真正要注入的对象</li><li>例：MyBatis 的 SqlSessionFactoryBean</li></ul>\n      <h2>7. 容器获取 FactoryBean 本身</h2>\n      <p><code>getBean(&quot;&amp;beanName&quot;)</code> 加 <code>&amp;</code> 前缀。</p>\n      <h2>8. @Lazy 懒加载</h2>\n      <ul><li>加在 <code>@Component</code> / <code>@Bean</code> 上 → 第一次使用时才创建</li><li>用途：循环依赖（构造器场景）、启动加速</li></ul>\n      <h2>9. @PostConstruct 和 @PreDestroy</h2>\n      <ul><li><code>@PostConstruct</code>：Bean 初始化完成后回调（替代 InitializingBean）</li><li><code>@PreDestroy</code>：容器销毁前回调</li></ul>\n      <h2>10. Spring 怎么解决属性循环依赖（不能解决的场景）</h2>\n      <p><strong>不能解决</strong>：</p>\n      <ul><li>构造器循环依赖 → 抛 BeanCurrentlyInCreationException</li><li>prototype 循环依赖 → 同上</li><li>多例的字段循环 → 不解决（每次都是新对象）</li></ul>\n      <h2>11. Bean 创建过程涉及的几个核心类</h2>\n      <ul><li><code>BeanDefinition</code>：Bean 的元数据</li><li><code>BeanFactory</code>：容器</li><li><code>BeanPostProcessor</code>：Bean 后置处理器</li><li><code>InstantiationStrategy</code>：实例化策略</li></ul>\n      <h2>12. Spring 中 this 调用方法失效</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Service\npublic class A {\n    public void m1() {\n        this.m2();  // ❌ this 是原对象不是代理，事务/AOP 失效\n    }\n    @Transactional\n    public void m2() {...}\n}</code></pre></figure>\n      <p>解决：自己注入自己 / AopContext。</p>\n      <hr>\n      <h1>三、Spring 事务（10 题）</h1>\n      <h2>1. Spring 事务的两种方式</h2>\n      <ul><li><strong>编程式</strong>：<code>TransactionTemplate</code> / <code>PlatformTransactionManager</code>（少用）</li><li><strong>声明式</strong>：<code>@Transactional</code>（推荐）</li></ul>\n      <h2>2. @Transactional 原理 ⭐⭐⭐</h2>\n      <p><strong>AOP 实现</strong>：</p>\n      <ol><li>启动时扫描 <code>@Transactional</code> 注解，生成代理</li><li>调用方法时拦截，开启事务</li><li>业务执行成功 → commit</li><li>抛 RuntimeException → rollback</li></ol>\n      <h2>3. @Transactional 失效场景 ⭐⭐⭐（必考！）</h2>\n      <ol><li><strong>同类内部调用</strong>：this 调用不走代理</li><li><strong>方法不是 public</strong>：Spring 只对 public 方法生效</li><li><strong>异常被 try-catch 吞了</strong>：默认只对未捕获的 RuntimeException 回滚</li><li><strong>抛了 Checked Exception</strong> 但没配 <code>rollbackFor</code></li><li><strong>没被 Spring 管理</strong>：类不是 Bean</li><li><strong>传播行为不对</strong>：如 <code>NOT_SUPPORTED</code> 内部就没事务</li><li><strong>多线程</strong>：事务绑定的是 ThreadLocal，新线程拿不到</li><li><strong>数据库引擎不支持</strong>：如 MyISAM</li><li><strong>rollbackFor 配置错</strong></li></ol>\n      <h2>4. 事务的 7 种传播行为 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>传播行为</th><th>当前有事务</th><th>当前无事务</th></tr></thead><tbody><tr><td><strong>REQUIRED</strong>（默认）</td><td>加入</td><td>新建</td></tr><tr><td><strong>REQUIRES_NEW</strong></td><td><strong>挂起当前</strong>，新建</td><td>新建</td></tr><tr><td><strong>SUPPORTS</strong></td><td>加入</td><td>非事务执行</td></tr><tr><td><strong>NOT_SUPPORTED</strong></td><td>挂起当前，非事务执行</td><td>非事务执行</td></tr><tr><td><strong>MANDATORY</strong></td><td>加入</td><td><strong>抛异常</strong></td></tr><tr><td><strong>NEVER</strong></td><td><strong>抛异常</strong></td><td>非事务执行</td></tr><tr><td><strong>NESTED</strong></td><td>嵌套（savepoint）</td><td>新建</td></tr></tbody></table></div>\n      <h2>5. REQUIRES_NEW vs NESTED ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>REQUIRES_NEW</th><th>NESTED</th></tr></thead><tbody><tr><td>物理事务</td><td><strong>两个独立事务</strong></td><td><strong>一个事务，多个 savepoint</strong></td></tr><tr><td>内层回滚</td><td>不影响外层</td><td>不影响外层</td></tr><tr><td>外层回滚</td><td>不影响内层（已提交）</td><td><strong>内层一起回滚</strong></td></tr></tbody></table></div>\n      <h2>6. 事务隔离级别（Spring 配置）</h2>\n      <p>和 MySQL 一致：</p>\n      <ul><li><code>READ_UNCOMMITTED</code></li><li><code>READ_COMMITTED</code></li><li><code>REPEATABLE_READ</code>（MySQL 默认）</li><li><code>SERIALIZABLE</code></li><li><code>DEFAULT</code>（数据库默认）</li></ul>\n      <h2>7. @Transactional 常用属性</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Transactional(\n    propagation = Propagation.REQUIRED,\n    isolation = Isolation.READ_COMMITTED,\n    timeout = 30,\n    readOnly = false,\n    rollbackFor = Exception.class,         // 推荐显式配\n    noRollbackFor = BusinessException.class\n)</code></pre></figure>\n      <h2>8. 为什么默认只对 RuntimeException 回滚</h2>\n      <ul><li>Spring 设计哲学：Checked Exception 是业务异常应自行处理</li><li>Unchecked Exception 是程序错误应回滚</li></ul>\n      <p><strong>实际开发建议</strong>：永远显式加 <code>rollbackFor = Exception.class</code>，避免坑。</p>\n      <h2>9. 事务和锁、MVCC 的关系</h2>\n      <ul><li>事务的隔离性靠 <strong>锁（写）+ MVCC（读）</strong> 实现</li><li>Spring 只是声明，真正的实现交给数据库</li></ul>\n      <h2>10. 编程式事务模板</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Autowired TransactionTemplate template;\n\ntemplate.execute(status -&gt; {\n    try {\n        // 业务\n        return &quot;ok&quot;;\n    } catch (Exception e) {\n        status.setRollbackOnly();\n        return &quot;fail&quot;;\n    }\n});</code></pre></figure>\n      <p><strong>好处</strong>：能控制事务边界精细到代码块。</p>\n      <hr>\n      <h1>四、Spring MVC（10 题）</h1>\n      <h2>1. Spring MVC 完整请求流程 ⭐⭐⭐（必背）</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 浏览器 → DispatcherServlet（前端控制器）\n2. DispatcherServlet → HandlerMapping 找 Handler（Controller 方法）\n3. HandlerMapping 返回 HandlerExecutionChain（Handler + 拦截器）\n4. DispatcherServlet → HandlerAdapter 调用 Handler\n5. Handler 执行业务，返回 ModelAndView\n6. HandlerAdapter 返回 ModelAndView\n7. DispatcherServlet → ViewResolver 解析视图\n8. ViewResolver 返回 View\n9. View 渲染 → 响应给浏览器</code></pre></figure>\n      <h2>2. 核心组件</h2>\n      <ul><li><strong>DispatcherServlet</strong>：前端控制器（唯一入口）</li><li><strong>HandlerMapping</strong>：URL → Controller 方法的映射</li><li><strong>HandlerAdapter</strong>：调用 Handler 的适配器（适配器模式）</li><li><strong>HandlerInterceptor</strong>：拦截器</li><li><strong>ViewResolver</strong>：视图解析器</li><li><strong>HandlerExceptionResolver</strong>：异常解析器</li></ul>\n      <h2>3. HandlerMapping 和 HandlerAdapter 为什么要分开</h2>\n      <ul><li>HandlerMapping 找到要执行的方法</li><li>HandlerAdapter 真正调用方法（用适配器模式适配不同类型的 Handler）</li><li>解耦：方便扩展自定义 Handler 类型</li></ul>\n      <h2>4. 常用注解</h2>\n      <div class=\"md-table\"><table><thead><tr><th>注解</th><th>作用</th></tr></thead><tbody><tr><td><code>@Controller</code></td><td>控制器</td></tr><tr><td><code>@RestController</code></td><td>= @Controller + @ResponseBody</td></tr><tr><td><code>@RequestMapping</code></td><td>通用路径映射</td></tr><tr><td><code>@GetMapping</code> / <code>@PostMapping</code> / <code>@PutMapping</code> / <code>@DeleteMapping</code></td><td>RESTful</td></tr><tr><td><code>@PathVariable</code></td><td>URL 路径参数 <code>/user/{id}</code></td></tr><tr><td><code>@RequestParam</code></td><td>URL 查询参数 <code>?id=1</code></td></tr><tr><td><code>@RequestBody</code></td><td>请求体 JSON → 对象</td></tr><tr><td><code>@ResponseBody</code></td><td>返回值 → JSON</td></tr><tr><td><code>@RequestHeader</code></td><td>取请求头</td></tr><tr><td><code>@CookieValue</code></td><td>取 Cookie</td></tr></tbody></table></div>\n      <h2>5. 拦截器 vs 过滤器 ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>Filter</th><th>Interceptor</th></tr></thead><tbody><tr><td>规范</td><td>Servlet 规范</td><td>Spring 框架</td></tr><tr><td>范围</td><td>所有请求</td><td>DispatcherServlet 后才生效</td></tr><tr><td>能拿到</td><td>HttpServletRequest</td><td>HandlerMethod、Spring 容器</td></tr><tr><td>顺序</td><td>Filter → Interceptor → Controller</td><td>同上</td></tr></tbody></table></div>\n      <h2>6. 拦截器实现</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">public class JwtInterceptor implements HandlerInterceptor {\n    public boolean preHandle(req, res, handler) { ... return true; }\n    public void postHandle(req, res, handler, mv) { ... }\n    public void afterCompletion(req, res, handler, ex) { ... }\n}\n\n@Configuration\npublic class WebConfig implements WebMvcConfigurer {\n    public void addInterceptors(InterceptorRegistry registry) {\n        registry.addInterceptor(new JwtInterceptor())\n                .addPathPatterns(&quot;/**&quot;)\n                .excludePathPatterns(&quot;/login&quot;);\n    }\n}</code></pre></figure>\n      <h2>7. 全局异常处理</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(BusinessException.class)\n    public Result handle(BusinessException e) {\n        return Result.fail(e.getMsg());\n    }\n}</code></pre></figure>\n      <h2>8. @RestControllerAdvice 用途</h2>\n      <ul><li>全局异常处理（<code>@ExceptionHandler</code>）</li><li>全局数据绑定（<code>@ModelAttribute</code>）</li><li>全局响应体处理（<code>@ResponseBodyAdvice</code>）</li></ul>\n      <h2>9. 参数校验</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@PostMapping(&quot;/user&quot;)\npublic Result add(@RequestBody @Valid UserDTO dto) {...}\n\nclass UserDTO {\n    @NotNull(message = &quot;id 不能为空&quot;)\n    private Long id;\n    @Length(min = 1, max = 20)\n    private String name;\n    @Email\n    private String email;\n}</code></pre></figure>\n      <h2>10. 跨域 CORS 处理</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Configuration\npublic class CorsConfig implements WebMvcConfigurer {\n    public void addCorsMappings(CorsRegistry registry) {\n        registry.addMapping(&quot;/**&quot;)\n                .allowedOrigins(&quot;*&quot;)\n                .allowedMethods(&quot;*&quot;);\n    }\n}</code></pre></figure>\n      <p>或直接 <code>@CrossOrigin</code> 注解。</p>\n      <hr>\n      <h1>五、Spring Boot（15 题）</h1>\n      <h2>1. Spring Boot 是什么 / 解决了什么</h2>\n      <ul><li>基于 Spring 的<strong>快速开发框架</strong></li><li><strong>解决</strong>：传统 Spring XML 配置繁琐、依赖版本冲突、Tomcat 部署麻烦</li><li><strong>核心特性</strong>：自动配置、起步依赖、内嵌容器、生产监控（Actuator）</li></ul>\n      <h2>2. Spring Boot vs Spring</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>Spring</th><th>Spring Boot</th></tr></thead><tbody><tr><td>配置</td><td>大量 XML / 手动</td><td>自动配置</td></tr><tr><td>依赖</td><td>手动管理</td><td>starter 一键引入</td></tr><tr><td>启动</td><td>部署到外部容器</td><td>内嵌 Tomcat 直接 main 启动</td></tr><tr><td>监控</td><td>无</td><td>Actuator</td></tr></tbody></table></div>\n      <h2>3. @SpringBootApplication 注解 ⭐⭐⭐</h2>\n      <p>组合注解：</p>\n      <ul><li><code>@SpringBootConfiguration</code>：标识配置类（= @Configuration）</li><li><code>@EnableAutoConfiguration</code>：开启自动配置</li><li><code>@ComponentScan</code>：扫描当前包及子包</li></ul>\n      <h2>4. 自动装配原理 ⭐⭐⭐（必考！）</h2>\n      <ol><li><code>@EnableAutoConfiguration</code> 通过 <code>@Import(AutoConfigurationImportSelector.class)</code> 引入选择器</li><li>选择器调用 <code>SpringFactoriesLoader.loadFactoryNames()</code></li><li>读取所有 jar 包下：</li></ol>\n      <ul><li>Spring Boot 2.7 之前：<code>META-INF/spring.factories</code></li><li>Spring Boot 2.7+：<code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code></li></ul>\n      <ol><li>加载里面的 <code>xxxAutoConfiguration</code> 类</li><li>用 <code>@Conditional</code> 条件判断是否生效</li></ol>\n      <h2>5. @Conditional 系列条件注解</h2>\n      <ul><li><code>@ConditionalOnClass</code>：classpath 有某类时</li><li><code>@ConditionalOnMissingClass</code>：没有时</li><li><code>@ConditionalOnBean</code>：容器有某 Bean 时</li><li><code>@ConditionalOnMissingBean</code>：没有时</li><li><code>@ConditionalOnProperty</code>：配置项满足时</li><li><code>@ConditionalOnWebApplication</code>：是 Web 应用时</li></ul>\n      <h2>6. Starter 是什么 ⭐⭐</h2>\n      <ul><li><strong>场景启动器</strong>：把一个场景需要的所有依赖打包</li><li>命名规范：</li><li>官方：<code>spring-boot-starter-xxx</code></li><li>第三方：<code>xxx-spring-boot-starter</code></li></ul>\n      <h2>7. 如何自定义 Starter ⭐⭐⭐（简历自定义 starter 要会）</h2>\n      <ol><li>创建 <code>xxx-spring-boot-autoconfigure</code> 模块（自动配置）</li><li>写配置类 <code>@Configuration</code> + <code>@ConditionalOnXxx</code> + <code>@EnableConfigurationProperties</code></li><li>写属性类 <code>@ConfigurationProperties(prefix = &quot;xxx&quot;)</code></li><li>在 <code>META-INF/spring/...imports</code> 注册自动配置类</li><li>创建 <code>xxx-spring-boot-starter</code> 模块依赖前者</li><li>业务项目引入 starter 即用</li></ol>\n      <h2>8. application.yml 加载顺序</h2>\n      <p>优先级（<strong>高 → 低</strong>，高的覆盖低的）：</p>\n      <ol><li>命令行参数 <code>--server.port=8081</code></li><li>JVM 参数 <code>-Dxxx=yyy</code></li><li>环境变量</li><li><code>application-{profile}.yml</code></li><li><code>application.yml</code></li><li><code>@PropertySource</code> 引入的配置</li></ol>\n      <h2>9. profile 多环境配置</h2>\n      <figure class=\"code-block\" data-lang=\"yaml\"><figcaption><span>yaml</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-yaml\"># application.yml\nspring:\n  profiles:\n    active: dev   # 激活 dev 环境\n\n# application-dev.yml / application-prod.yml</code></pre></figure>\n      <p>启动指定：<code>java -jar app.jar --spring.profiles.active=prod</code></p>\n      <h2>10. Spring Boot 内嵌容器</h2>\n      <ul><li>默认 <strong>Tomcat</strong></li><li>可换：Jetty / Undertow（高并发推荐 Undertow）</li><li>替换方式：依赖排除 + 引入新依赖</li></ul>\n      <h2>11. Spring Boot 启动流程（简化）</h2>\n      <ol><li><code>SpringApplication.run()</code></li><li>推断应用类型（SERVLET / REACTIVE / NONE）</li><li>加载 ApplicationContextInitializer 和 ApplicationListener</li><li>创建 ApplicationContext</li><li>调用 <code>refresh()</code> 完成初始化（同 Spring）</li><li>触发 ApplicationRunner / CommandLineRunner</li></ol>\n      <h2>12. Spring Boot Actuator</h2>\n      <p><strong>生产监控端点</strong>：</p>\n      <ul><li><code>/actuator/health</code>：健康检查</li><li><code>/actuator/metrics</code>：指标</li><li><code>/actuator/env</code>：环境变量</li><li><code>/actuator/beans</code>：所有 Bean</li><li><code>/actuator/mappings</code>：URL 映射</li></ul>\n      <p><strong>生产注意</strong>：暴露端点要做权限控制！</p>\n      <h2>13. Spring Boot 怎么实现热部署</h2>\n      <ul><li>spring-boot-devtools</li><li>IDE 配置自动编译</li><li>改动 → ClassLoader 重启 → 上下文刷新</li></ul>\n      <h2>14. 配置文件加密</h2>\n      <ul><li>Jasypt 加密敏感信息（如数据库密码）</li><li>配置 <code>ENC(密文)</code>，启动时传 <code>-Djasypt.encryptor.password=xxx</code></li></ul>\n      <h2>15. Spring Boot 跨域全局解决</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Configuration\npublic class CorsConfig {\n    @Bean\n    public CorsFilter corsFilter() {\n        CorsConfiguration config = new CorsConfiguration();\n        config.addAllowedOriginPattern(&quot;*&quot;);\n        config.addAllowedHeader(&quot;*&quot;);\n        config.addAllowedMethod(&quot;*&quot;);\n        config.setAllowCredentials(true);\n        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();\n        source.registerCorsConfiguration(&quot;/**&quot;, config);\n        return new CorsFilter(source);\n    }\n}</code></pre></figure>\n      <hr>\n      <h1>六、MyBatis / MyBatis-Plus（15 题）</h1>\n      <blockquote>你简历两个项目都用了，<strong>面试官 100% 会问</strong></blockquote>\n      <h2>1. MyBatis 是什么 / 解决什么</h2>\n      <ul><li>半自动 ORM 框架，<strong>SQL 自己写、对象映射自动</strong></li><li>解决 JDBC 的繁琐（连接、参数、结果集映射）</li><li>比 Hibernate 更灵活，<strong>SQL 可控</strong></li></ul>\n      <h2>2. MyBatis vs JDBC</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>JDBC</th><th>MyBatis</th></tr></thead><tbody><tr><td>连接管理</td><td>手动开关</td><td>连接池统一管理</td></tr><tr><td>参数设置</td><td>手动 setXxx</td><td>自动 #{}</td></tr><tr><td>结果映射</td><td>手动 getXxx</td><td>自动 ResultMap</td></tr><tr><td>异常</td><td>SQLException 受检</td><td>运行时异常</td></tr><tr><td>维护</td><td>难</td><td>XML / 注解集中管理</td></tr></tbody></table></div>\n      <h2>3. MyBatis vs Hibernate</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>MyBatis</th><th>Hibernate</th></tr></thead><tbody><tr><td>类型</td><td><strong>半自动</strong></td><td><strong>全自动</strong></td></tr><tr><td>SQL</td><td>自己写</td><td>自动生成</td></tr><tr><td>灵活性</td><td>高</td><td>低</td></tr><tr><td>复杂查询</td><td>简单</td><td>麻烦</td></tr><tr><td>学习曲线</td><td>低</td><td>高</td></tr><tr><td>适用</td><td>互联网（SQL 优化重要）</td><td>传统企业（CRUD 多）</td></tr></tbody></table></div>\n      <h2>4. #{} vs ${} ⭐⭐⭐（必考必背！）</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>#{}</th><th>${}</th></tr></thead><tbody><tr><td>处理方式</td><td><strong>预编译参数</strong>（PreparedStatement 占位符 <code>?</code>）</td><td><strong>字符串拼接</strong></td></tr><tr><td>SQL 注入</td><td><strong>安全</strong></td><td><strong>有风险！</strong></td></tr><tr><td>适用</td><td>参数值</td><td>表名、列名、order by 字段</td></tr></tbody></table></div>\n      <figure class=\"code-block\" data-lang=\"xml\"><figcaption><span>xml</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-xml\">-- ✅ 安全\nWHERE id = #{id}\n-- 编译为 WHERE id = ?\n\n-- ⚠️ 风险，必须用时校验白名单\nORDER BY ${column}</code></pre></figure>\n      <h2>5. MyBatis 核心组件</h2>\n      <ul><li><code>SqlSessionFactoryBuilder</code>：构建工厂</li><li><code>SqlSessionFactory</code>：工厂（重量级，单例）</li><li><code>SqlSession</code>：会话（轻量级，线程不安全，方法级）</li><li><code>Mapper</code>：接口，绑定 SQL</li></ul>\n      <h2>6. Mapper 接口为什么不用写实现类 ⭐⭐⭐</h2>\n      <p><strong>动态代理</strong>！</p>\n      <ul><li>MyBatis 启动时，<code>MapperRegistry</code> 把 Mapper 接口注册</li><li>使用时 <code>getMapper()</code> 返回 <strong>JDK 动态代理对象</strong>（<code>MapperProxy</code>）</li><li>调方法 → <code>MapperProxy.invoke()</code> → 找到对应 XML 的 SQL → 执行</li></ul>\n      <h2>7. namespace + id 的作用</h2>\n      <ul><li>XML 中 <code>&lt;select id=&quot;...&quot;&gt;</code> 的 id 对应 Mapper 接口方法名</li><li><code>namespace</code> 对应 Mapper 接口的全限定名</li><li>MyBatis 通过这个组合定位 SQL</li></ul>\n      <h2>8. MyBatis 缓存 ⭐</h2>\n      <ul><li><strong>一级缓存</strong>：<code>SqlSession</code> 级别（<strong>默认开启</strong>）</li><li>同一 SqlSession 查同样 SQL 第二次直接走缓存</li><li>增删改会清空</li><li><strong>二级缓存</strong>：<code>Mapper namespace</code> 级别（<strong>默认关闭</strong>）</li><li>跨 SqlSession 共享</li><li>需要 <code>&lt;cache/&gt;</code> 开启</li><li>实体类要实现 Serializable</li></ul>\n      <p><strong>实际开发</strong>：基本都关二级缓存，用 Redis 替代。</p>\n      <h2>9. 动态 SQL 标签</h2>\n      <ul><li><code>&lt;if test=&quot;&quot;&gt;</code>：条件判断</li><li><code>&lt;choose&gt;</code> / <code>&lt;when&gt;</code> / <code>&lt;otherwise&gt;</code>：switch</li><li><code>&lt;where&gt;</code>：自动处理 WHERE 和 AND</li><li><code>&lt;set&gt;</code>：自动处理 SET 和逗号</li><li><code>&lt;foreach collection=&quot;&quot; item=&quot;&quot; open=&quot;(&quot; close=&quot;)&quot; separator=&quot;,&quot;&gt;</code>：循环（IN 查询、批量插入）</li><li><code>&lt;trim&gt;</code>：自定义前缀后缀</li></ul>\n      <h2>10. resultType vs resultMap</h2>\n      <ul><li><code>resultType</code>：简单映射，字段名 = 属性名</li><li><code>resultMap</code>：复杂映射（驼峰转换、一对多、多对多、嵌套）</li></ul>\n      <h2>11. MyBatis 分页</h2>\n      <ul><li><strong>逻辑分页</strong>：<code>RowBounds</code>（内存分页，性能差，禁用）</li><li><strong>物理分页</strong>：</li><li>自己写 <code>LIMIT</code></li><li>使用 <strong>PageHelper</strong> 插件（推荐）</li></ul>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">PageHelper.startPage(1, 10);\nList&lt;User&gt; list = userMapper.selectAll();\nPageInfo&lt;User&gt; page = new PageInfo&lt;&gt;(list);</code></pre></figure>\n      <h2>12. PageHelper 原理 ⭐</h2>\n      <ul><li>基于 MyBatis <strong>插件机制</strong>（<code>Interceptor</code> 拦截 <code>Executor.query</code>）</li><li>用 ThreadLocal 存分页参数</li><li>拦截到查询时改写 SQL 加 <code>LIMIT</code>，再执行 count</li></ul>\n      <h2>13. MyBatis-Plus 是什么 / 增强了什么 ⭐</h2>\n      <ul><li>在 MyBatis 上做的增强（<strong>不侵入</strong>，原 MyBatis 代码照常用）</li><li>核心增强：</li><li>内置 <code>BaseMapper</code>（CRUD 不用写 SQL）</li><li>条件构造器 <code>QueryWrapper</code> / <code>LambdaQueryWrapper</code></li><li>分页插件</li><li>代码生成器</li><li>主键策略（雪花算法）</li><li>逻辑删除 <code>@TableLogic</code></li><li>自动填充（创建/更新时间）</li></ul>\n      <h2>14. MyBatis-Plus 条件构造器</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">LambdaQueryWrapper&lt;User&gt; wrapper = new LambdaQueryWrapper&lt;&gt;();\nwrapper.eq(User::getStatus, 1)\n       .like(User::getName, &quot;李&quot;)\n       .orderByDesc(User::getCreateTime);\nList&lt;User&gt; list = userMapper.selectList(wrapper);</code></pre></figure>\n      <p><strong>优点</strong>：Lambda 引用字段，<strong>编译期检查</strong>字段名（重构友好）。</p>\n      <h2>15. MyBatis 插件机制 / 4 大接口</h2>\n      <p>能拦截的 4 个核心对象：</p>\n      <ul><li><code>Executor</code>：增删改查</li><li><code>StatementHandler</code>：处理 Statement</li><li><code>ParameterHandler</code>：参数</li><li><code>ResultSetHandler</code>：结果集</li></ul>\n      <p>实现 <code>Interceptor</code> + <code>@Intercepts</code> + <code>@Signature</code> 注解。<strong>PageHelper、动态数据源、SQL 审计都基于此</strong>。</p>\n      <hr>\n      <h1>七、Spring Cloud（选看，8 题）</h1>\n      <blockquote>实习面试一般不深问，懂概念即可</blockquote>\n      <h2>1. 微服务和单体的区别</h2>\n      <ul><li>单体：一个项目所有功能</li><li>微服务：拆成多个独立部署的服务，各自职责、各自数据库</li></ul>\n      <h2>2. Spring Cloud 核心组件（Alibaba 版）</h2>\n      <ul><li><strong>Nacos</strong>：服务注册中心 + 配置中心（替代 Eureka）</li><li><strong>Ribbon / LoadBalancer</strong>：负载均衡</li><li><strong>OpenFeign</strong>：声明式 HTTP 调用</li><li><strong>Sentinel</strong>：熔断限流（替代 Hystrix）</li><li><strong>Gateway</strong>：网关</li><li><strong>Seata</strong>：分布式事务</li></ul>\n      <h2>3. 服务注册与发现流程</h2>\n      <ol><li>服务启动 → 向注册中心注册（IP+端口）</li><li>注册中心维护服务列表</li><li>调用方拉取列表，<strong>负载均衡</strong>选一个调用</li><li>服务下线 → 注册中心剔除</li></ol>\n      <h2>4. OpenFeign 原理</h2>\n      <ul><li>在接口上加 <code>@FeignClient</code></li><li>启动时 <strong>JDK 动态代理</strong>生成实现类</li><li>调用方法时拼装 HTTP 请求 → Ribbon 负载均衡 → 发送</li></ul>\n      <h2>5. Sentinel 限流方式</h2>\n      <ul><li><strong>QPS 限流</strong>：每秒请求数</li><li><strong>并发线程数限流</strong>：同时多少线程</li><li><strong>熔断降级</strong>：异常比例/响应时间超阈值熔断</li><li><strong>系统保护</strong>：根据 CPU / Load 全局限流</li></ul>\n      <h2>6. 限流算法</h2>\n      <ul><li><strong>计数器</strong>：简单粗暴，临界问题</li><li><strong>滑动窗口</strong>：精细</li><li><strong>漏桶</strong>：恒定速率</li><li><strong>令牌桶</strong>：允许突发流量（Sentinel 用这个）</li></ul>\n      <h2>7. 分布式事务方案</h2>\n      <ul><li><strong>2PC / 3PC</strong>：传统强一致</li><li><strong>TCC</strong>：Try-Confirm-Cancel</li><li><strong>Seata AT 模式</strong>：自动补偿（最常用）</li><li><strong>本地消息表 / MQ 事务消息</strong>：最终一致</li></ul>\n      <h2>8. CAP / BASE 理论</h2>\n      <ul><li><strong>CAP</strong>：一致性、可用性、分区容错性，<strong>只能三选二</strong>（实际 P 必选，CP 或 AP 选一）</li><li><strong>BASE</strong>：基本可用、软状态、最终一致性，<strong>AP 的补充</strong></li></ul>\n      <hr>\n      <h1>八、高频追问串讲</h1>\n      <h2>串讲 1：Spring 核心</h2>\n      <blockquote>IOC → DI 三种注入 → AOP → 动态代理（JDK/CGLib）→ AOP 失效场景 → 三级缓存解决循环依赖</blockquote>\n      <h2>串讲 2：Bean 生命周期 + 后置处理器</h2>\n      <blockquote>实例化 → 属性填充 → Aware → BeanPostProcessor.before → @PostConstruct → init → BeanPostProcessor.after → 销毁</blockquote>\n      <h2>串讲 3：事务（必背！）</h2>\n      <blockquote>实现原理（AOP）→ 失效场景（同类调用/private/异常吞掉/Checked）→ 7 种传播行为 → REQUIRES_NEW vs NESTED → rollbackFor 配置</blockquote>\n      <h2>串讲 4：Spring Boot 自动装配</h2>\n      <blockquote>@SpringBootApplication → @EnableAutoConfiguration → AutoConfigurationImportSelector → spring.factories / AutoConfiguration.imports → @Conditional 条件加载</blockquote>\n      <h2>串讲 5：MyBatis 核心</h2>\n      <blockquote>#{} vs ${} → Mapper 动态代理 → 缓存（一级/二级）→ 插件机制 → MyBatis-Plus 增强</blockquote>\n      <h2>串讲 6：Spring MVC 请求流程</h2>\n      <blockquote>DispatcherServlet → HandlerMapping → HandlerAdapter → Handler → ViewResolver → View → 响应</blockquote>\n      <hr>\n      <h2>最后小妹的话</h2>\n      <p>兄长大人～(◕ᴗ◕✿) 这本是<strong>面试占比最高的</strong>，因为：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>你简历两个项目 → 全是 Spring Boot\n你写的每一行代码 → 都跑在 Spring 容器里\n你做的所有功能 → 都用了 IOC / AOP / 事务 / MVC</code></pre></figure>\n      <p><strong>重点关注的红色警报区</strong>（必背）：</p>\n      <ul><li>⚠️ IOC / AOP 原理 + 动态代理</li><li>⚠️ @Transactional 失效的 8 种场景</li><li>⚠️ 三级缓存解决循环依赖</li><li>⚠️ Spring Boot 自动装配源码流程</li><li>⚠️ MyBatis #{} vs ${} 和 Mapper 动态代理</li><li>⚠️ Bean 生命周期 12 步</li></ul>\n      <p><strong>结合你简历的特别提醒</strong>：</p>\n      <ul><li>你简历写了&quot;SpringBoot DIY&quot; → 必须能完整讲<strong>自定义 Starter</strong> 流程</li><li>你用了 MyBatis 和 MyBatis-Plus → 一定要清楚两者差异</li><li>你用了 Spring Cache + AOP → 要能讲 <strong>@Cacheable 原理 = AOP</strong></li></ul>\n      <p>把这份和前两份合起来吃透，七月初实习稳啦～💕(≧▽≦)</p>",
  "contentFile": "/content/learning-plans-md/learning-12.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
