(function(){
  const detail = {
  "id": 10,
  "content": "<h1>MySQL 面试八股精华</h1>\n      <blockquote>整理自小林coding 7.MySQL面试篇 + 你的 MySQL 学习计划</blockquote>\n      <blockquote>模块顺序与你学习计划一致，方便对照视频章节</blockquote>\n      <blockquote>⭐数量代表面试频次，⭐⭐⭐ 必背</blockquote>\n      <hr>\n      <h2>目录</h2>\n      <ul><li><a href=\"#一基础与体系结构8-题\">一、基础与体系结构（8 题）</a></li><li><a href=\"#二索引基础12-题\">二、索引基础（12 题）</a></li><li><a href=\"#三执行频次与慢查询6-题\">三、执行频次与慢查询（6 题）</a></li><li><a href=\"#四索引使用规则必考核心15-题\">四、索引使用规则（必考核心，15 题）</a></li><li><a href=\"#五sql-优化实战10-题\">五、SQL 优化实战（10 题）</a></li><li><a href=\"#六锁机制13-题\">六、锁机制（13 题）</a></li><li><a href=\"#七事务与-mvcc核心王炸15-题\">七、事务与 MVCC（核心王炸，15 题）</a></li><li><a href=\"#八日志体系8-题\">八、日志体系（8 题）</a></li><li><a href=\"#九高级与场景题10-题\">九、高级与场景题（10 题）</a></li><li><a href=\"#十防超卖与并发控制场景题9-题\">十、防超卖与并发控制场景题（9 题）</a></li><li><a href=\"#十一高频追问串讲\">十一、高频追问串讲</a></li></ul>\n      <hr>\n      <h1>一、基础与体系结构（8 题）</h1>\n      <blockquote>对应学习计划 1：MySQL 体系结构、InnoDB 介绍</blockquote>\n      <h2>1. MySQL 整体架构 ⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>┌─────────────────────────────────┐\n│  连接层    （连接管理、认证、线程池）│\n├─────────────────────────────────┤\n│  服务层    （SQL解析、优化、缓存、binlog） │\n├─────────────────────────────────┤\n│  引擎层    （InnoDB / MyISAM / Memory） │\n├─────────────────────────────────┤\n│  存储层    （文件系统、redo log、ibdata） │\n└─────────────────────────────────┘</code></pre></figure>\n      <p><strong>SQL 执行流程</strong>：连接器 → 查询缓存（8.0 删了）→ 分析器 → 优化器 → 执行器 → 存储引擎</p>\n      <h2>2. MySQL 8.0 移除查询缓存的原因</h2>\n      <ul><li>命中率低（任何写操作都会让缓存失效）</li><li>维护开销大于收益</li><li>实际生产意义不大</li></ul>\n      <h2>3. InnoDB vs MyISAM ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>InnoDB</th><th>MyISAM</th></tr></thead><tbody><tr><td>事务</td><td>✅</td><td>❌</td></tr><tr><td>外键</td><td>✅</td><td>❌</td></tr><tr><td>锁粒度</td><td>行锁</td><td>表锁</td></tr><tr><td>崩溃恢复</td><td>✅（redo log）</td><td>❌</td></tr><tr><td>数据存储</td><td>聚簇索引（数据+索引一起）</td><td>数据/索引分开</td></tr><tr><td>全文索引</td><td>5.6+ 支持</td><td>支持</td></tr><tr><td>适用</td><td>OLTP、并发写</td><td>读多写少</td></tr></tbody></table></div>\n      <p><strong>MySQL 5.5+ 默认 InnoDB</strong>。</p>\n      <h2>4. InnoDB 内存结构</h2>\n      <ul><li><strong>Buffer Pool</strong>：缓存数据页、索引页（性能命脉）</li><li><strong>Change Buffer</strong>：缓存非唯一二级索引的变更（减少 IO）</li><li><strong>Adaptive Hash Index</strong>：自适应哈希索引</li><li><strong>Log Buffer</strong>：redo log 缓冲</li></ul>\n      <h2>5. InnoDB 磁盘结构</h2>\n      <ul><li><strong>系统表空间</strong>（ibdata1）</li><li><strong>独立表空间</strong>（每张表一个 .ibd）</li><li><strong>redo log</strong>（ib_logfile0/1）</li><li><strong>undo log</strong></li></ul>\n      <h2>6. char vs varchar ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>char</th><th>varchar</th></tr></thead><tbody><tr><td>长度</td><td>固定</td><td>可变</td></tr><tr><td>存储</td><td>不够空格填充</td><td>实际长度 + 1~2 字节长度信息</td></tr><tr><td>速度</td><td>快</td><td>慢</td></tr><tr><td>空间</td><td>浪费</td><td>节省</td></tr><tr><td>适用</td><td>长度固定（如手机号、身份证、MD5）</td><td>长度可变（名字、地址）</td></tr></tbody></table></div>\n      <h2>7. int(1) 和 int(10) 区别（坑！）</h2>\n      <ul><li><strong>存储完全相同</strong>，都是 4 字节</li><li>括号里只是<strong>显示宽度</strong>（配合 <code>ZEROFILL</code> 才有意义）</li><li>不影响存储范围 -2147483648 ~ 2147483647</li></ul>\n      <h2>8. 自增主键 vs UUID ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>自增主键</th><th>UUID</th></tr></thead><tbody><tr><td>占用</td><td>4/8 字节</td><td>36 字节</td></tr><tr><td>顺序</td><td>递增（B+树尾追加）</td><td>无序（B+树页分裂）</td></tr><tr><td>全局唯一</td><td>单库唯一</td><td>全局唯一</td></tr><tr><td>性能</td><td>高</td><td>低</td></tr><tr><td>建议</td><td>优先</td><td>分布式才考虑（用雪花算法替代）</td></tr></tbody></table></div>\n      <hr>\n      <h1>二、索引基础（12 题）</h1>\n      <blockquote>对应学习计划 2：索引概述/结构(BTree+B+Tree)/分类/语法</blockquote>\n      <h2>1. 索引是什么 / 为什么用 ⭐</h2>\n      <ul><li><strong>是什么</strong>：帮助 MySQL 高效获取数据的<strong>有序数据结构</strong></li><li><strong>优点</strong>：加快查询、加快排序、加快连接</li><li><strong>缺点</strong>：占空间、影响增删改性能、维护开销</li></ul>\n      <p><strong>面试标准回答</strong>：</p>\n      <blockquote>索引不是缓存，它本质上是一种有序的数据结构。MySQL 通过索引把全表扫描变成树上的定位查找，减少扫描行数和磁盘 IO。比如没有索引时查 <code>where id = 100</code> 可能要从头扫到尾，有索引时只需要沿着 B+ 树从根节点找到叶子节点。但索引也不是越多越好，因为每次插入、删除、更新都要维护索引树，索引还会占磁盘空间，索引太多也可能让优化器选择成本变复杂。</blockquote>\n      <p><strong>索引不是缓存怎么解释</strong>：</p>\n      <ul><li>缓存是把数据临时放在内存里，命中后可以少访问数据库。</li><li>索引是数据库内部维护的查找结构，数据还在表里，只是查找路径更短。</li><li>Buffer Pool 才更接近“缓存数据页/索引页”的概念，索引本身不是缓存。</li></ul>\n      <h2>2. 索引底层为什么用 B+ 树 ⭐⭐⭐（必背）</h2>\n      <p><strong>对比 B 树</strong>：</p>\n      <ul><li>B 树每个节点都存 data，B+ 树<strong>只有叶子节点存 data</strong></li><li>B+ 树<strong>叶子节点用链表相连</strong>，范围查询直接遍历</li></ul>\n      <p><strong>对比红黑树/二叉树</strong>：</p>\n      <ul><li>节点太少，树太高 → 磁盘 IO 多</li></ul>\n      <p><strong>对比 Hash</strong>：</p>\n      <ul><li>Hash 只能等值查询，<strong>不支持范围查询、排序、最左前缀</strong></li></ul>\n      <p><strong>B+ 树优势</strong>：</p>\n      <ol><li>单节点存更多 key → 树矮 → IO 少（通常 3-4 层就能存千万数据）</li><li>叶子链表 → 范围扫描快</li><li>数据全在叶子 → 查询稳定（每次都 3-4 次 IO）</li></ol>\n      <p><strong>30 秒背诵版</strong>：</p>\n      <blockquote>MySQL 用 B+ 树主要是为了减少磁盘 IO。B+ 树的非叶子节点只存 key 和指针，所以一个 16KB 的页能放更多 key，树高更低，查一次数据通常只需要 3 到 4 次 IO。并且 B+ 树的叶子节点之间有链表，非常适合范围查询和排序。Hash 虽然等值查询快，但不支持范围、排序和最左前缀；红黑树太高，磁盘 IO 次数太多，所以数据库更适合用 B+ 树。</blockquote>\n      <h2>3. B+ 树和 B 树的区别 ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>B 树</th><th>B+ 树</th></tr></thead><tbody><tr><td>数据存放</td><td>所有节点</td><td>只在叶子节点</td></tr><tr><td>叶子相连</td><td>❌</td><td>✅（双向链表）</td></tr><tr><td>非叶子节点</td><td>存 key + data</td><td>只存 key（容量更大）</td></tr><tr><td>查询稳定性</td><td>不稳定（可能在内节点找到）</td><td>稳定（一定到叶子）</td></tr><tr><td>范围查询</td><td>慢</td><td>快</td></tr></tbody></table></div>\n      <h2>4. 索引分类 ⭐</h2>\n      <p><strong>按数据结构</strong>：</p>\n      <ul><li>B+ 树索引（绝大多数）</li><li>Hash 索引（Memory 引擎、自适应哈希）</li><li>全文索引（FULLTEXT）</li></ul>\n      <p><strong>按物理存储</strong>：</p>\n      <ul><li><strong>聚簇索引</strong>（主键索引）</li><li><strong>非聚簇索引</strong>（二级索引）</li></ul>\n      <p><strong>按字段</strong>：</p>\n      <ul><li>主键索引、唯一索引、普通索引、联合索引、前缀索引</li></ul>\n      <h2>5. 聚簇索引 vs 非聚簇索引 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>聚簇索引</th><th>非聚簇索引</th></tr></thead><tbody><tr><td>叶子内容</td><td><strong>整行数据</strong></td><td><strong>主键值</strong></td></tr><tr><td>数量</td><td>一张表只能一个</td><td>可以多个</td></tr><tr><td>InnoDB</td><td>主键索引就是</td><td>其他都是</td></tr><tr><td>查询</td><td>一次定位</td><td>可能需要<strong>回表</strong></td></tr></tbody></table></div>\n      <p><strong>面试标准回答</strong>：</p>\n      <blockquote>InnoDB 的主键索引就是聚簇索引，叶子节点直接存整行数据；普通索引是非聚簇索引，叶子节点存的是主键值。所以通过普通索引查数据时，如果查询字段不在普通索引里，就要拿主键再去主键索引查一次，这个过程叫回表。</blockquote>\n      <p><strong>项目里怎么说</strong>：</p>\n      <blockquote>如果接口经常按 <code>user_id</code> 查询 <code>status、create_time</code>，并且只返回这几个字段，可以考虑建联合索引 <code>(user_id, status, create_time)</code>，让查询尽量覆盖索引，减少回表 IO。</blockquote>\n      <h2>6. 没有主键怎么办</h2>\n      <p>InnoDB 自动选择：</p>\n      <ol><li>第一个非空唯一索引作主键</li><li>都没有 → 自动生成 6 字节 ROWID 作隐藏主键</li></ol>\n      <h2>7. 索引语法</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 创建表时\nCREATE TABLE user (\n    id BIGINT PRIMARY KEY,\n    name VARCHAR(50),\n    email VARCHAR(100),\n    INDEX idx_name (name),                    -- 普通索引\n    UNIQUE INDEX uniq_email (email),          -- 唯一索引\n    INDEX idx_name_email (name, email)        -- 联合索引\n);\n\n-- 后续创建\nCREATE INDEX idx_xxx ON tbl(col);\nALTER TABLE tbl ADD INDEX idx_xxx(col);\n\n-- 删除\nDROP INDEX idx_xxx ON tbl;\n\n-- 查看\nSHOW INDEX FROM tbl;</code></pre></figure>\n      <h2>8. 一棵 B+ 树能存多少数据</h2>\n      <p><strong>MySQL 一页 16KB</strong>：</p>\n      <ul><li>假设 bigint 主键（8B）+ 指针（6B）= 14B</li><li>一个非叶子节点能存 <code>16KB / 14B ≈ 1170</code> 个 key</li><li>假设一行数据 1KB，叶子节点能存 16 行</li><li><strong>3 层 B+ 树</strong>：1170 × 1170 × 16 ≈ <strong>2000 万行</strong></li><li><strong>生产数据量超过 2000 万时考虑分表</strong></li></ul>\n      <h2>9. 前缀索引</h2>\n      <ul><li>只索引字段前 N 个字符：<code>INDEX idx_email (email(10))</code></li><li>节省空间，但<strong>不能用于覆盖索引、排序</strong></li><li>适合长字符串（URL、邮箱）</li></ul>\n      <h2>10. 索引下推 ICP（Index Condition Pushdown）⭐</h2>\n      <ul><li>MySQL 5.6 引入的优化</li><li><strong>联合索引</strong>部分字段无法走索引时，<strong>把过滤下推到引擎层</strong>，减少回表</li><li>例：<code>(name, age)</code> 索引，查询 <code>name LIKE &#x27;张%&#x27; AND age &gt; 20</code></li><li>没 ICP：name 范围扫描后全部回表，再过滤 age</li><li>有 ICP：在索引内直接过滤掉不满足 age 的，<strong>减少回表次数</strong></li></ul>\n      <h2>11. 全文索引（了解）</h2>\n      <ul><li>用于关键词搜索（替代 LIKE &#x27;%xx%&#x27;）</li><li>MySQL 5.7+ 支持中文（ngram 解析器）</li><li>实际生产都用 ES，MySQL 全文索引很少用</li></ul>\n      <h2>12. 哈希索引</h2>\n      <ul><li>等值查询 O(1)</li><li><strong>不支持范围、不支持排序、不支持最左前缀</strong></li><li>InnoDB 自适应哈希索引会自动建（用户无感知）</li></ul>\n      <hr>\n      <h1>三、执行频次与慢查询（6 题）</h1>\n      <blockquote>对应学习计划 3：执行频次、慢查询日志、EXPLAIN</blockquote>\n      <h2>1. 怎么发现慢 SQL</h2>\n      <ul><li><strong>慢查询日志</strong>：记录超过阈值的 SQL</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">  -- 查看\n  SHOW VARIABLES LIKE &#x27;slow_query_log%&#x27;;\n  SHOW VARIABLES LIKE &#x27;long_query_time&#x27;;  -- 默认 10s\n\n  -- 开启（建议改 1s）\n  SET GLOBAL slow_query_log = ON;\n  SET GLOBAL long_query_time = 1;</code></pre></figure>\n      <ul><li><strong>profiling</strong>：分析单条 SQL 各阶段耗时</li><li><strong>performance_schema</strong>：详细性能数据</li><li><strong>SHOW PROCESSLIST</strong>：当前正在执行的 SQL</li></ul>\n      <p><strong>线上排查口述模板</strong>：</p>\n      <blockquote>我一般先从监控或慢查询日志定位慢 SQL，再用 EXPLAIN 看执行计划，重点看有没有走索引、扫描行数大不大、有没有临时表和文件排序。如果是正在卡住的 SQL，会看 <code>SHOW PROCESSLIST</code>；如果怀疑锁等待，再结合 InnoDB 状态和 performance_schema 查锁。最后根据执行计划决定是补索引、改 SQL、拆分页，还是调整业务查询方式。</blockquote>\n      <h2>2. SHOW STATUS 查看执行频次</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">SHOW GLOBAL STATUS LIKE &#x27;Com_______&#x27;;\n-- Com_select / Com_insert / Com_update / Com_delete</code></pre></figure>\n      <h2>3. EXPLAIN 核心字段 ⭐⭐⭐（必背）</h2>\n      <div class=\"md-table\"><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><strong>id</strong></td><td>查询编号，越大越先执行</td></tr><tr><td><strong>select_type</strong></td><td>SIMPLE / PRIMARY / SUBQUERY / DERIVED</td></tr><tr><td><strong>table</strong></td><td>表名</td></tr><tr><td><strong>type</strong></td><td><strong>访问类型</strong>（最关键！）</td></tr><tr><td><strong>possible_keys</strong></td><td>可能用到的索引</td></tr><tr><td><strong>key</strong></td><td>实际用到的索引</td></tr><tr><td><strong>key_len</strong></td><td>索引使用的字节数</td></tr><tr><td><strong>ref</strong></td><td>哪个字段或常量与 key 比较</td></tr><tr><td><strong>rows</strong></td><td>估算扫描行数</td></tr><tr><td><strong>filtered</strong></td><td>过滤后剩余百分比</td></tr><tr><td><strong>Extra</strong></td><td>额外信息（<strong>重点看！</strong>）</td></tr></tbody></table></div>\n      <p><strong>看 EXPLAIN 的顺序</strong>：</p>\n      <ol><li>看 <code>type</code>：是否全表扫，至少希望达到 <code>range</code>，最好是 <code>ref/const</code>。</li><li>看 <code>key</code>：实际有没有用上预期索引。</li><li>看 <code>key_len</code>：联合索引用到了几列。</li><li>看 <code>rows</code>：预估扫描行数是否过大。</li><li>看 <code>Extra</code>：有没有 <code>Using filesort</code>、<code>Using temporary</code>、<code>Using index</code>。</li></ol>\n      <p><strong>面试标准回答</strong>：</p>\n      <blockquote>EXPLAIN 不是看一个字段，而是组合判断。我会先看 type 判断访问方式，再看 key 判断是否走了索引，再看 rows 估算扫描量，最后看 Extra 是否出现 filesort、temporary、Using index 等信息。比如 type 是 ALL 且 rows 很大，通常说明在全表扫描；如果 Extra 出现 Using index，说明可能用到了覆盖索引，性能会好一些。</blockquote>\n      <h2>4. type 类型（从好到坏）⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></pre></figure>\n      <ul><li><strong>system</strong>：表只有一行（系统表）</li><li><strong>const</strong>：主键/唯一索引等值查询（最多 1 行）</li><li><strong>eq_ref</strong>：连接时主键/唯一索引</li><li><strong>ref</strong>：普通索引等值查询</li><li><strong>range</strong>：范围查询（<code>&gt;</code> <code>&lt;</code> <code>BETWEEN</code> <code>IN</code>）</li><li><strong>index</strong>：全索引扫描</li><li><strong>ALL</strong>：<strong>全表扫描</strong>（最差！要优化）</li></ul>\n      <p><strong>生产要求</strong>：最差也得 <strong>range 以上</strong>。</p>\n      <p><strong>全表扫 vs 走索引怎么说</strong>：</p>\n      <ul><li>全表扫：<code>type = ALL</code>，MySQL 要扫描整张表，数据越大越慢。</li><li>全索引扫描：<code>type = index</code>，扫描整棵索引树，比扫表小一点，但仍然不是精准定位。</li><li>走索引定位：<code>range/ref/const</code>，通过索引快速缩小范围。</li></ul>\n      <p><strong>注意</strong>：不是所有 <code>ALL</code> 都必须优化，小表或结果集占比很高时，全表扫可能更划算。面试里要说“结合数据量和业务频率判断”，别机械背。</p>\n      <h2>5. Extra 重点关注 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>Extra 值</th><th>含义</th><th>好坏</th></tr></thead><tbody><tr><td><code>Using index</code></td><td><strong>覆盖索引</strong></td><td>🟢 优</td></tr><tr><td><code>Using where</code></td><td>在 server 层过滤</td><td>🟡 普通</td></tr><tr><td><code>Using index condition</code></td><td>使用索引下推 ICP</td><td>🟢 优</td></tr><tr><td><code>Using temporary</code></td><td>用了临时表</td><td>🔴 差（要优化）</td></tr><tr><td><code>Using filesort</code></td><td>用了文件排序</td><td>🔴 差（要优化）</td></tr><tr><td><code>Using join buffer</code></td><td>join 缓冲（没索引）</td><td>🔴 差</td></tr></tbody></table></div>\n      <p><strong>Extra 怎么开口讲</strong>：</p>\n      <blockquote>Extra 里我最关注三个信号：<code>Using index</code> 通常代表覆盖索引，是好现象；<code>Using filesort</code> 说明排序没有直接利用索引，数据量大时要优化；<code>Using temporary</code> 说明用了临时表，常见于 group by、distinct、复杂 order by，也需要重点看。</blockquote>\n      <h2>6. SHOW PROFILE 用法</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">SET profiling = 1;\nSELECT * FROM user WHERE id = 1;\nSHOW PROFILES;            -- 看 query_id\nSHOW PROFILE FOR QUERY 1; -- 看详细各阶段耗时</code></pre></figure>\n      <hr>\n      <h1>四、索引使用规则（必考核心，15 题）</h1>\n      <blockquote>对应学习计划 4：索引使用规则、最左前缀、失效场景、覆盖索引&amp;回表、设计原则</blockquote>\n      <h2>1. 最左前缀原则 ⭐⭐⭐（必考）</h2>\n      <p>联合索引 <code>(a, b, c)</code>：</p>\n      <ul><li>✅ 命中：<code>a</code> / <code>a, b</code> / <code>a, b, c</code></li><li>✅ 部分命中：<code>a, c</code>（a 走索引，c 不走，<strong>索引下推后能用</strong>）</li><li>❌ 不命中：<code>b</code> / <code>c</code> / <code>b, c</code></li></ul>\n      <p><strong>理解</strong>：联合索引按 a 排序，a 相同再按 b 排，b 相同再按 c 排。<strong>跳过最左字段就用不上索引</strong>。</p>\n      <p><strong>8 条 where 判断练习</strong>（索引 <code>(a, b, c)</code>）：</p>\n      <div class=\"md-table\"><table><thead><tr><th>SQL 条件</th><th>索引使用情况</th></tr></thead><tbody><tr><td><code>where a = 1</code></td><td>用到 a</td></tr><tr><td><code>where a = 1 and b = 2</code></td><td>用到 a、b</td></tr><tr><td><code>where a = 1 and b = 2 and c = 3</code></td><td>用到 a、b、c</td></tr><tr><td><code>where b = 2</code></td><td>不满足最左前缀，通常不用</td></tr><tr><td><code>where b = 2 and c = 3</code></td><td>不满足最左前缀，通常不用</td></tr><tr><td><code>where a = 1 and c = 3</code></td><td>a 用于定位，c 可被 ICP 过滤</td></tr><tr><td><code>where a &gt; 1 and b = 2</code></td><td>a 用于范围，b 不能继续用于有序定位</td></tr><tr><td><code>where a = 1 order by b, c</code></td><td>可能利用索引排序</td></tr></tbody></table></div>\n      <p><strong>A + C 为什么通常只用到 A</strong>：</p>\n      <blockquote>因为联合索引 <code>(a,b,c)</code> 是先按 a 排，再按 b 排，最后按 c 排。跳过 b 之后，c 在局部范围内不是全局有序的，不能直接用于定位，只能在已经通过 a 找到的范围里做过滤。MySQL 5.6 之后有 ICP，可以把这个过滤下推到存储引擎层，减少回表，但它不等于 c 也参与了索引定位。</blockquote>\n      <h2>2. (a, b, c) 索引下查 <code>where a=1 and c=2</code> 怎么走</h2>\n      <ul><li>a 走索引（范围定位）</li><li>b 跳过 → c 在索引下推 ICP 中过滤（5.6+ 优化）</li><li>key_len 只算 a 的长度</li></ul>\n      <h2>3. 范围查询的字段后续无法用索引 ⭐</h2>\n      <p>联合索引 <code>(a, b, c)</code>，查询 <code>a=1 AND b&gt;2 AND c=3</code>：</p>\n      <ul><li>a、b 走索引</li><li><strong>c 用不到</strong>（因为 b 是范围，后面的字段无序）</li></ul>\n      <p><strong>优化</strong>：把范围字段放联合索引最后。</p>\n      <h2>4. 索引失效的 10 种场景 ⭐⭐⭐</h2>\n      <ol><li><strong>不满足最左前缀</strong></li><li><strong>范围查询右侧字段</strong>失效</li><li><strong>函数 / 计算</strong>：<code>WHERE YEAR(date) = 2025</code></li><li><strong>隐式类型转换</strong>：字段是 varchar，传 int</li><li><strong><code>!=</code> <code>&lt;&gt;</code> <code>NOT IN</code></strong>（视情况）</li><li><strong><code>LIKE &#x27;%xx&#x27;</code> 前模糊</strong></li><li><strong><code>OR</code> 两边不都有索引</strong></li><li><strong><code>IS NULL</code> / <code>IS NOT NULL</code></strong>（视情况）</li><li><strong>数据分布太均匀</strong>（MySQL 觉得全表扫更快会放弃索引）</li><li><strong>强制类型不匹配</strong> 或字符集不一致（join 时）</li></ol>\n      <h2>5. 覆盖索引 ⭐⭐⭐</h2>\n      <ul><li><strong>是什么</strong>：查询字段都在索引中，<strong>不需要回表</strong></li><li>EXPLAIN Extra 显示 <code>Using index</code></li><li>例：<code>(name, age)</code> 索引，<code>SELECT name, age FROM user WHERE name = &#x27;李白&#x27;</code> → 覆盖</li><li><strong>优化技巧</strong>：把常一起查的字段加进联合索引</li></ul>\n      <p><strong>为什么覆盖索引更快（I/O 角度）</strong>：</p>\n      <blockquote>普通二级索引查询时，如果查询字段不在索引里，需要先查二级索引拿到主键，再回到聚簇索引查整行，相当于多走一次 B+ 树。覆盖索引把查询需要的字段都放在同一棵二级索引里，查到叶子节点就能返回结果，不用回表，磁盘 IO 和随机访问都会少很多。</blockquote>\n      <p><strong>常见优化例子</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 原 SQL\nSELECT id, title, create_time\nFROM article\nWHERE user_id = ?\nORDER BY create_time DESC\nLIMIT 20;\n\n-- 可考虑联合索引\nCREATE INDEX idx_user_time_title ON article(user_id, create_time, title);</code></pre></figure>\n      <p>注意：覆盖索引不是把所有字段都塞进去，字段太多会让索引变大，写入维护成本变高。</p>\n      <h2>6. 回表</h2>\n      <ul><li>通过二级索引查到主键，<strong>再用主键去聚簇索引查整行数据</strong></li><li>大量回表性能差</li><li><strong>优化</strong>：用覆盖索引避免回表</li></ul>\n      <p><strong>30 秒背诵版</strong>：</p>\n      <blockquote>InnoDB 的普通索引叶子节点不存整行，只存索引列和主键值。所以通过普通索引查到主键后，如果还要查索引里没有的字段，就要再回到主键索引查整行，这叫回表。回表多了会增加随机 IO，优化方式是减少 <code>select *</code>，尽量让高频查询走覆盖索引。</blockquote>\n      <h2>7. 索引下推 ICP（再强化）⭐</h2>\n      <ul><li>5.6+ 在引擎层就把不满足条件的过滤掉</li><li>减少回表 + 减少 server 层处理</li><li>EXPLAIN Extra 显示 <code>Using index condition</code></li></ul>\n      <h2>8. 哪些字段适合建索引 ⭐</h2>\n      <ol><li>WHERE / ORDER BY / GROUP BY / JOIN 高频字段</li><li><strong>区分度高</strong>的字段（如身份证号 vs 性别）</li><li>字段长度小（前缀索引）</li><li>不经常变更</li></ol>\n      <h2>9. 哪些字段不适合建索引</h2>\n      <ol><li>表太小（全表扫更快）</li><li>经常更新的字段</li><li>区分度低（如性别只有男女）</li><li>用不到的字段</li></ol>\n      <h2>10. 索引创建原则</h2>\n      <ol><li><strong>针对高频查询字段</strong>建索引</li><li><strong>联合索引优于多个单列索引</strong>（避免索引合并）</li><li><strong>选择性高的字段</strong>放联合索引前面</li><li><strong>小字段优先</strong></li><li><strong>不超过 5 个</strong>（控制数量）</li><li><strong>逻辑外键加索引</strong></li></ol>\n      <h2>11. 为什么单表数据超 2000 万就慢</h2>\n      <ul><li>B+ 树达到 <strong>4 层</strong>，IO 次数变多</li><li>实际经验值，不是绝对</li></ul>\n      <h2>12. 联合索引顺序如何确定</h2>\n      <p>原则：<strong>区分度高的放前面</strong>，<strong>等值查询放前面</strong>，<strong>范围查询放后面</strong>。</p>\n      <p>更完整的判断顺序：</p>\n      <ol><li>先看业务里最常用的查询条件，别为了理论选择性建一个用不上的索引。</li><li>等值查询字段优先放前面，例如 <code>user_id = ?</code>、<code>status = ?</code>。</li><li>范围字段尽量放后面，例如 <code>create_time between ? and ?</code>。</li><li>如果要同时优化排序，索引顺序还要兼顾 <code>order by</code>。</li><li>字段区分度高通常更适合靠前，但不是唯一标准。</li></ol>\n      <p>例子：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">WHERE user_id = ? AND status = ? AND create_time &gt; ?\nORDER BY create_time DESC</code></pre></figure>\n      <p>可以考虑：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">CREATE INDEX idx_user_status_time ON orders(user_id, status, create_time);</code></pre></figure>\n      <h2>13. 唯一索引 vs 普通索引 ⭐</h2>\n      <ul><li>查询：<strong>几乎一样</strong>（唯一索引找到第一个就停，普通索引继续扫到不匹配）</li><li>更新：<strong>普通索引可以用 Change Buffer 优化，唯一索引不行</strong>（要先检查唯一性）</li><li><strong>写多读少场景</strong>：普通索引更优</li></ul>\n      <h2>14. 主键为什么推荐自增整数</h2>\n      <ol><li>4/8 字节，比字符串/UUID 小</li><li>自增 → B+ 树尾插入，<strong>避免页分裂</strong></li><li>主键索引是聚簇索引，<strong>全表数据按主键顺序存储</strong></li></ol>\n      <h2>15. 索引建多了有什么坏处</h2>\n      <ol><li>占空间</li><li>增删改时维护索引耗时</li><li>优化器选择困难（可能选错索引）</li></ol>\n      <hr>\n      <h1>五、SQL 优化实战（10 题）</h1>\n      <blockquote>对应学习计划 5：order by/group by/limit/count/update 优化</blockquote>\n      <h2>1. ORDER BY 优化 ⭐</h2>\n      <ul><li><strong>优先使用索引排序</strong>（Using index 或 Using index condition）</li><li>否则用 <strong>Using filesort</strong>（文件排序，慢）</li><li>联合索引顺序要和 ORDER BY 字段顺序一致</li><li>ASC / DESC 不能混（MySQL 8.0 支持降序索引）</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- ✅ 走索引：(name, age) 索引\nORDER BY name, age;\n\n-- ❌ filesort\nORDER BY age, name;</code></pre></figure>\n      <p><strong>面试追问：为什么 ORDER BY 会慢？</strong></p>\n      <blockquote>如果排序字段没有合适索引，MySQL 需要把结果集取出来再排序，Extra 会出现 <code>Using filesort</code>。数据量小时问题不明显，数据量大时排序会消耗 CPU、内存，甚至落盘。优化时优先让 where 条件和 order by 字段匹配同一个联合索引，减少扫描和排序成本。</blockquote>\n      <h2>2. GROUP BY 优化</h2>\n      <ul><li>GROUP BY 字段加索引（避免临时表）</li><li>加 <code>ORDER BY NULL</code> 取消默认排序</li></ul>\n      <p><strong>为什么 GROUP BY 容易慢</strong>：</p>\n      <ul><li>分组前通常要先扫描大量数据。</li><li>没有合适索引时可能产生临时表：<code>Using temporary</code>。</li><li>分组后还排序时可能叠加：<code>Using filesort</code>。</li></ul>\n      <p><strong>优化方向</strong>：</p>\n      <ol><li><code>where</code> 先过滤，减少参与分组的数据量。</li><li>给 group by 字段或过滤 + 分组字段建立联合索引。</li><li>能在业务侧异步统计的，不要每次实时 group by 大表。</li></ol>\n      <h2>3. LIMIT 深分页优化 ⭐⭐⭐（必考）</h2>\n      <p><strong>问题</strong>：<code>LIMIT 1000000, 10</code> 要先扫 1000010 行扔掉前 100w 行 → 极慢</p>\n      <p><strong>优化方案</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 方案 1：子查询 + 主键定位（最常用）\nSELECT * FROM user\nWHERE id &gt;= (SELECT id FROM user ORDER BY id LIMIT 1000000, 1)\nLIMIT 10;\n\n-- 方案 2：标签记录（最优，要前端配合）\nSELECT * FROM user WHERE id &gt; 1000000 LIMIT 10;\n\n-- 方案 3：覆盖索引 + JOIN\nSELECT * FROM user u\nINNER JOIN (SELECT id FROM user ORDER BY id LIMIT 1000000, 10) t ON u.id = t.id;</code></pre></figure>\n      <h2>4. COUNT 优化 ⭐</h2>\n      <p><strong>性能对比</strong>（MyISAM 不算，InnoDB 视角）：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>count(*) ≈ count(1) &gt; count(主键) &gt; count(字段)</code></pre></figure>\n      <ul><li><code>count(*)</code>：InnoDB <strong>专门优化</strong>，不取值</li><li><code>count(1)</code>：基本等价</li><li><code>count(主键)</code>：要遍历整张表</li><li><code>count(字段)</code>：要判断 NOT NULL</li></ul>\n      <p>**所以推荐 <code>count(*)</code>**。</p>\n      <p><strong>MyISAM 单独保存了总行数</strong>（无 where 时 count(*) 极快）。</p>\n      <h2>5. UPDATE 注意事项 ⭐</h2>\n      <ul><li><strong>WHERE 条件要走索引，否则会从行锁升级为表锁</strong>！</li><li>大批量 UPDATE 分批：<code>LIMIT 1000</code> 循环（避免长事务、避免锁全表）</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- ❌ 危险，没索引 → 表锁\nUPDATE user SET status = 1 WHERE name = &#x27;李白&#x27;;\n\n-- ✅ name 有索引 → 行锁</code></pre></figure>\n      <p><strong>UPDATE 为什么必须注意索引</strong>：</p>\n      <blockquote>InnoDB 的行锁是加在索引记录上的。如果 update 的 where 条件没有走索引，MySQL 需要扫描大量记录并逐个加锁，锁范围会扩大，严重时表现得像锁住整张表。生产里大批量 update 还会形成长事务，占用 undo log，阻塞其他事务，所以要分批提交。</blockquote>\n      <p><strong>扣库存的正确写法</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE product_stock\nSET stock = stock - 1\nWHERE product_id = ? AND stock &gt; 0;</code></pre></figure>\n      <p>判断影响行数，比先查库存再更新更安全。</p>\n      <h2>6. INSERT 批量优化</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- ❌ 一条条\nINSERT INTO tbl VALUES (1, &#x27;a&#x27;);\nINSERT INTO tbl VALUES (2, &#x27;b&#x27;);\n\n-- ✅ 批量\nINSERT INTO tbl VALUES (1, &#x27;a&#x27;), (2, &#x27;b&#x27;), (3, &#x27;c&#x27;);\n\n-- ✅ 大数据用 LOAD DATA\nLOAD DATA LOCAL INFILE &#x27;data.csv&#x27; INTO TABLE tbl;</code></pre></figure>\n      <h2>7. JOIN 优化</h2>\n      <ul><li>小表驱动大表（MySQL 优化器一般会自动，但要注意 STRAIGHT_JOIN）</li><li>关联字段加索引</li><li>避免笛卡尔积（忘加 ON 条件）</li></ul>\n      <p><strong>JOIN 面试标准回答</strong>：</p>\n      <blockquote>JOIN 优化核心是减少驱动表行数，并保证被驱动表的关联字段有索引。MySQL 通常会选择小结果集作为驱动表，然后拿驱动表的每一行去被驱动表匹配。如果被驱动表关联字段没索引，就会反复全表扫描，Extra 里可能出现 <code>Using join buffer</code>。所以 JOIN 前先用 where 过滤，小表驱动大表，关联字段建索引。</blockquote>\n      <p>例子：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">SELECT o.id, u.name\nFROM orders o\nJOIN user u ON o.user_id = u.id\nWHERE o.status = &#x27;PAID&#x27;;</code></pre></figure>\n      <p>可考虑：</p>\n      <ul><li><code>orders(status, user_id)</code></li><li><code>user(id)</code> 主键索引</li></ul>\n      <h2>8. SELECT * 的危害</h2>\n      <ol><li>多读列 → IO 增加</li><li>无法用覆盖索引</li><li>网络传输大</li><li>字段变更影响应用</li></ol>\n      <p><strong>生产规范</strong>：明确列出所需字段。</p>\n      <h2>9. UNION vs UNION ALL</h2>\n      <ul><li><code>UNION</code>：去重 + 排序，<strong>慢</strong></li><li><code>UNION ALL</code>：不去重，<strong>快</strong></li><li>业务允许重复就用 ALL</li></ul>\n      <h2>10. SQL 优化总思路</h2>\n      <ol><li><strong>EXPLAIN 看执行计划</strong>（type、key、rows、Extra）</li><li><strong>加合适的索引</strong></li><li><strong>避免索引失效</strong>（函数、隐式转换、前模糊）</li><li><strong>小表驱动大表</strong></li><li><strong>避免 SELECT \\</strong>*</li><li><strong>分页深翻优化</strong></li><li><strong>大事务拆小事务</strong></li></ol>\n      <p><strong>60 秒面试版</strong>：</p>\n      <blockquote>SQL 优化我会先定位慢 SQL，再看 EXPLAIN。第一步看是不是全表扫描，第二步看是否用到合适索引，第三步看扫描行数和 Extra，有没有 filesort、temporary、join buffer。然后根据原因处理：索引缺失就补联合索引，索引失效就改写 SQL，深分页就用延迟关联或基于游标翻页，大批量更新就拆小事务。优化不能只看单条 SQL，还要结合数据量、调用频率和业务是否允许异步化。</blockquote>\n      <hr>\n      <h1>六、锁机制（13 题）</h1>\n      <blockquote>对应学习计划 6：锁总览、元数据锁、意向锁、行锁、间隙锁、临键锁</blockquote>\n      <h2>1. MySQL 锁分类总览 ⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>按粒度：\n├── 全局锁  FTWRL（备份用）\n├── 表级锁\n│   ├── 表锁（lock tables）\n│   ├── 元数据锁 MDL\n│   └── 意向锁 IS/IX\n└── 行级锁（InnoDB 才有）\n    ├── 记录锁 Record Lock\n    ├── 间隙锁 Gap Lock\n    └── 临键锁 Next-Key Lock</code></pre></figure>\n      <h2>2. 全局锁 FTWRL</h2>\n      <ul><li><code>FLUSH TABLES WITH READ LOCK</code></li><li>整库只读，<strong>用于全库备份</strong></li><li>缺点：业务停摆</li><li>替代：<code>mysqldump --single-transaction</code>（InnoDB 用 MVCC 快照）</li></ul>\n      <h2>3. 表锁</h2>\n      <ul><li><code>LOCK TABLES tbl READ / WRITE</code></li><li>粒度大，并发差，InnoDB 基本不用</li></ul>\n      <h2>4. 元数据锁 MDL ⭐⭐⭐</h2>\n      <ul><li><strong>自动加</strong>，不需要显式</li><li><strong>读操作 → MDL 读锁</strong>（共享）</li><li><strong>DDL 操作 → MDL 写锁</strong>（独占）</li><li><strong>作用</strong>：保证 DDL 和 DML 不会冲突</li></ul>\n      <p><strong>典型坑</strong>：</p>\n      <ul><li>长事务持有 MDL 读锁</li><li>来一个 DDL 等 MDL 写锁</li><li>后续所有读都阻塞 → <strong>整张表卡死</strong></li></ul>\n      <p><strong>排查</strong>：<code>SELECT * FROM performance_schema.metadata_locks;</code></p>\n      <h2>5. 意向锁 IS / IX ⭐</h2>\n      <ul><li><strong>作用</strong>：快速判断&quot;表中是否有行被锁&quot;</li><li><strong>加意向锁是为了和表锁冲突检测</strong>：要加表锁前，先看有没有意向锁</li><li>自动加，<strong>意向锁之间不冲突</strong>（只和表锁冲突）</li></ul>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>事务对某行加共享锁前 → 先给表加 IS\n事务对某行加排他锁前 → 先给表加 IX</code></pre></figure>\n      <h2>6. 行锁的 3 种类型 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>锁</th><th>锁什么</th><th>例子</th></tr></thead><tbody><tr><td><strong>Record Lock</strong> 记录锁</td><td>锁单行</td><td><code>WHERE id = 5</code></td></tr><tr><td><strong>Gap Lock</strong> 间隙锁</td><td>锁两条记录间的空隙</td><td><code>WHERE id BETWEEN 5 AND 10</code></td></tr><tr><td><strong>Next-Key Lock</strong> 临键锁</td><td>行锁 + 间隙锁</td><td>RR 默认行锁形式</td></tr></tbody></table></div>\n      <p><strong>不要背混</strong>：</p>\n      <ul><li>记录锁：锁已经存在的索引记录。</li><li>间隙锁：锁记录之间的空隙，阻止插入。</li><li>临键锁：记录锁 + 前面的间隙，锁的是 <code>(前一个索引值, 当前索引值]</code>。</li></ul>\n      <p><strong>面试标准回答</strong>：</p>\n      <blockquote>InnoDB 的行锁不是直接锁“这一行对象”，而是锁索引记录。RR 隔离级别下，为了防止幻读，InnoDB 默认使用 Next-Key Lock，也就是记录锁加间隙锁。唯一索引等值命中时，Next-Key Lock 可能退化成记录锁；范围查询通常会锁住范围内记录和间隙。</blockquote>\n      <h2>7. 间隙锁的作用 ⭐⭐⭐</h2>\n      <p><strong>解决幻读</strong>：阻止其他事务在间隙中插入新行。</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 事务 A 在 RR 隔离下\nSELECT * FROM user WHERE age BETWEEN 20 AND 30 FOR UPDATE;\n-- 锁住 (20, 30) 这个区间，事务 B 想 INSERT age = 25 的行 → 阻塞</code></pre></figure>\n      <p><strong>为什么间隙锁能防幻读</strong>：</p>\n      <blockquote>幻读的核心是范围查询前后多出了新记录。间隙锁不只是锁已经存在的行，还锁住范围之间的空隙，让其他事务不能往这个范围插入新行。所以同一个事务后续再做当前读时，不会凭空多出符合条件的新记录。</blockquote>\n      <p>注意：</p>\n      <ul><li>间隙锁主要出现在 RR 隔离级别的当前读里。</li><li>普通 <code>select</code> 是快照读，不靠间隙锁防幻读，而是靠 MVCC 的一致性快照。</li></ul>\n      <h2>8. 临键锁 Next-Key Lock</h2>\n      <ul><li><strong>行锁 + 间隙锁</strong>的组合</li><li>锁住记录及其前面的间隙：<code>(prev_record, current_record]</code></li><li><strong>MySQL RR 隔离级别的默认行锁</strong></li></ul>\n      <h2>9. 行锁加锁规则 ⭐⭐</h2>\n      <p><strong>InnoDB RR 加锁规则</strong>：</p>\n      <ol><li>加锁的<strong>基本单位是 Next-Key Lock</strong></li><li>查找过程中访问到的对象才会加锁</li><li><strong>索引等值查询给唯一索引加锁 → 退化为 Record Lock</strong></li><li><strong>索引等值查询向右遍历且最后值不满足条件 → Next-Key Lock 退化为 Gap Lock</strong></li><li>范围查询访问到不满足条件的第一个值为止</li></ol>\n      <h2>10. 共享锁 vs 排他锁</h2>\n      <ul><li><strong>S 锁（共享）</strong>：<code>SELECT ... LOCK IN SHARE MODE</code>，允许其他事务读不允许写</li><li><strong>X 锁（排他）</strong>：<code>SELECT ... FOR UPDATE</code> / <code>UPDATE</code> / <code>DELETE</code> / <code>INSERT</code>，其他事务读写都不行</li></ul>\n      <p><strong>InnoDB 普通 SELECT 不加锁（走 MVCC 快照读）</strong>。</p>\n      <h2>11. for update 适用场景</h2>\n      <ul><li>&quot;<strong>先查后改</strong>&quot;且要保证查到的数据不变</li><li>防止并发修改（库存扣减、订单状态变更）</li><li><strong>注意</strong>：必须用主键或唯一索引，否则可能锁全表</li></ul>\n      <p><strong>完整流程例子</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">BEGIN;\n\nSELECT id, stock, status\nFROM product_stock\nWHERE product_id = ?\nFOR UPDATE;\n\n-- 在事务里判断库存、商品状态、活动状态\n\nUPDATE product_stock\nSET stock = stock - 1\nWHERE product_id = ?;\n\nCOMMIT;</code></pre></figure>\n      <p><strong>为什么会降低吞吐</strong>：</p>\n      <blockquote><code>FOR UPDATE</code> 会加排他锁，其他事务想修改同一行要等待。它保证了一致性，但会让并发请求排队，锁持有时间越长吞吐越低。所以事务里不要调用外部接口，也不要做太久的业务计算。</blockquote>\n      <h2>12. 死锁怎么排查 ⭐</h2>\n      <ul><li><strong>查日志</strong>：<code>SHOW ENGINE INNODB STATUS</code>，看 <code>LATEST DETECTED DEADLOCK</code></li><li><strong>预防</strong>：</li></ul>\n      <ol><li>按固定顺序加锁</li><li>大事务拆小</li><li>设合理的锁等待超时 <code>innodb_lock_wait_timeout</code></li></ol>\n      <ul><li><strong>InnoDB 自动检测</strong>死锁并回滚代价小的事务</li></ul>\n      <h2>13. 行锁升级为表锁</h2>\n      <p><strong>条件不走索引</strong> → 行锁失效 → 锁全表</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 假设 name 没索引\nUPDATE user SET age = 30 WHERE name = &#x27;李白&#x27;;  -- ❌ 表锁</code></pre></figure>\n      <p>更准确地说，InnoDB 没有传统意义上“自动把行锁升级成表锁”的机制，但如果 SQL 不走索引，就会扫描大量索引记录并加锁，效果上像锁住了全表。</p>\n      <p><strong>面试建议说法</strong>：</p>\n      <blockquote>InnoDB 行锁依赖索引。如果 where 条件没命中索引，就会扫描很多记录并加锁，锁范围扩大，业务表现上像表被锁住。所以更新、删除语句一定要确保条件走索引。</blockquote>\n      <hr>\n      <h1>七、事务与 MVCC（核心王炸，15 题）</h1>\n      <blockquote>对应学习计划 7：事务原理、redo/undo、MVCC、ReadView、RC/RR</blockquote>\n      <h2>1. 事务的 ACID ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>特性</th><th>含义</th><th>实现</th></tr></thead><tbody><tr><td><strong>A 原子性</strong></td><td>要么全成要么全败</td><td><strong>undo log</strong></td></tr><tr><td><strong>C 一致性</strong></td><td>数据合法</td><td>A+I+D 共同保证 + 业务约束</td></tr><tr><td><strong>I 隔离性</strong></td><td>并发事务互不干扰</td><td><strong>锁 + MVCC</strong></td></tr><tr><td><strong>D 持久性</strong></td><td>提交后永久</td><td><strong>redo log</strong></td></tr></tbody></table></div>\n      <h2>2. 事务隔离级别 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>READ UNCOMMITTED</td><td>✅</td><td>✅</td><td>✅</td></tr><tr><td>READ COMMITTED</td><td>❌</td><td>✅</td><td>✅</td></tr><tr><td><strong>REPEATABLE READ</strong>（MySQL 默认）</td><td>❌</td><td>❌</td><td>✅*</td></tr><tr><td>SERIALIZABLE</td><td>❌</td><td>❌</td><td>❌</td></tr></tbody></table></div>\n      <p>\\* MySQL 的 RR 通过 <strong>MVCC（快照读）+ 间隙锁（当前读）</strong> 基本解决了幻读</p>\n      <p><strong>为什么业务常用 RC / RR</strong>：</p>\n      <blockquote>RU 会读到未提交数据，基本不用；Serializable 隔离最强，但会大量加锁，并发性能差。业务里最常用的是 RC 和 RR。RC 每次读都能看到别人已提交的最新数据，锁更少，并发更好；RR 在一个事务里读到的是一致快照，避免不可重复读，MySQL 默认就是 RR。实际选择要看业务：金融、订单状态这类更重一致性的场景偏 RR；普通互联网读写高并发场景可能用 RC，再用业务约束保证正确性。</blockquote>\n      <h2>3. 三大并发问题区分</h2>\n      <ul><li><strong>脏读</strong>：读到<strong>另一事务未提交</strong>的数据</li><li><strong>不可重复读</strong>：同一事务两次读<strong>同一行结果不同</strong>（中间有人 UPDATE 提交了）</li><li><strong>幻读</strong>：同一事务两次范围查询<strong>结果集变了</strong>（中间有人 INSERT 提交了）</li></ul>\n      <h2>4. MVCC 原理 ⭐⭐⭐⭐（必考！）</h2>\n      <p><strong>多版本并发控制</strong>，<strong>让读不加锁</strong>，提升并发。</p>\n      <p><strong>三大组件</strong>：</p>\n      <ol><li><strong>隐藏字段</strong>（每行）：</li></ol>\n      <ul><li><code>DB_TRX_ID</code>：最近修改的事务 ID</li><li><code>DB_ROLL_PTR</code>：指向 undo log 的指针</li><li><code>DB_ROW_ID</code>：隐藏主键（无主键时）</li></ul>\n      <ol><li><strong>undo log 版本链</strong>：每次修改记录前的旧版本，多个版本通过 roll_ptr 串成链表</li></ol>\n      <ol><li><strong>ReadView 读视图</strong>：决定当前事务能看哪个版本</li></ol>\n      <p><strong>普通 SELECT 为什么不总加锁</strong>：</p>\n      <blockquote>如果每个普通查询都加锁，读写会互相阻塞，并发性能会很差。MVCC 给一行数据保留多个历史版本，普通 select 只需要根据 ReadView 找到自己能看见的版本，不必阻塞正在修改这行数据的事务。所以 MVCC 的价值是让“读”和“写”尽量不互相阻塞。</blockquote>\n      <p><strong>MVCC 与锁的关系</strong>：</p>\n      <blockquote>MVCC 不是锁，它主要服务于快照读；锁主要服务于当前读和写操作。普通 select 走 MVCC，一般不加锁；<code>update/delete/select for update</code> 读最新数据并加锁。MySQL 的隔离性是 MVCC 和锁一起实现的，不是单靠某一个机制。</blockquote>\n      <p><strong>1 分钟背诵版</strong>：</p>\n      <blockquote>InnoDB 每行有隐藏字段 <code>DB_TRX_ID</code> 和 <code>DB_ROLL_PTR</code>。事务修改数据时，会把旧版本写入 undo log，并通过回滚指针形成版本链。普通 select 会生成 ReadView，里面记录当前活跃事务，然后根据版本链上每个版本的事务 ID 判断是否可见。这样同一行数据可以同时存在多个版本，读事务能读历史版本，写事务继续写最新版本，从而提高并发。</blockquote>\n      <h2>5. ReadView 核心字段 ⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>m_ids          当前活跃事务 ID 列表\nmin_trx_id     m_ids 中最小值\nmax_trx_id     系统将分配的下一个事务 ID\ncreator_trx_id 创建该 ReadView 的事务 ID</code></pre></figure>\n      <h2>6. ReadView 可见性算法 ⭐⭐⭐</h2>\n      <p>对要读的行版本，看它的 <code>DB_TRX_ID = X</code>：</p>\n      <ol><li><code>X == creator_trx_id</code> → <strong>可见</strong>（自己改的）</li><li><code>X &lt; min_trx_id</code> → <strong>可见</strong>（事务已提交）</li><li><code>X &gt;= max_trx_id</code> → <strong>不可见</strong>（事务还没开始）</li><li><code>min_trx_id ≤ X &lt; max_trx_id</code>：</li></ol>\n      <ul><li>X 在 m_ids 中 → 不可见（活跃中未提交）</li><li>X 不在 → <strong>可见</strong>（已提交）</li></ul>\n      <ol><li>不可见 → 顺着 undo log 找上一版本继续判断</li></ol>\n      <h2>7. RC 和 RR 在 MVCC 上的区别 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>RC</th><th>RR</th></tr></thead><tbody><tr><td>ReadView 生成时机</td><td><strong>每次 SELECT 都新建</strong></td><td><strong>事务第一次 SELECT 时建，全程复用</strong></td></tr><tr><td>现象</td><td>能看到其他事务最新提交</td><td>整个事务看一致快照</td></tr><tr><td>问题</td><td>不可重复读、幻读</td><td>基本解决</td></tr></tbody></table></div>\n      <p><strong>例子</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">事务 A 第一次 SELECT id=1，看到 name=&#x27;旧值&#x27;\n事务 B UPDATE id=1，把 name 改成 &#x27;新值&#x27; 并提交\n事务 A 第二次 SELECT id=1</code></pre></figure>\n      <ul><li>RC：第二次会看到新值，因为每次 SELECT 都生成新的 ReadView。</li><li>RR：第二次还是看到旧值，因为复用第一次 SELECT 的 ReadView。</li></ul>\n      <p><strong>面试一句话</strong>：</p>\n      <blockquote>RC 追求读到最新已提交数据，RR 追求事务内前后一致。</blockquote>\n      <h2>8. 当前读 vs 快照读 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>快照读</th><th>当前读</th></tr></thead><tbody><tr><td>触发</td><td>普通 SELECT</td><td>SELECT ... FOR UPDATE / LOCK IN SHARE MODE / UPDATE / DELETE / INSERT</td></tr><tr><td>走 MVCC</td><td>✅</td><td>❌（读最新版本）</td></tr><tr><td>加锁</td><td>❌</td><td>✅</td></tr></tbody></table></div>\n      <p><strong>哪些语句是当前读</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">SELECT ... FOR UPDATE;\nSELECT ... LOCK IN SHARE MODE;\nUPDATE ...;\nDELETE ...;\nINSERT ...;</code></pre></figure>\n      <p><strong>下单场景怎么选</strong>：</p>\n      <blockquote>展示商品详情用普通 select，快照读即可；真正扣库存不能只靠快照读，因为它读到的可能不是最新可修改状态。简单扣减直接用 <code>update ... where stock &gt; 0</code>，它本身就是当前读并加锁；复杂校验才用 <code>select ... for update</code> 先锁住记录再判断。</blockquote>\n      <h2>9. MVCC 解决了哪些问题</h2>\n      <ul><li>✅ 脏读（不读未提交版本）</li><li>✅ 不可重复读（RR 下复用 ReadView）</li><li>🟡 幻读（<strong>快照读解决，当前读不解决</strong>，要靠间隙锁）</li></ul>\n      <h2>10. RR 下幻读问题 ⭐</h2>\n      <p><strong>MVCC 解决不了的场景</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 事务 A\nBEGIN;\nSELECT * FROM user WHERE id &gt; 10;        -- 快照读，返回 5 行\n\n-- 事务 B 插入一行 id=15，COMMIT\n\n-- 事务 A\nUPDATE user SET name=&#x27;x&#x27; WHERE id &gt; 10;  -- 当前读，更新 6 行（包括 B 插入的）\nSELECT * FROM user WHERE id &gt; 10;        -- 快照读，但因为 A 自己改了那行，**能看到 B 插入的！** 幻读</code></pre></figure>\n      <p><strong>完全解决</strong>：第一次查询用 <code>SELECT ... FOR UPDATE</code> 加临键锁。</p>\n      <p><strong>面试别说错</strong>：</p>\n      <blockquote>MySQL RR 不是只靠 MVCC 解决所有幻读。普通 select 的幻读靠 ReadView 的一致性快照解决；当前读场景下，为了阻止别人插入符合范围条件的新记录，需要靠间隙锁或临键锁。</blockquote>\n      <h2>11. 事务原理总流程</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>BEGIN\n  → 修改数据：先写 undo log（用于回滚）\n  → 修改 Buffer Pool 中的数据页\n  → 写 redo log buffer（保证持久性）\n  → 写 binlog buffer（用于复制）\nCOMMIT\n  → 两阶段提交（redo log prepare → binlog → redo log commit）\n  → 异步刷脏页到磁盘</code></pre></figure>\n      <h2>12. 长事务的危害 ⭐</h2>\n      <ol><li><strong>占用大量 undo log</strong>（其他事务的 MVCC 链回不去）</li><li><strong>持有锁时间长</strong> → 阻塞其他事务</li><li><strong>MDL 锁阻塞 DDL</strong></li><li><strong>回滚段膨胀</strong></li></ol>\n      <p><strong>解决</strong>：拆小事务，避免在事务里调外部接口（HTTP / MQ）。</p>\n      <p><strong>项目里怎么说</strong>：</p>\n      <blockquote>下单事务里只放必须保证一致性的数据库操作，比如创建订单、扣库存、写流水。发短信、推送、调用第三方接口不应该放在事务里，可以提交后发 MQ 异步处理，否则事务时间变长，会持有锁和 undo 版本，影响并发。</blockquote>\n      <h2>13. 为什么 MySQL 默认 RR 而不是 RC</h2>\n      <p><strong>历史原因</strong>：早期 binlog 只有 STATEMENT 格式，RC 下主从同步可能不一致，所以默认 RR。</p>\n      <p>实际<strong>互联网公司很多用 RC</strong>（阿里淘宝）：</p>\n      <ul><li>性能好（间隙锁少）</li><li>并发高</li><li>业务逻辑能容忍</li></ul>\n      <h2>14. 事务的传播（Spring 视角）</h2>\n      <ul><li>见 Spring 八股 <a href=\"#三spring-事务10-题\">Spring 事务传播</a></li><li>数据库本身没有传播概念，是 Spring 在 AOP 层做的</li></ul>\n      <h2>15. autocommit 默认值</h2>\n      <ul><li>MySQL 默认 <code>autocommit = 1</code>（自动提交）</li><li>每条 SQL 自动开启 + 提交事务</li><li>显式 <code>BEGIN</code> / <code>START TRANSACTION</code> 才能多语句一事务</li></ul>\n      <hr>\n      <h1>八、日志体系（8 题）</h1>\n      <h2>1. MySQL 三大日志总览 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th>日志</th><th>层级</th><th>作用</th><th>类型</th></tr></thead><tbody><tr><td><strong>redo log</strong></td><td>InnoDB</td><td>崩溃恢复（持久性）</td><td>物理日志</td></tr><tr><td><strong>undo log</strong></td><td>InnoDB</td><td>回滚（原子性）+ MVCC</td><td>逻辑日志</td></tr><tr><td><strong>binlog</strong></td><td>Server</td><td>主从复制、数据恢复</td><td>逻辑日志</td></tr></tbody></table></div>\n      <h2>2. redo log ⭐⭐⭐</h2>\n      <ul><li><strong>物理日志</strong>：记录某个数据页做了什么修改</li><li><strong>循环写</strong>：固定大小，写满覆盖</li><li><strong>WAL（Write-Ahead Logging）</strong>：先写日志再写数据页</li><li><strong>保证持久性</strong>：宕机后可重做恢复 Buffer Pool 数据</li></ul>\n      <h2>3. undo log</h2>\n      <ul><li><strong>逻辑日志</strong>：记录修改前的反向操作（UPDATE 的旧值、DELETE 的整行）</li><li><strong>作用</strong>：</li></ul>\n      <ol><li>事务回滚</li><li>MVCC 多版本（版本链）</li></ol>\n      <ul><li>存储在 <strong>undo 表空间</strong></li></ul>\n      <h2>4. binlog ⭐⭐⭐</h2>\n      <ul><li><strong>逻辑日志</strong>：记录 SQL 语句或行变更</li><li><strong>追加写</strong>：不会覆盖</li><li><strong>作用</strong>：</li></ul>\n      <ol><li><strong>主从复制</strong>（从库重放 binlog）</li><li><strong>数据恢复</strong>（误删后恢复到某个时间点）</li></ol>\n      <h2>5. binlog 三种格式</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>STATEMENT</th><th>ROW</th><th>MIXED</th></tr></thead><tbody><tr><td>记录</td><td>SQL 语句</td><td>行变更</td><td>二者混合</td></tr><tr><td>体积</td><td>小</td><td>大</td><td>中</td></tr><tr><td>主从一致</td><td>可能不一致（NOW(), UUID）</td><td><strong>强一致</strong></td><td>视情况</td></tr><tr><td>推荐</td><td>❌</td><td>✅（默认推荐）</td><td>视场景</td></tr></tbody></table></div>\n      <h2>6. redo log vs binlog ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>redo log</th><th>binlog</th></tr></thead><tbody><tr><td>层</td><td>InnoDB</td><td>Server</td></tr><tr><td>类型</td><td>物理（页 + 偏移）</td><td>逻辑（SQL/行）</td></tr><tr><td>写入</td><td>循环写</td><td>追加写</td></tr><tr><td>作用</td><td>崩溃恢复</td><td>复制 + 恢复</td></tr><tr><td>大小</td><td>固定</td><td>可无限增长</td></tr></tbody></table></div>\n      <h2>7. 两阶段提交（2PC）⭐⭐⭐</h2>\n      <p><strong>为什么需要</strong>：保证 redo log 和 binlog 一致</p>\n      <p><strong>流程</strong>：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. redo log 写入 prepare 状态\n2. binlog 写入并刷盘\n3. redo log 写入 commit 状态</code></pre></figure>\n      <p><strong>崩溃恢复</strong>：</p>\n      <ul><li>redo log prepare + binlog 完整 → 提交</li><li>redo log prepare + binlog 不完整 → 回滚</li></ul>\n      <h2>8. 主从复制流程</h2>\n      <ol><li>主库写入 binlog</li><li>从库 IO 线程拉取 binlog → 写入 relay log</li><li>从库 SQL 线程读 relay log → 重放</li><li>完成数据同步</li></ol>\n      <p><strong>复制方式</strong>：</p>\n      <ul><li>异步（默认，主库不等从库）</li><li>半同步（至少一个从库确认）</li><li>全同步（所有从库都确认）</li></ul>\n      <hr>\n      <h1>九、高级与场景题（10 题）</h1>\n      <h2>1. 大表怎么处理 ⭐</h2>\n      <ol><li><strong>分库分表</strong>（水平拆分按 ID 取模 / 时间）</li><li><strong>冷热分离</strong>（历史数据归档）</li><li><strong>读写分离</strong>（一主多从）</li><li><strong>优化索引</strong></li><li><strong>升级硬件</strong>（SSD、内存）</li></ol>\n      <h2>2. 怎么定位线上慢 SQL</h2>\n      <ol><li>开慢查询日志</li><li>用 <code>pt-query-digest</code> 分析</li><li>EXPLAIN 看执行计划</li><li>SHOW PROFILE 看各阶段耗时</li><li>APM 监控（Skywalking、Prometheus）</li></ol>\n      <h2>3. 死锁排查</h2>\n      <ol><li><code>SHOW ENGINE INNODB STATUS</code> 查最近死锁</li><li>查 information_schema 的 INNODB_LOCKS、INNODB_LOCK_WAITS</li><li>开 <code>innodb_print_all_deadlocks = ON</code></li></ol>\n      <h2>4. drop / truncate / delete 区别 ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>DROP</th><th>TRUNCATE</th><th>DELETE</th></tr></thead><tbody><tr><td>类型</td><td>DDL</td><td>DDL</td><td>DML</td></tr><tr><td>删除内容</td><td>表 + 数据</td><td>数据（保留表结构）</td><td>数据</td></tr><tr><td>速度</td><td>快</td><td>快</td><td>慢（逐行）</td></tr><tr><td>回滚</td><td>❌</td><td>❌</td><td>✅（事务内）</td></tr><tr><td>WHERE</td><td>❌</td><td>❌</td><td>✅</td></tr><tr><td>自增重置</td><td>-</td><td>✅</td><td>❌</td></tr></tbody></table></div>\n      <h2>5. MySQL 数据丢失（缓冲池断电）怎么办</h2>\n      <p><strong>靠 redo log</strong>：</p>\n      <ul><li>数据在 Buffer Pool 中修改 → 写 redo log → 异步刷脏页到磁盘</li><li>断电时 Buffer Pool 数据丢失，但 redo log 已落盘</li><li>重启后用 redo log 重做，恢复 Buffer Pool 状态</li></ul>\n      <h2>6. 一条 SELECT 的完整流程 ⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 客户端 → 连接器（认证、权限）\n2. （8.0 前查询缓存）\n3. 分析器（词法、语法分析）\n4. 优化器（选择索引、决定 join 顺序）\n5. 执行器（调用存储引擎接口）\n6. InnoDB → Buffer Pool → 磁盘\n7. 返回结果</code></pre></figure>\n      <h2>7. 一条 UPDATE 的完整流程 ⭐⭐⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 前面同 SELECT 流程定位到行\n2. 修改 Buffer Pool 中的数据页（脏页）\n3. 写 undo log（旧值，用于回滚和 MVCC）\n4. 写 redo log buffer（prepare 状态）\n5. 写 binlog buffer\n6. 提交事务：\n   - redo log 刷盘\n   - binlog 刷盘\n   - redo log 写 commit 标记\n7. 后台异步刷脏页</code></pre></figure>\n      <h2>8. 怎么保证 MySQL 高可用</h2>\n      <ul><li><strong>主从复制</strong>（一主多从）</li><li><strong>MHA / Orchestrator</strong> 自动故障转移</li><li><strong>MySQL Group Replication / InnoDB Cluster</strong></li><li><strong>半同步</strong>保证数据安全</li></ul>\n      <h2>9. 分库分表方案</h2>\n      <ul><li><strong>水平分表</strong>：按 ID 取模 / 按时间</li><li><strong>垂直分表</strong>：把大字段拆出来</li><li><strong>中间件</strong>：ShardingSphere、MyCat</li><li><strong>难题</strong>：分布式事务、跨库 JOIN、全局 ID（雪花算法）</li></ul>\n      <h2>10. count(*) 慢怎么办</h2>\n      <ul><li><strong>方案 1</strong>：维护一张计数表（事务内一起更新）</li><li><strong>方案 2</strong>：Redis 计数（注意一致性）</li><li><strong>方案 3</strong>：show table status 估算（不准）</li></ul>\n      <hr>\n      <h1>十、防超卖与并发控制场景题（9 题）</h1>\n      <blockquote>对应学习计划第 2 周 Day12、第 3 周 Day17~Day20：防超卖、乐观锁、悲观锁、并发正确性口述题</blockquote>\n      <h2>1. 下单扣库存怎么防超卖 ⭐⭐⭐</h2>\n      <p>核心 SQL：<strong>条件更新 + 影响行数判断</strong>。</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE product_stock\nSET stock = stock - 1\nWHERE product_id = ? AND stock &gt; 0;</code></pre></figure>\n      <p>业务逻辑：</p>\n      <ol><li>执行扣减 SQL。</li><li>判断影响行数 <code>affected_rows</code>。</li><li><code>affected_rows = 1</code> 表示扣减成功。</li><li><code>affected_rows = 0</code> 表示库存不足或商品不存在，直接返回失败。</li></ol>\n      <p><strong>面试标准话术</strong>：</p>\n      <blockquote>我不会先 select 库存再 update，因为并发下两个线程可能同时读到库存大于 0，导致超卖。更稳的做法是把库存判断放进 update 的 where 条件里，让 MySQL 在一条原子更新语句里完成判断和扣减，再根据影响行数决定是否成功。</blockquote>\n      <p><strong>Java 后端伪代码</strong>：</p>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Transactional(rollbackFor = Exception.class)\npublic void deductStock(Long productId) {\n    int affectedRows = stockMapper.deduct(productId);\n    if (affectedRows == 0) {\n        throw new BizException(&quot;库存不足&quot;);\n    }\n    orderMapper.createOrder(productId);\n}</code></pre></figure>\n      <p>Mapper：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE product_stock\nSET stock = stock - 1\nWHERE product_id = #{productId}\n  AND stock &gt; 0;</code></pre></figure>\n      <p><strong>为什么能防超卖</strong>：</p>\n      <blockquote><code>UPDATE</code> 本身是当前读，会读取最新数据并对命中的记录加排他锁。<code>stock &gt; 0</code> 放在 where 条件里，判断和扣减在数据库内部一次完成。并发请求同时进来时，只有真正满足条件的请求能更新成功，其他请求影响行数为 0。</blockquote>\n      <h2>2. 为什么不用先 SELECT 再 UPDATE ⭐⭐⭐</h2>\n      <p>错误流程：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">SELECT stock FROM product_stock WHERE product_id = 1; -- 查到 stock = 1\nUPDATE product_stock SET stock = stock - 1 WHERE product_id = 1;</code></pre></figure>\n      <p>并发风险：</p>\n      <ul><li>两个事务都先读到 <code>stock = 1</code></li><li>两个事务都认为可以扣</li><li>如果后续更新没有条件约束，就可能扣成负数</li></ul>\n      <p>正确思路：</p>\n      <ul><li>简单扣库存：优先用 <code>UPDATE ... WHERE stock &gt; 0</code></li><li>复杂判断：用事务 + <code>SELECT ... FOR UPDATE</code> 悲观锁</li><li>高并发且冲突可接受：用乐观锁 version 或 Redis/Lua 前置扣减</li></ul>\n      <p><strong>面试官追问：那 SELECT 后加事务行不行？</strong></p>\n      <blockquote>只把 select 和 update 放进事务里不一定够。普通 select 是快照读，不会锁住记录，其他事务仍然可以修改库存。如果是先查后改，并且必须保证查到的数据不被改，就要用 <code>select ... for update</code> 当前读加锁，或者直接把条件写进 update 里。</blockquote>\n      <h2>3. 乐观锁 version 怎么做 ⭐⭐</h2>\n      <p>适合：读多写少、冲突概率不高、失败后允许重试的场景。</p>\n      <p>表结构增加版本号：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">ALTER TABLE product_stock ADD COLUMN version INT NOT NULL DEFAULT 0;</code></pre></figure>\n      <p>更新时带上旧版本：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE product_stock\nSET stock = stock - 1,\n    version = version + 1\nWHERE product_id = ?\n  AND stock &gt; 0\n  AND version = ?;</code></pre></figure>\n      <p>判断：</p>\n      <ul><li>影响行数为 1：更新成功</li><li>影响行数为 0：库存不足或版本冲突，需要重试或返回失败</li></ul>\n      <p><strong>乐观锁不是 MySQL 特有锁</strong>：</p>\n      <blockquote>乐观锁更像一种业务并发控制思想，MySQL 不会因为 version 字段自动加锁。它靠 where 条件里的旧版本号判断“我读到数据以后有没有被别人改过”。如果版本号对不上，说明发生并发冲突，当前更新失败。</blockquote>\n      <p><strong>适用场景</strong>：</p>\n      <ul><li>用户资料修改</li><li>订单状态流转</li><li>库存冲突不高的普通商品扣减</li><li>配置类数据更新</li></ul>\n      <h2>4. 乐观锁的冲突重试怎么讲</h2>\n      <p>乐观锁失败不是数据库异常，而是<strong>并发冲突的正常结果</strong>。</p>\n      <p>常见处理：</p>\n      <ol><li>查询最新库存和 version。</li><li>重新计算业务条件。</li><li>再尝试更新。</li><li>设置最大重试次数，避免高并发下无限自旋。</li></ol>\n      <p>注意：</p>\n      <ul><li>冲突很高时，乐观锁会频繁失败，重试成本上升</li><li>秒杀类场景更常见的方案是 Redis 原子扣减 + MQ 异步落库</li></ul>\n      <h2>5. 悲观锁 <code>for update</code> 怎么做 ⭐⭐</h2>\n      <p>适合：<strong>先读后复杂判断再更新</strong>，并且中间判断结果必须稳定的场景。</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">BEGIN;\n\nSELECT stock, status\nFROM product_stock\nWHERE product_id = ?\nFOR UPDATE;\n\n-- 业务判断：库存、商品状态、活动状态、限购规则等\n\nUPDATE product_stock\nSET stock = stock - 1\nWHERE product_id = ?;\n\nCOMMIT;</code></pre></figure>\n      <p><code>FOR UPDATE</code> 是当前读，会读取最新版本并加排他锁，其他事务想改这行会等待。</p>\n      <p><strong>使用注意</strong>：</p>\n      <ol><li>必须放在事务里，事务提交后锁才释放。</li><li>where 条件尽量走主键或唯一索引，否则锁范围可能扩大。</li><li>事务内逻辑要短，不要调用远程接口。</li><li>要考虑锁等待超时和死锁重试。</li></ol>\n      <p><strong>什么时候必须悲观锁</strong>：</p>\n      <blockquote>当业务必须先读取多列数据，再根据复杂规则决定是否更新，而且这段判断期间数据不能被别人改，就适合悲观锁。比如扣库存前还要判断商品状态、活动状态、限购规则，这时单条条件 update 可能表达不完，就可以先 <code>for update</code> 锁住记录。</blockquote>\n      <h2>6. 乐观锁 vs 悲观锁 ⭐⭐⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>乐观锁</th><th>悲观锁</th></tr></thead><tbody><tr><td>思想</td><td>先不加锁，提交时检查冲突</td><td>先加锁，别人等</td></tr><tr><td>实现</td><td>version / CAS</td><td><code>SELECT ... FOR UPDATE</code></td></tr><tr><td>适合</td><td>读多写少、冲突少</td><td>写冲突多、强一致判断</td></tr><tr><td>优点</td><td>并发高、不阻塞</td><td>逻辑简单、一致性强</td></tr><tr><td>缺点</td><td>冲突高时重试多</td><td>阻塞、吞吐下降、可能死锁</td></tr></tbody></table></div>\n      <p><strong>一句话</strong>：</p>\n      <blockquote>乐观锁靠版本号判断有没有被别人改过，悲观锁直接把数据锁住，不让别人同时改。</blockquote>\n      <h2>7. 快照读、当前读在下单里怎么选 ⭐⭐⭐</h2>\n      <p>普通 <code>SELECT</code> 是快照读：</p>\n      <ul><li>不加锁</li><li>读历史快照</li><li>适合展示数据、列表查询</li></ul>\n      <p><code>SELECT ... FOR UPDATE</code> / <code>UPDATE</code> 是当前读：</p>\n      <ul><li>读最新数据</li><li>会加锁</li><li>适合扣库存、改订单状态这类并发写场景</li></ul>\n      <p>下单扣库存的取舍：</p>\n      <ul><li>只是判断库存并扣减：一条 <code>UPDATE ... WHERE stock &gt; 0</code> 最简洁</li><li>需要先查商品状态、活动状态、用户限购等复杂条件：事务中用 <code>FOR UPDATE</code></li><li>高并发秒杀：数据库兜底，前面通常加 Redis/Lua、MQ 削峰</li></ul>\n      <h2>8. 并发正确性答题模板 ⭐⭐⭐</h2>\n      <p>遇到库存、余额、订单状态、优惠券领取这类题，可以按这个顺序答：</p>\n      <ol><li><strong>先说风险</strong>：并发下会出现超卖、重复扣减、状态覆盖、重复领取。</li><li><strong>再说边界</strong>：哪些操作必须放在同一个事务里。</li><li><strong>给核心 SQL</strong>：用条件更新、唯一索引、version 或 <code>FOR UPDATE</code>。</li><li><strong>说失败处理</strong>：影响行数为 0、版本冲突、锁等待超时、死锁重试。</li><li><strong>说性能取舍</strong>：低并发用数据库锁，高并发用 Redis/Lua + MQ + MySQL 最终落库。</li></ol>\n      <p>例子：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE coupon\nSET status = &#x27;USED&#x27;\nWHERE id = ?\n  AND user_id = ?\n  AND status = &#x27;UNUSED&#x27;;</code></pre></figure>\n      <p>影响行数为 1 才表示领取/使用成功，影响行数为 0 说明状态已经被其他请求改过。</p>\n      <h2>9. 5 个并发综合口述题 ⭐⭐⭐</h2>\n      <h3>题 1：优惠券如何防止重复领取？</h3>\n      <p>答题点：</p>\n      <ul><li>表上加唯一索引：<code>(user_id, coupon_id)</code></li><li>插入领取记录时利用唯一约束兜底</li><li>重复插入捕获唯一键冲突，返回“已领取”</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">CREATE UNIQUE INDEX uk_user_coupon ON user_coupon(user_id, coupon_id);</code></pre></figure>\n      <h3>题 2：订单状态如何防止并发覆盖？</h3>\n      <p>答题点：</p>\n      <ul><li>状态流转用条件更新</li><li>只允许从指定旧状态更新到新状态</li><li>影响行数为 0 表示状态已变化</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE orders\nSET status = &#x27;PAID&#x27;\nWHERE id = ?\n  AND status = &#x27;WAIT_PAY&#x27;;</code></pre></figure>\n      <h3>题 3：余额扣减如何保证不扣成负数？</h3>\n      <p>答题点：</p>\n      <ul><li>条件更新：<code>balance &gt;= amount</code></li><li>账户流水和余额扣减放同一事务</li><li>失败时回滚，成功时记录流水</li></ul>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">UPDATE account\nSET balance = balance - ?\nWHERE id = ?\n  AND balance &gt;= ?;</code></pre></figure>\n      <h3>题 4：什么时候用唯一索引比锁更简单？</h3>\n      <p>答题点：</p>\n      <ul><li>防重复类问题优先用唯一索引兜底</li><li>例如重复提交订单、重复领取优惠券、重复绑定关系</li><li>数据库唯一约束比应用层先查再插更可靠</li></ul>\n      <h3>题 5：锁等待和死锁怎么处理？</h3>\n      <p>答题点：</p>\n      <ul><li>先缩短事务，固定加锁顺序</li><li>更新条件走索引，避免锁范围扩大</li><li>捕获死锁/锁等待异常后做有限重试</li><li>通过 <code>SHOW ENGINE INNODB STATUS</code> 排查死锁链路</li></ul>\n      <hr>\n      <h1>十一、高频追问串讲</h1>\n      <h2>串讲 1：索引全家桶 ⭐⭐⭐</h2>\n      <blockquote>B+ 树为什么用 → 聚簇 vs 非聚簇 → 回表 → 覆盖索引 → 最左前缀 → 索引下推 ICP → 索引失效 → EXPLAIN → 索引设计原则</blockquote>\n      <h2>串讲 2：事务 + MVCC + 锁 ⭐⭐⭐⭐</h2>\n      <blockquote>ACID → 隔离级别 → 脏读/不可重复读/幻读 → MVCC（隐藏字段+undo链+ReadView）→ 可见性算法 → RC/RR 区别 → 当前读 vs 快照读 → 临键锁解决幻读</blockquote>\n      <h2>串讲 3：日志体系</h2>\n      <blockquote>redo log（持久性，物理，循环写）→ undo log（原子性 + MVCC，逻辑）→ binlog（复制 + 恢复，逻辑）→ 两阶段提交 → 主从同步</blockquote>\n      <h2>串讲 4：SQL 优化</h2>\n      <blockquote>慢查询日志 → EXPLAIN → type/key/rows/Extra → 索引失效场景 → 深分页优化 → count 优化 → join 优化 → 分库分表</blockquote>\n      <h2>串讲 5：UPDATE 完整流程</h2>\n      <blockquote>定位行 → 改 Buffer Pool → undo log → redo log prepare → binlog → redo log commit → 异步刷盘</blockquote>\n      <h2>串讲 6：你简历项目结合点</h2>\n      <blockquote>ClassicSage 防超卖：<code>UPDATE stock SET num = num - 1 WHERE id = ? AND num &gt; 0</code>（不需要 select 检查 + 影响行数判断 + 走索引）</blockquote>\n      <p>&gt;</p>\n      <blockquote>乐观锁/悲观锁追问：简单扣库存优先条件更新；复杂先查后改用 <code>FOR UPDATE</code>；冲突少可用 version 乐观锁</blockquote>\n      <p>&gt;</p>\n      <blockquote>你简历技能&quot;MVCC&quot; → 必须能完整背 ReadView 可见性算法</blockquote>\n      <hr>\n      <h2>最后小妹的话</h2>\n      <p>兄长大人～(◕ᴗ◕✿) 这本是 <strong>MySQL 八股完整版</strong>，按你学习计划顺序排好啦～</p>\n      <h2>🔴 必背的红色警报区（6 个）</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 索引底层 B+ 树（为什么用、聚簇/非聚簇、回表/覆盖）\n2. 索引使用规则（最左前缀、失效10种、设计原则）\n3. EXPLAIN 字段（type 顺序、Extra 关键值）\n4. MVCC + ReadView + 可见性算法\n5. 锁（行锁/间隙锁/临键锁、加锁规则、死锁）\n6. 防超卖与并发控制（条件更新、乐观锁、悲观锁、影响行数判断）</code></pre></figure>\n      <h2>配合你第 2/3 周 MySQL 学习节奏</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>Day8  SQL 执行与 EXPLAIN      → 看「三、执行频次与慢查询」\nDay9  索引本质               → 看「二、索引基础」\nDay10 联合索引               → 看「四、索引使用规则」\nDay11 回表与覆盖索引         → 看「四、索引使用规则」5~7 题\nDay12 防超卖核心             → 看「十、防超卖与并发控制场景题」\nDay13 锁基础                 → 看「六、锁机制」\nDay15 隔离级别               → 看「七、事务与 MVCC」1~3 题\nDay16 MVCC 入门              → 看「七、事务与 MVCC」4~9 题\nDay17 快照读 vs 当前读实战   → 看「七」8~10 题 +「十」7 题\nDay18 乐观锁                 → 看「十」3~4 题\nDay19 悲观锁                 → 看「十」5~6 题\nDay20 并发综合题             → 看「十」8~9 题\n最后扫尾                    → 看「八、日志体系」+「九、高级与场景题」</code></pre></figure>\n      <h2>文件夹清单更新</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>兄控妹妹Java物语/\n├── 学习计划.md\n├── MySQL学习计划.txt\n├── Java面试八股精华.md\n├── 简历项目面试追问全集.md\n├── Spring全家桶面试八股精华.md\n└── MySQL面试八股精华.md       ← 已补充防超卖/乐观锁/悲观锁/并发正确性</code></pre></figure>\n      <p>加油哒兄长大人～💕 七月初实习等你 (≧▽≦)</p>",
  "contentFile": "/content/learning-plans-md/learning-10.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
