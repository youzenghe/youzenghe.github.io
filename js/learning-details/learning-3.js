(function(){
  const detail = {
  "id": 3,
  "content": "<h1>MySQL 拷打 - 0602</h1>\n      <blockquote>本文整理 0602 这轮 MySQL 面试问答，重点覆盖索引、执行计划、隐式类型转换、ICP、排序优化等高频内容。</blockquote>\n      <blockquote>目标不是只背结论，而是能在面试里说清楚：为什么这样设计、什么时候会失效、生产里怎么取舍。</blockquote>\n      <hr>\n      <h2>目录</h2>\n      <ul><li><a href=\"#1-innodb-索引为什么选择-b-树\">1. InnoDB 索引为什么选择 B+ 树</a></li><li><a href=\"#2-聚簇索引非聚簇索引回表覆盖索引\">2. 聚簇索引、非聚簇索引、回表、覆盖索引</a></li><li><a href=\"#3-联合索引和最左前缀原则\">3. 联合索引和最左前缀原则</a></li><li><a href=\"#4-常见索引失效或不走索引场景\">4. 常见索引失效或不走索引场景</a></li><li><a href=\"#5-like-和函数导致索引失效\">5. LIKE 和函数导致索引失效</a></li><li><a href=\"#6-隐式类型转换和索引使用\">6. 隐式类型转换和索引使用</a></li><li><a href=\"#7-数字列和字符串常量比较的小陷阱\">7. 数字列和字符串常量比较的小陷阱</a></li><li><a href=\"#8-索引下推-icp\">8. 索引下推 ICP</a></li><li><a href=\"#9-explain-中-type-字段怎么看\">9. EXPLAIN 中 type 字段怎么看</a></li><li><a href=\"#10-explain-常见字段和-extra\">10. EXPLAIN 常见字段和 Extra</a></li><li><a href=\"#11-覆盖索引排序和-limit-优化场景题\">11. 覆盖索引、排序和 limit 优化场景题</a></li><li><a href=\"#12-今日复盘重点\">12. 今日复盘重点</a></li></ul>\n      <hr>\n      <h1>1. InnoDB 索引为什么选择 B+ 树</h1>\n      <h2>面试题</h2>\n      <p>MySQL InnoDB 的索引为什么选择 B+ 树，而不是 Hash、红黑树或者 B 树？</p>\n      <h2>标准答案</h2>\n      <p>InnoDB 使用 B+ 树作为主要索引结构，核心原因是它非常适合磁盘存储场景，可以减少磁盘 IO，并且天然支持范围查询和排序。</p>\n      <p>B+ 树的非叶子节点只保存 key 和页指针，不保存完整行数据，所以一个 16KB 的页里可以放下更多 key。每个节点能保存更多 key，树的分叉就更多，树高就更低。实际生产中，即使有千万级数据，B+ 树通常也只有 3 到 4 层，查询一次数据需要访问的页数比较少。</p>\n      <p>B+ 树的叶子节点之间通过链表连接，所有数据都在叶子节点上。这样范围查询非常方便，比如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select *\nfrom user\nwhere id between 100 and 200;</code></pre></figure>\n      <p>数据库只需要先定位到 <code>id = 100</code> 附近的叶子节点，然后顺着叶子节点链表向后扫描即可。</p>\n      <p>所以，B+ 树的主要优势可以总结为：</p>\n      <ol><li>树高低，磁盘 IO 少。</li><li>非叶子节点只存 key 和指针，单页能容纳更多 key。</li><li>叶子节点有序并通过链表连接，适合范围查询。</li><li>所有数据都在叶子节点，查询路径更稳定。</li><li>可以支持排序、范围查询、最左前缀匹配等数据库常见需求。</li></ol>\n      <h2>对比红黑树</h2>\n      <p>红黑树本质上是自平衡二叉搜索树。二叉树每个节点最多只有两个子节点，数据量一大，树高就会明显增加。</p>\n      <p>数据库索引通常存在磁盘页中，访问一个节点往往意味着一次磁盘页访问。树越高，需要访问的页越多，磁盘 IO 成本越高。</p>\n      <p>所以红黑树适合内存中的有序结构，比如 Java 的 <code>TreeMap</code>、<code>TreeSet</code>，但不适合作为磁盘数据库的大规模索引结构。</p>\n      <h2>对比 Hash</h2>\n      <p>Hash 索引等值查询很快，比如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where id = 100</code></pre></figure>\n      <p>但 Hash 不适合数据库通用索引，主要因为：</p>\n      <ol><li>不支持范围查询。</li><li>不支持排序。</li><li>不支持最左前缀匹配。</li><li>Hash 冲突需要额外处理。</li><li>只能很好地处理等值查询。</li></ol>\n      <p>例如下面这些查询，Hash 索引都不擅长：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where id &gt; 100;\nwhere name like &#x27;张%&#x27;;\norder by create_time;</code></pre></figure>\n      <p>注意：Hash 不适合作为 InnoDB 通用索引结构，原因不是“它不支持事务或崩溃恢复”。事务、崩溃恢复主要是存储引擎能力，不是 Hash 数据结构本身的核心缺陷。</p>\n      <h2>对比 B 树</h2>\n      <p>B 树和 B+ 树都属于多路平衡搜索树，但它们有重要区别：</p>\n      <div class=\"md-table\"><table><thead><tr><th>对比点</th><th>B 树</th><th>B+ 树</th></tr></thead><tbody><tr><td>数据存放位置</td><td>非叶子节点和叶子节点都可能存数据</td><td>数据只在叶子节点</td></tr><tr><td>非叶子节点容量</td><td>因为要存数据，能放的 key 更少</td><td>只存 key 和指针，能放更多 key</td></tr><tr><td>树高</td><td>相对可能更高</td><td>更矮</td></tr><tr><td>范围查询</td><td>需要中序遍历，局部跳转更多</td><td>叶子链表顺序扫描</td></tr><tr><td>查询稳定性</td><td>可能在非叶子节点命中，路径不完全一致</td><td>最终都走到叶子节点</td></tr></tbody></table></div>\n      <p>B 树不是不能用，而是 B+ 树更适合数据库索引这种磁盘页模型。</p>\n      <h2>易错点</h2>\n      <h3>易错点 1：说“B+ 树叶子节点只存 key 和主键”</h3>\n      <p>这个说法不完整。</p>\n      <p>在 InnoDB 中：</p>\n      <ol><li>如果是主键索引，也就是聚簇索引，叶子节点存的是整行数据。</li><li>如果是普通索引，也就是二级索引，叶子节点存的是索引列和主键值。</li><li>非叶子节点主要存 key 和页指针。</li></ol>\n      <h3>易错点 2：说 Hash 不行是因为事务和崩溃恢复</h3>\n      <p>这个角度不对。Hash 不适合作为通用索引，主要是因为不能支持范围、排序和前缀匹配。</p>\n      <h2>面试背诵版</h2>\n      <blockquote>InnoDB 选择 B+ 树，核心是为了减少磁盘 IO。B+ 树的非叶子节点只保存 key 和页指针，一个 16KB 的页能容纳更多 key，所以树高很低，通常 3 到 4 层就能支持大量数据。并且 B+ 树叶子节点之间有链表，非常适合范围查询和排序。红黑树是二叉树，数据量大时树高太高；Hash 虽然等值查询快，但不支持范围查询、排序和最左前缀；B 树的非叶子节点也存数据，导致单页 key 更少，范围查询也不如 B+ 树方便。所以 InnoDB 更适合用 B+ 树作为索引结构。</blockquote>\n      <hr>\n      <h1>2. 聚簇索引、非聚簇索引、回表、覆盖索引</h1>\n      <h2>面试题</h2>\n      <p>聚簇索引和非聚簇索引有什么区别？什么是回表？什么是覆盖索引？</p>\n      <h2>标准答案</h2>\n      <p>在 InnoDB 中，主键索引通常就是聚簇索引。聚簇索引的叶子节点存放整行数据，所以通过主键查询时，可以直接在主键 B+ 树上找到完整记录。</p>\n      <p>非聚簇索引一般指二级索引，也就是普通索引、唯一索引、联合索引等非主键索引。二级索引的叶子节点不直接存整行数据，而是存索引列和主键值。</p>\n      <p>举例：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create table user (\n    id bigint primary key,\n    name varchar(64),\n    age int,\n    city varchar(64),\n    index idx_name(name)\n);</code></pre></figure>\n      <p>对于主键索引 <code>id</code>：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">主键索引叶子节点：id + 整行数据</code></pre></figure>\n      <p>对于二级索引 <code>idx_name(name)</code>：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">二级索引叶子节点：name + id</code></pre></figure>\n      <p>如果执行：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select *\nfrom user\nwhere name = &#x27;张三&#x27;;</code></pre></figure>\n      <p>执行过程大概是：</p>\n      <ol><li>先走 <code>idx_name</code> 二级索引，找到 <code>name = &#x27;张三&#x27;</code> 对应的主键 <code>id</code>。</li><li>再拿这个 <code>id</code> 去主键索引中查整行数据。</li></ol>\n      <p>第二步就叫回表。</p>\n      <h2>什么是回表</h2>\n      <p>回表就是通过二级索引查到主键值后，再回到聚簇索引中查询完整行数据。</p>\n      <p>不是“回到表文件里随便找”，而是回到 InnoDB 的主键 B+ 树中查。</p>\n      <p>示例：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_name on user(name);\n\nselect age, city\nfrom user\nwhere name = &#x27;张三&#x27;;</code></pre></figure>\n      <p>如果 <code>age</code>、<code>city</code> 不在 <code>idx_name</code> 索引中，则流程是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">idx_name 二级索引 -&gt; 找到主键 id -&gt; 主键索引 -&gt; 找到 age、city</code></pre></figure>\n      <h2>什么是覆盖索引</h2>\n      <p>覆盖索引不是一种新的索引类型，而是一种查询状态。</p>\n      <p>只要查询需要的字段都能从某个索引中直接拿到，不需要回表，就叫覆盖索引。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_name_age on user(name, age);\n\nselect id, name, age\nfrom user\nwhere name = &#x27;张三&#x27;;</code></pre></figure>\n      <p><code>idx_name_age</code> 这个二级索引中包含：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">name + age + 主键 id</code></pre></figure>\n      <p>查询字段刚好是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">id, name, age</code></pre></figure>\n      <p>都能从二级索引中拿到，所以不需要回表。</p>\n      <p>此时 <code>EXPLAIN</code> 的 <code>Extra</code> 中可能出现：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index</code></pre></figure>\n      <p>这通常表示使用了覆盖索引。</p>\n      <h2>聚簇索引的选择规则</h2>\n      <p>InnoDB 聚簇索引选择规则：</p>\n      <ol><li>如果有主键，主键就是聚簇索引。</li><li>如果没有主键，选择第一个非空唯一索引作为聚簇索引。</li><li>如果连非空唯一索引都没有，InnoDB 会生成一个隐藏的 6 字节 <code>row_id</code> 作为聚簇索引。</li></ol>\n      <h2>为什么推荐自增主键</h2>\n      <p>推荐使用自增主键，主要原因是：</p>\n      <ol><li>自增主键递增插入，B+ 树通常在尾部追加。</li><li>减少页分裂。</li><li>减少数据移动。</li><li>主键值更短，二级索引也更小。</li></ol>\n      <p>如果使用 UUID 作为主键：</p>\n      <ol><li>UUID 长度大，二级索引叶子节点也要存主键，索引体积变大。</li><li>UUID 无序插入，容易造成页分裂。</li><li>B+ 树维护成本更高。</li></ol>\n      <h2>面试背诵版</h2>\n      <blockquote>InnoDB 中主键索引就是聚簇索引，叶子节点存放整行数据。一张表只能有一个聚簇索引。普通索引、联合索引属于二级索引，叶子节点存放索引列和主键值。通过二级索引查询时，如果查询字段不在索引里，需要先拿到主键值，再去主键索引中查整行数据，这个过程叫回表。如果查询字段都能从索引中直接拿到，不需要回表，就叫覆盖索引，<code>EXPLAIN</code> 里通常可以看到 <code>Using index</code>。</blockquote>\n      <hr>\n      <h1>3. 联合索引和最左前缀原则</h1>\n      <h2>面试题</h2>\n      <p>联合索引的最左前缀原则是什么？下面这个索引哪些 SQL 能用上？</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_user_status_time on orders(user_id, status, create_time);</code></pre></figure>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 1\nselect * from orders where user_id = 1;\n\n-- 2\nselect * from orders where status = 1;\n\n-- 3\nselect * from orders where user_id = 1 and status = 1;\n\n-- 4\nselect * from orders where user_id = 1 and create_time &gt; &#x27;2026-01-01&#x27;;\n\n-- 5\nselect * from orders where user_id = 1 and status &gt; 1 and create_time &gt; &#x27;2026-01-01&#x27;;\n\n-- 6\nselect * from orders where user_id = 1 order by create_time;</code></pre></figure>\n      <h2>最左前缀原则</h2>\n      <p>联合索引 <code>(a, b, c)</code> 的底层排序规则不是分别维护三棵树，而是按照多列组合排序：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">先按 a 排序\na 相同，再按 b 排序\nb 相同，再按 c 排序</code></pre></figure>\n      <p>所以联合索引能高效使用的前提是从最左列开始连续匹配。</p>\n      <p>例如索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(a, b, c)</code></pre></figure>\n      <p>可以较好使用：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where a = 1;\nwhere a = 1 and b = 2;\nwhere a = 1 and b = 2 and c = 3;\nwhere a = 1 and b &gt; 2;</code></pre></figure>\n      <p>不符合最左前缀：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where b = 2;\nwhere c = 3;\nwhere b = 2 and c = 3;</code></pre></figure>\n      <p>因为没有从最左列 <code>a</code> 开始。</p>\n      <h2>范围查询的影响</h2>\n      <p>联合索引中，等值匹配可以继续向右使用索引。</p>\n      <p>但是遇到范围查询后，范围列可以使用索引，范围列右边的列通常不能继续用于索引定位。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(a, b, c)\n\nwhere a = 1 and b &gt; 10 and c = 3;</code></pre></figure>\n      <p>一般可以用到：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">a、b</code></pre></figure>\n      <p>但是 <code>c</code> 通常不能继续用于缩小索引扫描范围。</p>\n      <p>原因是：当 <code>b &gt; 10</code> 时，<code>b</code> 已经是一个范围，范围内的记录再按 <code>c</code> 看，并不能形成一个全局连续可定位的区间。</p>\n      <h2>逐条判断</h2>\n      <h3>1. <code>where user_id = 1</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders where user_id = 1;</code></pre></figure>\n      <p>可以使用联合索引。</p>\n      <p>命中了最左列 <code>user_id</code>，可以用到索引的第一列。</p>\n      <h3>2. <code>where status = 1</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders where status = 1;</code></pre></figure>\n      <p>通常不能使用这个联合索引做高效定位。</p>\n      <p>原因是跳过了最左列 <code>user_id</code>，不符合最左前缀原则。</p>\n      <p>补充：MySQL 8.0 有一些场景可能出现 Index Skip Scan，但面试和常规优化中不要把它当成主要依赖。设计索引时仍然应该遵守最左前缀原则。</p>\n      <h3>3. <code>where user_id = 1 and status = 1</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders\nwhere user_id = 1 and status = 1;</code></pre></figure>\n      <p>可以使用联合索引。</p>\n      <p>可以用到：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id + status</code></pre></figure>\n      <p>这是典型的从左到右连续等值匹配。</p>\n      <h3>4. <code>where user_id = 1 and create_time &gt; &#x27;2026-01-01&#x27;</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders\nwhere user_id = 1 and create_time &gt; &#x27;2026-01-01&#x27;;</code></pre></figure>\n      <p>可以部分使用联合索引。</p>\n      <p>可以用到：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id</code></pre></figure>\n      <p>但是 <code>create_time</code> 中间跳过了 <code>status</code>，所以它通常不能用于联合索引的连续定位。</p>\n      <p>不过，由于 <code>create_time</code> 本身也在联合索引里，MySQL 可能通过 ICP，也就是索引下推，在索引层做一部分过滤，减少回表。</p>\n      <h3>5. <code>where user_id = 1 and status &gt; 1 and create_time &gt; &#x27;2026-01-01&#x27;</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders\nwhere user_id = 1\n  and status &gt; 1\n  and create_time &gt; &#x27;2026-01-01&#x27;;</code></pre></figure>\n      <p>可以部分使用联合索引。</p>\n      <p>一般可以用到：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id + status</code></pre></figure>\n      <p>因为 <code>user_id</code> 是等值条件，<code>status</code> 是范围条件。</p>\n      <p>但是 <code>status</code> 之后的 <code>create_time</code> 通常不能继续用于索引定位。</p>\n      <p><code>create_time</code> 可能作为 ICP 条件在索引层过滤，但这不等于它参与了联合索引范围定位。</p>\n      <h3>6. <code>where user_id = 1 order by create_time</code></h3>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select * from orders\nwhere user_id = 1\norder by create_time;</code></pre></figure>\n      <p>这个查询可以用 <code>user_id</code> 做过滤，但通常不能直接利用这个联合索引完成 <code>create_time</code> 排序。</p>\n      <p>原因是索引顺序是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id -&gt; status -&gt; create_time</code></pre></figure>\n      <p>当 <code>user_id = 1</code> 固定后，索引内部是先按 <code>status</code> 排序，再按 <code>create_time</code> 排序。</p>\n      <p>也就是说，整体顺序类似：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">status = 1, create_time = ...\nstatus = 2, create_time = ...\nstatus = 3, create_time = ...</code></pre></figure>\n      <p>这并不能保证全局按 <code>create_time</code> 有序。</p>\n      <p>所以 <code>order by create_time</code> 通常还需要额外排序，也就是可能出现：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using filesort</code></pre></figure>\n      <h2>常见误区</h2>\n      <h3>误区 1：把“用到索引”和“用满联合索引”混为一谈</h3>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where user_id = 1 and create_time &gt; &#x27;2026-01-01&#x27;</code></pre></figure>\n      <p>不能说“完全没用索引”。它至少可以用到 <code>user_id</code>。</p>\n      <p>更准确的说法是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">能用到联合索引的最左列 user_id，但 create_time 因为跳过 status，不能继续用于联合索引定位。</code></pre></figure>\n      <h3>误区 2：以为排序字段在索引里就一定不会 filesort</h3>\n      <p>不对。</p>\n      <p>排序能不能用索引，要看：</p>\n      <ol><li>排序字段是否符合联合索引顺序。</li><li>排序字段前面的列是否被等值固定。</li><li>排序方向是否一致。</li><li>是否混合升序降序，以及 MySQL 版本是否支持对应索引能力。</li></ol>\n      <h2>面试背诵版</h2>\n      <blockquote>联合索引遵循最左前缀原则。比如 <code>(a, b, c)</code>，索引整体按 <code>a</code> 排序，<code>a</code> 相同再按 <code>b</code>，<code>b</code> 相同再按 <code>c</code>。所以查询要从最左列开始连续匹配。等值条件可以继续向右使用索引，遇到范围查询后，范围列可以用索引，但范围列右边的列通常不能继续用于索引定位。如果跳过中间列，后面的列一般不能用于定位，但可能通过 ICP 在索引层过滤。</blockquote>\n      <hr>\n      <h1>4. 常见索引失效或不走索引场景</h1>\n      <h2>面试题</h2>\n      <p>什么情况下索引会失效？至少说 6 种，并解释为什么。</p>\n      <h2>说明</h2>\n      <p>严格说，“索引失效”这个词有点笼统。更准确地说，有些情况是：</p>\n      <ol><li>完全不能使用索引定位。</li><li>只能使用联合索引的一部分。</li><li>能扫索引，但效率不高。</li><li>优化器判断全表扫描成本更低，所以放弃索引。</li></ol>\n      <p>面试中可以统称为“索引失效或不走索引场景”，但解释时要分清。</p>\n      <h2>1. 违反最左前缀原则</h2>\n      <p>索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(a, b, c)</code></pre></figure>\n      <p>查询：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where b = 1;</code></pre></figure>\n      <p>因为跳过了最左列 <code>a</code>，无法按联合索引的有序结构定位。</p>\n      <h2>2. 范围查询右侧列无法继续用于索引定位</h2>\n      <p>索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(a, b, c)</code></pre></figure>\n      <p>查询：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where a = 1 and b &gt; 10 and c = 3;</code></pre></figure>\n      <p>一般可以用到：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">a + b</code></pre></figure>\n      <p>但是 <code>c</code> 通常不能继续用于索引定位。</p>\n      <p>注意：这不是整个索引都失效，而是范围列右边的索引列不能继续参与定位。</p>\n      <h2>3. 对索引列使用函数</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where date(create_time) = &#x27;2026-06-02&#x27;;</code></pre></figure>\n      <p>如果索引是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(create_time)</code></pre></figure>\n      <p>索引中保存的是原始 <code>create_time</code> 值，不是 <code>date(create_time)</code> 的结果。</p>\n      <p>对列做函数运算，相当于改变了索引列的比较形式，普通 B+ 树索引无法直接利用。</p>\n      <p>推荐改写：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where create_time &gt;= &#x27;2026-06-02 00:00:00&#x27;\n  and create_time &lt;  &#x27;2026-06-03 00:00:00&#x27;;</code></pre></figure>\n      <p>这样可以继续使用 <code>create_time</code> 的范围索引。</p>\n      <h2>4. 对索引列使用表达式运算</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where age + 1 = 18;</code></pre></figure>\n      <p>如果 <code>age</code> 上有索引，这种写法通常不能很好使用索引。</p>\n      <p>推荐改写为：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where age = 17;</code></pre></figure>\n      <p>原则是：不要在索引列上做计算，把计算挪到常量侧。</p>\n      <h2>5. 字符串列发生隐式类型转换</h2>\n      <p>字段：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">phone varchar(20)</code></pre></figure>\n      <p>索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(phone)</code></pre></figure>\n      <p>错误写法：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where phone = 13800138000;</code></pre></figure>\n      <p>因为 <code>phone</code> 是字符串列，右边是数字，MySQL 可能把 <code>phone</code> 列转换成数字再比较，类似：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where cast(phone as signed) = 13800138000;</code></pre></figure>\n      <p>这相当于对索引列做函数计算，可能导致索引失效。</p>\n      <p>正确写法：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where phone = &#x27;13800138000&#x27;;</code></pre></figure>\n      <h2>6. LIKE 左模糊</h2>\n      <p>可以使用索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where name like &#x27;张%&#x27;;</code></pre></figure>\n      <p>通常不能使用索引定位：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where name like &#x27;%张&#x27;;\nwhere name like &#x27;%张%&#x27;;</code></pre></figure>\n      <p>B+ 树索引按从左到右的字符顺序排列，前缀确定时可以定位范围；左边不确定时无法确定扫描起点。</p>\n      <h2>7. OR 条件中有一边不能走索引</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where indexed_col = 1\n   or no_index_col = 2;</code></pre></figure>\n      <p>如果 <code>no_index_col</code> 没有索引，优化器可能认为走索引意义不大，直接选择全表扫描。</p>\n      <p>优化方向：</p>\n      <ol><li>给两边条件都建立合适索引。</li><li>改写为 <code>union</code> 或 <code>union all</code>，让每个子查询分别使用索引。</li><li>根据数据分布判断是否真的需要优化。</li></ol>\n      <h2>8. 负向条件选择性差</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where status != 1;\nwhere status not in (1, 2);</code></pre></figure>\n      <p>这类条件不一定完全不能用索引，但通常选择性较差。</p>\n      <p>如果大部分数据都满足条件，走索引再大量回表，成本可能比全表扫描更高。</p>\n      <h2>9. 优化器认为全表扫描成本更低</h2>\n      <p>这是非常重要的面试加分点。</p>\n      <p>即使建立了索引，MySQL 也不一定会用。优化器会根据统计信息估算成本。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where status = 1;</code></pre></figure>\n      <p>如果 <code>status = 1</code> 的数据占全表 90%，走索引会查到大量主键，再大量回表。此时优化器可能认为直接全表扫描更便宜。</p>\n      <p>这种情况下不能简单说“索引失效”，更准确是“优化器基于成本选择了全表扫描”。</p>\n      <h2>10. 索引区分度太低</h2>\n      <p>字段值重复度很高，索引选择性就差。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">gender: 男/女\nstatus: 0/1\ndeleted: 0/1</code></pre></figure>\n      <p>这种字段单独建索引不一定有效。更常见的做法是把它放进联合索引中，配合高区分度字段使用。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(user_id, status, create_time)</code></pre></figure>\n      <p>其中 <code>user_id</code> 区分度高，<code>status</code> 用来进一步过滤，<code>create_time</code> 用来排序或范围查询。</p>\n      <h2>面试背诵版</h2>\n      <blockquote>常见索引失效或不走索引的情况有：违反最左前缀原则、范围查询右侧列不能继续用于索引定位、对索引列使用函数或表达式、字符串字段发生隐式类型转换、<code>like</code> 左模糊、<code>or</code> 条件中有一边不能走索引、负向查询选择性差、索引区分度低，以及优化器认为全表扫描成本更低。核心原因是 B+ 树依赖有序性和高选择性，如果条件破坏了有序定位，或者回表成本太高，优化器就可能不走索引。</blockquote>\n      <hr>\n      <h1>5. LIKE 和函数导致索引失效</h1>\n      <h2>面试题</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_name on user(name);</code></pre></figure>\n      <p>下面三个条件哪个能走索引？为什么？</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 1\nwhere name like &#x27;张%&#x27;;\n\n-- 2\nwhere name like &#x27;%张&#x27;;\n\n-- 3\nwhere substring(name, 1, 1) = &#x27;张&#x27;;</code></pre></figure>\n      <h2>判断</h2>\n      <h3>1. <code>name like &#x27;张%&#x27;</code></h3>\n      <p>可以走索引。</p>\n      <p>原因是前缀确定，B+ 树可以定位到以“张”开头的第一个位置，然后向后扫描直到不满足前缀条件。</p>\n      <p>这个查询本质上可以理解为一个范围查询：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">name &gt;= &#x27;张&#x27;\n并且 name 小于某个不再以 张 开头的边界</code></pre></figure>\n      <p>具体边界由 MySQL 内部处理，不需要手写。</p>\n      <h2>2. <code>name like &#x27;%张&#x27;</code></h2>\n      <p>通常不能走索引定位。</p>\n      <p>原因是左边不确定，B+ 树无法知道应该从哪里开始查。</p>\n      <p>如果要支持后缀匹配，可以考虑：</p>\n      <ol><li>反转字段单独存一列，例如 <code>reverse_name</code>。</li><li>使用全文索引，适合文本检索场景。</li><li>使用 Elasticsearch 等搜索引擎。</li><li>小数据量直接扫描，避免过度设计。</li></ol>\n      <h2>3. <code>substring(name, 1, 1) = &#x27;张&#x27;</code></h2>\n      <p>普通索引通常不能使用。</p>\n      <p>原因是索引中保存的是原始 <code>name</code> 值，而查询条件对 <code>name</code> 做了函数运算。</p>\n      <p>这类似：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where 函数(name) = &#x27;张&#x27;;</code></pre></figure>\n      <p>普通 B+ 树无法直接按函数结果定位。</p>\n      <h2>MySQL 8.0 的补充</h2>\n      <p>MySQL 8.0 支持函数索引，也可以通过生成列配合索引优化函数查询。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">alter table user\nadd column name_first varchar(8)\ngenerated always as (substring(name, 1, 1)) stored,\nadd index idx_name_first(name_first);</code></pre></figure>\n      <p>然后查询：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where name_first = &#x27;张&#x27;;</code></pre></figure>\n      <p>但是面试中默认语境通常是普通索引，所以仍然要回答：对索引列使用函数会导致普通索引无法直接使用。</p>\n      <h2>面试背诵版</h2>\n      <blockquote><code>like &#x27;张%&#x27;</code> 可以走索引，因为前缀确定，B+ 树可以按前缀定位范围；<code>like &#x27;%张&#x27;</code> 和 <code>like &#x27;%张%&#x27;</code> 通常不能走索引，因为左边不确定，无法定位起点；<code>substring(name, 1, 1) = &#x27;张&#x27;</code> 一般也不能走普通索引，因为对索引列做了函数计算，索引中保存的是原始值，不是函数结果。</blockquote>\n      <hr>\n      <h1>6. 隐式类型转换和索引使用</h1>\n      <h2>面试题</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_phone on user(phone);\n-- phone 是 varchar 类型</code></pre></figure>\n      <p>这两个 SQL 哪个更容易走索引？为什么？</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 1\nwhere phone = &#x27;13800138000&#x27;;\n\n-- 2\nwhere phone = 13800138000;</code></pre></figure>\n      <h2>标准答案</h2>\n      <p>第一种更容易走索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where phone = &#x27;13800138000&#x27;;</code></pre></figure>\n      <p>原因是 <code>phone</code> 是 <code>varchar</code> 类型，查询条件也是字符串，类型一致，可以按照索引中的字符串值进行查找。</p>\n      <p>第二种写法：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where phone = 13800138000;</code></pre></figure>\n      <p>右边是数字，左边是字符串列。MySQL 可能发生隐式类型转换，把 <code>phone</code> 列转换成数字再比较。</p>\n      <p>类似：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where cast(phone as signed) = 13800138000;</code></pre></figure>\n      <p>这相当于对索引列做了函数操作，可能导致普通索引无法用于定位。</p>\n      <h2>为什么不是“破坏了索引结构”</h2>\n      <p>索引结构本身没有被破坏。</p>\n      <p>真正的问题是：查询条件的比较方式让 MySQL 不能直接按照索引中保存的原始字符串值进行有序查找。</p>\n      <p>换句话说：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">索引还在，但当前 SQL 的写法让优化器难以使用它做高效定位。</code></pre></figure>\n      <h2>生产建议</h2>\n      <ol><li>字段是什么类型，SQL 参数就传什么类型。</li><li>字符串字段比较时一定加引号。</li><li>Java 中 MyBatis、JPA、JDBC 参数绑定要保持类型一致。</li><li>手机号、身份证号、订单号通常不要用数字类型，应该用字符串。</li></ol>\n      <p>手机号不适合用数字类型的原因：</p>\n      <ol><li>不参与数学运算。</li><li>可能有前导零。</li><li>长度可能超过部分数字类型范围。</li><li>本质是标识符，不是数值。</li></ol>\n      <h2>面试背诵版</h2>\n      <blockquote>如果 <code>phone</code> 是 <code>varchar</code>，<code>where phone = &#x27;13800138000&#x27;</code> 更容易走索引，因为类型一致。<code>where phone = 13800138000</code> 可能触发隐式类型转换，MySQL 可能把字符串列 <code>phone</code> 转成数字再比较，相当于对索引列做函数操作，普通索引就不能很好用于定位。所以开发中要保证字段类型和参数类型一致。</blockquote>\n      <hr>\n      <h1>7. 数字列和字符串常量比较的小陷阱</h1>\n      <h2>面试题</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_age on user(age);\n-- age 是 int 类型</code></pre></figure>\n      <p>下面两个 SQL 哪个更可能不走索引？为什么？</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">-- 1\nwhere age = &#x27;18&#x27;;\n\n-- 2\nwhere age = 18;</code></pre></figure>\n      <h2>标准答案</h2>\n      <p>这题的陷阱是：如果 <code>age</code> 是 <code>int</code>，那么：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where age = &#x27;18&#x27;;</code></pre></figure>\n      <p>通常仍然可以走索引。</p>\n      <p>原因是 MySQL 通常会把右边的字符串常量 <code>&#x27;18&#x27;</code> 转成数字 <code>18</code>，转换发生在常量侧，而不是索引列 <code>age</code> 上。</p>\n      <p>转换后类似：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where age = 18;</code></pre></figure>\n      <p>这不会破坏 <code>age</code> 索引的有序定位能力。</p>\n      <p>更规范的写法当然是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where age = 18;</code></pre></figure>\n      <h2>和 varchar 列比较的区别</h2>\n      <p>危险的是这种：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">phone varchar(20)\n\nwhere phone = 13800138000;</code></pre></figure>\n      <p>这时 MySQL 可能把列 <code>phone</code> 转成数字比较，相当于：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where cast(phone as signed) = 13800138000;</code></pre></figure>\n      <p>转换发生在索引列侧，就可能导致索引失效。</p>\n      <h2>判断原则</h2>\n      <p>看隐式转换发生在哪里：</p>\n      <ol><li>如果转换发生在常量侧，一般不影响索引。</li><li>如果转换发生在索引列侧，就可能导致索引不能用于定位。</li></ol>\n      <h2>面试背诵版</h2>\n      <blockquote>如果 <code>age</code> 是 int，<code>where age = &#x27;18&#x27;</code> 通常也可以走索引，因为 MySQL 会把字符串常量转换成数字，转换发生在常量侧，不影响索引列的有序性。但如果索引列是 varchar，却拿数字去比较，MySQL 可能把列转换成数字，相当于对索引列做函数操作，就可能导致索引失效。实际开发中仍然建议类型保持一致。</blockquote>\n      <hr>\n      <h1>8. 索引下推 ICP</h1>\n      <h2>面试题</h2>\n      <p>什么是索引下推 ICP？它解决了什么问题？</p>\n      <p>示例：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_name_age on user(name, age);\n\nselect *\nfrom user\nwhere name like &#x27;张%&#x27;\n  and age = 18;</code></pre></figure>\n      <h2>ICP 是什么</h2>\n      <p>ICP 全称是 Index Condition Pushdown，中文一般叫索引下推。</p>\n      <p>它的核心作用是：把一部分可以通过索引字段判断的条件下推到存储引擎层，在扫描索引时先过滤，减少回表次数。</p>\n      <h2>没有 ICP 时</h2>\n      <p>索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(name, age)</code></pre></figure>\n      <p>查询：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where name like &#x27;张%&#x27; and age = 18;</code></pre></figure>\n      <p>由于 <code>name like &#x27;张%&#x27;</code> 是范围查询，<code>age</code> 通常不能继续用于联合索引定位。</p>\n      <p>没有 ICP 时，执行流程可能是：</p>\n      <ol><li>存储引擎根据 <code>name like &#x27;张%&#x27;</code> 扫描二级索引。</li><li>对每条满足 <code>name</code> 范围的索引记录，都拿主键去回表。</li><li>Server 层拿到完整行后，再判断 <code>age = 18</code>。</li><li>不满足 <code>age = 18</code> 的记录被丢弃。</li></ol>\n      <p>问题是：会产生大量无意义回表。</p>\n      <h2>有 ICP 时</h2>\n      <p>因为 <code>age</code> 字段也在联合索引 <code>idx_name_age(name, age)</code> 中，所以即使它不能继续用于索引范围定位，也可以在索引扫描阶段先判断。</p>\n      <p>有 ICP 后，流程变成：</p>\n      <ol><li>存储引擎根据 <code>name like &#x27;张%&#x27;</code> 扫描二级索引。</li><li>在索引层直接判断 <code>age = 18</code>。</li><li>只有 <code>age = 18</code> 的记录才回表。</li><li>减少回表次数。</li></ol>\n      <h2>ICP 不能做什么</h2>\n      <p>ICP 不能把范围查询后面的字段重新变成“可用于联合索引定位的字段”。</p>\n      <p>也就是说：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where name like &#x27;张%&#x27; and age = 18</code></pre></figure>\n      <p><code>age</code> 在这里不是继续缩小 B+ 树扫描区间，而是在扫描出来的索引记录上做过滤。</p>\n      <p>所以要区分：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">索引定位：决定扫描范围从哪里开始，到哪里结束。\n索引下推：扫描过程中用索引字段提前过滤。</code></pre></figure>\n      <h2>EXPLAIN 表现</h2>\n      <p>使用 ICP 时，<code>EXPLAIN</code> 的 <code>Extra</code> 中可能出现：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index condition</code></pre></figure>\n      <p>注意它和 <code>Using index</code> 不一样：</p>\n      <div class=\"md-table\"><table><thead><tr><th>Extra</th><th>含义</th></tr></thead><tbody><tr><td>Using index</td><td>覆盖索引，不需要回表</td></tr><tr><td>Using index condition</td><td>索引下推，在存储引擎层过滤索引条件</td></tr></tbody></table></div>\n      <h2>面试背诵版</h2>\n      <blockquote>ICP 是索引下推，作用是把能通过索引字段判断的条件下推到存储引擎层，在扫描索引时先过滤，减少回表次数。比如联合索引 <code>(name, age)</code>，查询 <code>name like &#x27;张%&#x27; and age = 18</code>，<code>name</code> 是范围条件，<code>age</code> 通常不能继续用于索引定位，但因为 <code>age</code> 在索引里，所以可以通过 ICP 在索引层先判断 <code>age = 18</code>，满足条件的记录才回表。<code>EXPLAIN</code> 中通常表现为 <code>Using index condition</code>。</blockquote>\n      <hr>\n      <h1>9. EXPLAIN 中 type 字段怎么看</h1>\n      <h2>面试题</h2>\n      <p>看执行计划时，<code>type</code> 字段常见有哪些？从好到坏大概怎么排？</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">system / const / eq_ref / ref / range / index / ALL</code></pre></figure>\n      <h2>排序</h2>\n      <p>从好到坏大致是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></pre></figure>\n      <p>注意：这个排序只是一般经验，不是绝对性能结论。最终还要结合 <code>rows</code>、<code>filtered</code>、<code>key</code>、<code>Extra</code>、数据量和 SQL 业务语义判断。</p>\n      <h2>system</h2>\n      <p><code>system</code> 是 <code>const</code> 的特殊情况，表示表中只有一行数据。</p>\n      <p>这种情况很少见，一般可以理解为最快访问类型之一。</p>\n      <h2>const</h2>\n      <p><code>const</code> 通常表示通过主键或唯一索引做等值查询，最多匹配一行。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select *\nfrom user\nwhere id = 1;</code></pre></figure>\n      <p>如果 <code>id</code> 是主键，那么结果最多一行，优化器可以把它当成常量处理。</p>\n      <h2>eq_ref</h2>\n      <p><code>eq_ref</code> 常见于多表 join。</p>\n      <p>含义是：对于前表的每一行，后表通过主键或唯一索引匹配，最多匹配一行。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select *\nfrom orders o\njoin user u on o.user_id = u.id;</code></pre></figure>\n      <p>如果 <code>u.id</code> 是主键，那么对每个订单的 <code>user_id</code>，用户表最多匹配一行。</p>\n      <h2>ref</h2>\n      <p><code>ref</code> 表示使用普通索引进行等值查询，可能匹配多行。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_status on orders(status);\n\nselect *\nfrom orders\nwhere status = 1;</code></pre></figure>\n      <p>如果 <code>status</code> 不是唯一索引，<code>status = 1</code> 可能匹配很多行，所以是 <code>ref</code>。</p>\n      <p>注意：<code>ref</code> 不是覆盖索引。覆盖索引看 <code>Extra</code> 中的 <code>Using index</code>。</p>\n      <h2>range</h2>\n      <p><code>range</code> 表示索引范围扫描。</p>\n      <p>常见条件：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where id &gt; 100;\nwhere id between 100 and 200;\nwhere create_time &gt;= &#x27;2026-01-01&#x27;;\nwhere name like &#x27;张%&#x27;;</code></pre></figure>\n      <p>范围扫描比等值查询通常更宽，但仍然可以利用索引定位范围。</p>\n      <h2>index</h2>\n      <p><code>index</code> 表示全索引扫描。</p>\n      <p>它会扫描整棵索引树，通常比 <code>ALL</code> 稍好，因为索引可能比整行数据更小。</p>\n      <p>但本质上仍然是全量扫描，不代表一定很快。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select name\nfrom user;</code></pre></figure>\n      <p>如果 <code>name</code> 有索引，MySQL 可能直接扫描 <code>idx_name</code>，不扫整张表。</p>\n      <p>这时可能是 <code>index</code>。</p>\n      <h2>ALL</h2>\n      <p><code>ALL</code> 表示全表扫描。</p>\n      <p>通常是最差访问类型，尤其在大表中要警惕。</p>\n      <p>但小表 <code>ALL</code> 不一定有问题。比如一张配置表只有几十行，全表扫描可能比走索引还便宜。</p>\n      <h2>面试背诵版</h2>\n      <blockquote><code>type</code> 表示访问类型，常见从好到坏是 <code>system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code>。<code>const</code> 一般是主键或唯一索引等值查询，最多一行；<code>eq_ref</code> 常见于 join 中通过主键或唯一索引关联；<code>ref</code> 是普通索引等值查询，可能多行；<code>range</code> 是范围扫描；<code>index</code> 是全索引扫描；<code>ALL</code> 是全表扫描。<code>type</code> 不是唯一性能指标，还要结合 <code>rows</code>、<code>key</code>、<code>Extra</code> 和数据量判断。</blockquote>\n      <hr>\n      <h1>10. EXPLAIN 常见字段和 Extra</h1>\n      <h2>面试题</h2>\n      <p><code>EXPLAIN</code> 里 <code>possible_keys</code>、<code>key</code>、<code>key_len</code>、<code>rows</code>、<code>Extra</code> 分别怎么看？</p>\n      <p>尤其是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index\nUsing index condition\nUsing filesort\nUsing temporary</code></pre></figure>\n      <p>各代表什么？</p>\n      <h2>possible_keys</h2>\n      <p><code>possible_keys</code> 表示优化器认为当前 SQL 可能使用到的索引。</p>\n      <p>它不代表最终一定使用这些索引。</p>\n      <p>如果这里是 <code>NULL</code>，说明优化器没有找到可用索引，或者认为没有合适索引。</p>\n      <h2>key</h2>\n      <p><code>key</code> 表示最终实际选择的索引。</p>\n      <p>如果 <code>key</code> 是 <code>NULL</code>，说明没有使用索引。</p>\n      <p>注意：<code>possible_keys</code> 有值但 <code>key</code> 为 <code>NULL</code> 是可能的，说明优化器虽然认为有候选索引，但最终估算成本后没有选择它。</p>\n      <h2>key_len</h2>\n      <p><code>key_len</code> 表示 MySQL 实际使用索引的长度。</p>\n      <p>它可以辅助判断联合索引用到了几列。</p>\n      <p>例如联合索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(a, b, c)</code></pre></figure>\n      <p>如果 <code>key_len</code> 只对应 <code>a</code> 的长度，说明可能只用到了第一列。</p>\n      <p>如果对应 <code>a + b</code>，说明可能用到了两列。</p>\n      <p>注意：<code>key_len</code> 会受字段类型、字符集、是否允许 <code>NULL</code>、变长字段长度字节影响。</p>\n      <h2>rows</h2>\n      <p><code>rows</code> 表示优化器估算需要扫描的行数。</p>\n      <p>它不是准确结果，而是根据统计信息估算出来的。</p>\n      <p>一般来说，<code>rows</code> 越大，查询风险越高，但还要结合 <code>filtered</code> 和实际返回行数判断。</p>\n      <h2>filtered</h2>\n      <p><code>filtered</code> 表示经过条件过滤后，预计剩余记录的比例。</p>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">rows = 10000\nfiltered = 10.00</code></pre></figure>\n      <p>大概表示扫描 10000 行，其中预计 10% 满足条件，也就是约 1000 行。</p>\n      <h2>Extra</h2>\n      <p><code>Extra</code> 展示额外执行信息，是定位 SQL 性能问题非常重要的字段。</p>\n      <h3>Using index</h3>\n      <p><code>Using index</code> 通常表示覆盖索引。</p>\n      <p>也就是查询需要的字段都能从索引中拿到，不需要回表。</p>\n      <p>示例：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_user_time on orders(user_id, create_time);\n\nselect user_id, create_time\nfrom orders\nwhere user_id = 100;</code></pre></figure>\n      <p>如果查询字段都在索引中，就可能出现 <code>Using index</code>。</p>\n      <h3>Using index condition</h3>\n      <p><code>Using index condition</code> 表示索引下推 ICP。</p>\n      <p>它代表 MySQL 会在存储引擎层使用索引字段先过滤，减少回表。</p>\n      <p>注意：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index          = 覆盖索引，通常不回表\nUsing index condition = 索引下推，通常仍可能回表，只是减少回表</code></pre></figure>\n      <h3>Using filesort</h3>\n      <p><code>Using filesort</code> 表示 MySQL 不能直接利用索引顺序完成排序，需要额外排序。</p>\n      <p>注意：名字里有 <code>file</code>，但不代表一定落磁盘文件。它可能在内存中排序，也可能因为数据量太大使用磁盘临时文件。</p>\n      <p>常见触发原因：</p>\n      <ol><li><code>order by</code> 字段没有合适索引。</li><li><code>order by</code> 顺序和联合索引顺序不匹配。</li><li>联合索引中排序字段前面的列没有被等值固定。</li><li>排序方向复杂，索引无法直接满足。</li></ol>\n      <h3>Using temporary</h3>\n      <p><code>Using temporary</code> 表示使用了临时表。</p>\n      <p>常见场景：</p>\n      <ol><li><code>group by</code></li><li><code>distinct</code></li><li>复杂 <code>order by</code></li><li>派生表</li><li>部分 union 查询</li></ol>\n      <p>它不一定代表一定很慢，但如果数据量很大，就要重点关注。</p>\n      <h2>Extra 里的几个对比</h2>\n      <div class=\"md-table\"><table><thead><tr><th>Extra</th><th>含义</th><th>是否一定坏</th></tr></thead><tbody><tr><td>Using index</td><td>覆盖索引，不回表</td><td>通常是好事</td></tr><tr><td>Using index condition</td><td>ICP，索引层过滤</td><td>通常是优化</td></tr><tr><td>Using where</td><td>Server 层还有条件过滤</td><td>正常现象，不一定坏</td></tr><tr><td>Using filesort</td><td>额外排序</td><td>大数据量要警惕</td></tr><tr><td>Using temporary</td><td>使用临时表</td><td>大数据量要警惕</td></tr></tbody></table></div>\n      <h2>面试背诵版</h2>\n      <blockquote><code>possible_keys</code> 是可能使用的索引，<code>key</code> 是实际选择的索引，<code>key_len</code> 可以辅助判断索引用到了多长，常用于分析联合索引用到几列，<code>rows</code> 是预计扫描行数，<code>Extra</code> 是额外执行信息。<code>Using index</code> 表示覆盖索引，不需要回表；<code>Using index condition</code> 表示索引下推，在索引层先过滤；<code>Using filesort</code> 表示不能利用索引顺序完成排序，需要额外排序；<code>Using temporary</code> 表示使用临时表，常见于分组、去重、复杂排序等场景。</blockquote>\n      <hr>\n      <h1>11. 覆盖索引、排序和 limit 优化场景题</h1>\n      <h2>面试题</h2>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">create index idx_user_time on orders(user_id, create_time);\n\nselect id, user_id, create_time\nfrom orders\nwhere user_id = 100\norder by create_time desc\nlimit 10;</code></pre></figure>\n      <p>这个 SQL 可能走什么索引？会不会回表？会不会 filesort？为什么？</p>\n      <h2>标准答案</h2>\n      <p>这个 SQL 大概率会走联合索引：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">idx_user_time(user_id, create_time)</code></pre></figure>\n      <p>原因是：</p>\n      <ol><li><code>where user_id = 100</code> 命中联合索引最左列。</li><li>在 <code>user_id</code> 固定后，索引内部按 <code>create_time</code> 有序。</li><li><code>order by create_time desc</code> 可以通过反向扫描索引完成。</li><li><code>limit 10</code> 可以让 MySQL 找到前 10 条后尽快停止扫描。</li></ol>\n      <h2>会不会回表</h2>\n      <p>一般不需要回表。</p>\n      <p>因为 InnoDB 二级索引叶子节点中保存：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id + create_time + 主键 id</code></pre></figure>\n      <p>查询字段是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">id, user_id, create_time</code></pre></figure>\n      <p>这些字段都在二级索引中能拿到，因此形成覆盖索引。</p>\n      <p><code>EXPLAIN</code> 的 <code>Extra</code> 中可能出现：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index</code></pre></figure>\n      <p>表示不需要回表。</p>\n      <h2>会不会 filesort</h2>\n      <p>一般不需要 <code>filesort</code>。</p>\n      <p>因为索引顺序是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">user_id -&gt; create_time</code></pre></figure>\n      <p>当 <code>user_id = 100</code> 被等值固定后，满足条件的数据在索引中已经按 <code>create_time</code> 排好序。</p>\n      <p><code>order by create_time desc</code> 可以通过反向扫描索引得到结果。</p>\n      <h2>为什么 limit 10 很关键</h2>\n      <p>如果索引能同时满足过滤和排序：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where user_id = 100\norder by create_time desc\nlimit 10</code></pre></figure>\n      <p>MySQL 可以沿着索引顺序扫描，找到 10 条就停止。</p>\n      <p>这比先查出大量数据再排序，然后取前 10 条要高效很多。</p>\n      <h2>什么时候可能出现 filesort</h2>\n      <h3>1. 索引列顺序不匹配</h3>\n      <p>如果索引是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(create_time, user_id)</code></pre></figure>\n      <p>这个查询不一定理想。</p>\n      <p>因为条件是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where user_id = 100\norder by create_time desc</code></pre></figure>\n      <p><code>user_id</code> 不是最左列，无法高效定位某个用户的数据。</p>\n      <h3>2. 中间跳过索引列</h3>\n      <p>如果索引是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(user_id, status, create_time)</code></pre></figure>\n      <p>查询是：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where user_id = 100\norder by create_time desc</code></pre></figure>\n      <p>因为中间跳过了 <code>status</code>，在 <code>user_id</code> 固定后，索引内部是先按 <code>status</code> 排序，再按 <code>create_time</code> 排序。</p>\n      <p>整体不能保证按 <code>create_time</code> 有序，所以可能需要 <code>filesort</code>。</p>\n      <h3>3. 查询字段不被索引覆盖</h3>\n      <p>如果查询：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">select id, user_id, create_time, amount, address\nfrom orders\nwhere user_id = 100\norder by create_time desc\nlimit 10;</code></pre></figure>\n      <p>如果 <code>amount</code>、<code>address</code> 不在索引中，就需要回表。</p>\n      <p>不过是否需要 <code>filesort</code> 和是否回表是两个问题：</p>\n      <ol><li>回表是为了拿字段。</li><li>filesort 是因为不能利用索引顺序排序。</li></ol>\n      <p>有回表不一定有 filesort，有 filesort 也不一定回表。</p>\n      <h2>面试背诵版</h2>\n      <blockquote>这个 SQL 大概率走 <code>(user_id, create_time)</code> 联合索引。<code>user_id = 100</code> 命中最左列，在 <code>user_id</code> 固定后，索引内部按 <code>create_time</code> 有序，所以 <code>order by create_time desc</code> 可以通过反向扫描索引完成，一般不需要 <code>filesort</code>。查询字段是 <code>id、user_id、create_time</code>，而 InnoDB 二级索引叶子节点包含索引列和主键 id，所以字段都在索引中，形成覆盖索引，一般不需要回表。这里要注意，不是排序字段在索引里就一定没有 filesort，而是排序字段顺序要和联合索引顺序匹配，并且前面的索引列要被等值固定。</blockquote>\n      <hr>\n      <h1>12. 今日复盘重点</h1>\n      <h2>已经掌握得比较好的点</h2>\n      <ol><li>知道 B+ 树相比红黑树、Hash、B 树的核心优势。</li><li>能说明聚簇索引、二级索引、回表、覆盖索引的基本关系。</li><li>能判断 <code>like &#x27;张%&#x27;</code>、<code>like &#x27;%张&#x27;</code>、函数查询对索引的影响。</li><li>能理解字符串列和数字比较时，隐式转换可能导致索引失效。</li><li>能说出 <code>Using index</code> 和 <code>Using index condition</code> 的区别。</li><li>能判断 <code>(user_id, create_time)</code> 对过滤、排序和 <code>limit</code> 的优化价值。</li></ol>\n      <h2>需要继续强化的点</h2>\n      <h3>1. “用到索引”和“用满索引”要分开说</h3>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">index(user_id, status, create_time)\n\nwhere user_id = 1 and create_time &gt; &#x27;2026-01-01&#x27;</code></pre></figure>\n      <p>不能简单说“不走索引”。</p>\n      <p>准确说法：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">可以用到 user_id，但因为跳过 status，create_time 不能用于联合索引连续定位，可能通过 ICP 过滤。</code></pre></figure>\n      <h3>2. 范围查询右侧列的表达要严谨</h3>\n      <p>例如：</p>\n      <figure class=\"code-block\" data-lang=\"sql\"><figcaption><span>sql</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-sql\">where a = 1 and b &gt; 10 and c = 3</code></pre></figure>\n      <p>准确说法：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">a 和 b 可以用于索引定位，c 通常不能继续用于定位，但如果 c 在索引里，可能通过 ICP 做索引层过滤。</code></pre></figure>\n      <h3>3. <code>Using filesort</code> 的含义不能说反</h3>\n      <p><code>Using filesort</code> 不是“使用索引排序”，而是：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">不能直接利用索引顺序完成排序，需要额外排序。</code></pre></figure>\n      <h3>4. <code>ref</code> 不是覆盖索引</h3>\n      <p><code>ref</code> 是 <code>EXPLAIN.type</code> 中的一种访问类型，表示普通索引等值查询，可能匹配多行。</p>\n      <p>覆盖索引看 <code>Extra</code>：</p>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index</code></pre></figure>\n      <h3>5. Hash 索引的核心缺点要说准</h3>\n      <p>Hash 不适合作为通用索引，核心不是事务或崩溃恢复，而是：</p>\n      <ol><li>不支持范围查询。</li><li>不支持排序。</li><li>不支持最左前缀。</li><li>只适合等值查询。</li></ol>\n      <h2>最后速背版</h2>\n      <h3>B+ 树</h3>\n      <blockquote>B+ 树非叶子节点只存 key 和指针，单页能存更多 key，树更矮，磁盘 IO 更少；叶子节点链表相连，适合范围查询和排序。</blockquote>\n      <h3>回表</h3>\n      <blockquote>二级索引叶子节点存索引列和主键值。如果查询字段不在二级索引中，就要拿主键回到聚簇索引查整行，这叫回表。</blockquote>\n      <h3>覆盖索引</h3>\n      <blockquote>查询字段都能从索引中拿到，不需要回表，就叫覆盖索引，<code>Extra</code> 常见 <code>Using index</code>。</blockquote>\n      <h3>最左前缀</h3>\n      <blockquote>联合索引要从最左列开始连续匹配，等值条件可以继续向右，遇到范围查询后，右边列通常不能继续用于索引定位。</blockquote>\n      <h3>ICP</h3>\n      <blockquote>ICP 是索引下推，把能通过索引字段判断的条件下推到存储引擎层，在索引扫描阶段先过滤，减少回表。<code>Extra</code> 常见 <code>Using index condition</code>。</blockquote>\n      <h3>EXPLAIN type</h3>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></pre></figure>\n      <h3>Extra</h3>\n      <figure class=\"code-block\" data-lang=\"text\"><figcaption><span>text</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-text\">Using index           覆盖索引\nUsing index condition ICP 索引下推\nUsing filesort        额外排序\nUsing temporary       使用临时表</code></pre></figure>\n      <h3>隐式转换</h3>\n      <blockquote>字符串列和数字比较时，MySQL 可能把列转成数字，相当于对索引列做函数，导致索引失效。数字列和字符串常量比较时，通常是常量转数字，一般还能走索引。</blockquote>",
  "contentFile": "/content/learning-plans-md/learning-03.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
