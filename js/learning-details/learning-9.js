(function(){
  const detail = {
  "id": 9,
  "content": "<h1>Java 后端实习面试八股精华</h1>\n      <blockquote>整理自小林coding 4 本（Java基础/集合/并发/JVM），按面试高频度筛选</blockquote>\n      <blockquote>难点配前置知识，可独立看懂</blockquote>\n      <blockquote>使用方法：每题先看「一句话答案」→ 再看「展开」→ 最后看「追问」</blockquote>\n      <hr>\n      <h2>目录</h2>\n      <ul><li><a href=\"#一java-基础22-题\">一、Java 基础（22 题）</a></li><li><a href=\"#二java-集合18-题\">二、Java 集合（18 题）</a></li><li><a href=\"#三java-并发35-题含前置\">三、Java 并发（35 题，含前置）</a></li><li><a href=\"#四jvm20-题\">四、JVM（20 题）</a></li><li><a href=\"#五附录高频追问串讲\">五、附录：高频追问串讲</a></li></ul>\n      <hr>\n      <h1>一、Java 基础（22 题）</h1>\n      <h2>1. == 和 equals 的区别</h2>\n      <ul><li><strong>一句话</strong>：<code>==</code> 比较内存地址（基本类型比值），<code>equals</code> 默认也是 <code>==</code>，但 String/Integer 等重写后比内容。</li><li><strong>展开</strong>：基本类型 <code>==</code> 比值；引用类型 <code>==</code> 比引用地址。<code>Object.equals</code> 源码就是 <code>return this == obj</code>，String 重写了 equals 改为逐字符比较。</li><li><strong>追问</strong>：为什么重写 equals 必须重写 hashCode？答：哈希集合（HashMap/HashSet）先比 hashCode 再比 equals，不重写 hashCode 会导致 equals 相等的对象 hashCode 不同 → 放进 HashMap 找不到。</li></ul>\n      <h2>2. String、StringBuilder、StringBuffer 的区别</h2>\n      <ul><li><strong>String</strong>：不可变（final char[]），每次拼接生成新对象。</li><li><strong>StringBuilder</strong>：可变，<strong>线程不安全</strong>，速度最快，单线程用它。</li><li><strong>StringBuffer</strong>：可变，<strong>线程安全</strong>（方法加 synchronized），多线程用它。</li><li><strong>追问</strong>：String 为什么不可变？答：字段是 final char[]，类本身也是 final。好处：线程安全、可作 HashMap 的 key（hashCode 不变）、字符串常量池可复用。</li></ul>\n      <h2>3. String s = &quot;abc&quot; 和 String s = new String(&quot;abc&quot;) 区别</h2>\n      <ul><li>前者：先查常量池，有就复用，没有就在常量池建一个，<strong>0 或 1 个对象</strong>。</li><li>后者：在堆上新建一个对象，常量池如果没有也建一个，<strong>1 或 2 个对象</strong>。</li><li><strong>追问</strong>：<code>String.intern()</code> 干啥？答：返回常量池里的引用，没有则放进去。</li></ul>\n      <h2>4. 面向对象三大特性</h2>\n      <ul><li><strong>封装</strong>：隐藏内部细节，通过 public 方法访问。</li><li><strong>继承</strong>：子类复用父类，Java 单继承。</li><li><strong>多态</strong>：父类引用指向子类对象，运行时动态绑定方法。</li></ul>\n      <h2>5. 抽象类 vs 接口</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>抽象类</th><th>接口</th></tr></thead><tbody><tr><td>关键字</td><td>abstract class</td><td>interface</td></tr><tr><td>字段</td><td>任意</td><td>默认 public static final</td></tr><tr><td>方法</td><td>可有实现</td><td>JDK 8+ 可有 default 方法</td></tr><tr><td>继承</td><td>单继承</td><td>多实现</td></tr><tr><td>定位</td><td>&quot;is-a&quot; 关系</td><td>&quot;has-a 能力&quot;关系</td></tr></tbody></table></div>\n      <h2>6. 重载 vs 重写</h2>\n      <ul><li><strong>重载（Overload）</strong>：同类中，方法名相同、参数列表不同。<strong>编译期</strong>确定。</li><li><strong>重写（Override）</strong>：子类覆盖父类方法，签名必须相同。<strong>运行期</strong>多态。</li></ul>\n      <h2>7. final、finally、finalize 区别</h2>\n      <ul><li><strong>final</strong>：修饰类不可继承、修饰方法不可重写、修饰变量不可改。</li><li><strong>finally</strong>：try-catch 后必定执行（除非 JVM 退出）。</li><li><strong>finalize</strong>：Object 方法，GC 前调用，<strong>已废弃，不要用</strong>。</li></ul>\n      <h2>8. static 关键字</h2>\n      <ul><li>修饰变量 → 类变量，所有实例共享，存在方法区。</li><li>修饰方法 → 静态方法，不能用 this，不能直接调用非静态方法。</li><li>修饰代码块 → 类加载时执行一次。</li><li>修饰内部类 → 静态内部类，不依赖外部类实例。</li></ul>\n      <h2>9. 深拷贝 vs 浅拷贝</h2>\n      <ul><li><strong>浅拷贝</strong>：只复制引用，新旧对象指向同一内存（<code>Object.clone</code> 默认行为）。</li><li><strong>深拷贝</strong>：连同引用对象一起复制，递归克隆所有引用。</li><li>实现方式：重写 clone() 递归复制 / 序列化 + 反序列化。</li></ul>\n      <h2>10. 异常体系</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>Throwable\n├── Error        （JVM 错误，OOM、StackOverflow，不应捕获）\n└── Exception\n    ├── RuntimeException （非受检异常，NPE、ClassCast、ArrayIndexOut）\n    └── 其他              （受检异常，IOException、SQLException，必须 throws 或 try-catch）</code></pre></figure>\n      <ul><li><strong>追问</strong>：try-catch-finally 中 return 顺序？答：try 的 return 值先暂存，执行 finally，最后返回暂存值。<strong>finally 里 return 会覆盖 try 的 return</strong>（坑！面试常考）。</li></ul>\n      <h2>11. throw 和 throws 区别</h2>\n      <ul><li><code>throw</code>：方法内主动抛异常对象。</li><li><code>throws</code>：方法签名上声明可能抛出的异常。</li></ul>\n      <h2>12. Java 反射</h2>\n      <ul><li><strong>是什么</strong>：运行时获取类信息、调用方法、访问字段，绕过编译期限制。</li><li><strong>三种获取 Class 方式</strong>：<code>类名.class</code>、<code>对象.getClass()</code>、<code>Class.forName(&quot;全限定名&quot;)</code>。</li><li><strong>用途</strong>：Spring IOC、MyBatis 映射、JSON 序列化、动态代理。</li><li><strong>缺点</strong>：性能差（JIT 难优化）、破坏封装、可访问 private。</li></ul>\n      <h2>13. 泛型与类型擦除</h2>\n      <ul><li>泛型只在编译期检查，<strong>运行时擦除为 Object</strong>（或边界类型）。</li><li>所以 <code>List&lt;Integer&gt;</code> 和 <code>List&lt;String&gt;</code> 的 <code>getClass()</code> 相同。</li><li><strong>追问</strong>：能 <code>new T()</code> 吗？不能，运行时 T 已擦除。</li></ul>\n      <h2>14. 自动装箱 / 拆箱</h2>\n      <ul><li>装箱：基本类型 → 包装类（<code>Integer.valueOf</code>）。</li><li>拆箱：包装类 → 基本类型（<code>intValue</code>）。</li><li><strong>整型缓存池</strong>：<code>Integer.valueOf</code> 对 <code>-128~127</code> 范围使用缓存。</li></ul>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">Integer a = 127, b = 127;   // true，缓存\nInteger c = 128, d = 128;   // false，new 出来的</code></pre></figure>\n      <h2>15. Object 类有哪些方法</h2>\n      <p><code>equals</code> / <code>hashCode</code> / <code>toString</code> / <code>clone</code> / <code>getClass</code> / <code>notify</code> / <code>notifyAll</code> / <code>wait</code> / <code>finalize</code></p>\n      <h2>16. hashCode() 和 equals() 的关系</h2>\n      <ul><li><code>equals</code> 相等 → <code>hashCode</code> 必须相等。</li><li><code>hashCode</code> 相等 → <code>equals</code> <strong>不一定</strong>相等（哈希冲突）。</li></ul>\n      <h2>17. Java 中 IO 模型（BIO / NIO / AIO）</h2>\n      <ul><li><strong>BIO</strong>：同步阻塞，每个连接一个线程。</li><li><strong>NIO</strong>：同步非阻塞，多路复用（Selector），一个线程管多个连接（Netty 基础）。</li><li><strong>AIO</strong>：异步非阻塞，回调通知。</li><li><strong>追问</strong>：和 Redis epoll 啥关系？Redis 单线程 + epoll = Java NIO 的原理。</li></ul>\n      <h2>18. 接口和抽象类如何选择</h2>\n      <ul><li>想定义&quot;能力规范&quot;（如 Comparable）→ 接口。</li><li>想定义&quot;模板&quot;（部分实现 + 子类补全）→ 抽象类。</li></ul>\n      <h2>19. Java 修饰符可见性</h2>\n      <div class=\"md-table\"><table><thead><tr><th>修饰符</th><th>类内</th><th>同包</th><th>子类</th><th>其他</th></tr></thead><tbody><tr><td>private</td><td>✅</td><td>❌</td><td>❌</td><td>❌</td></tr><tr><td>默认</td><td>✅</td><td>✅</td><td>❌</td><td>❌</td></tr><tr><td>protected</td><td>✅</td><td>✅</td><td>✅</td><td>❌</td></tr><tr><td>public</td><td>✅</td><td>✅</td><td>✅</td><td>✅</td></tr></tbody></table></div>\n      <h2>20. JDK / JRE / JVM 区别</h2>\n      <ul><li><strong>JVM</strong>：虚拟机，执行字节码。</li><li><strong>JRE</strong>：JVM + 运行时类库。</li><li><strong>JDK</strong>：JRE + 编译器 + 调试工具。</li></ul>\n      <h2>21. 值传递 vs 引用传递</h2>\n      <ul><li><strong>Java 只有值传递</strong>。</li><li>基本类型传值的拷贝；引用类型传引用地址的拷贝（所以方法内能改对象属性，但不能让原引用指向新对象）。</li></ul>\n      <h2>22. JDK 8 新特性</h2>\n      <p>Lambda 表达式、Stream API、函数式接口、Optional、新日期 API（LocalDateTime）、接口 default 方法、HashMap 链表转红黑树。</p>\n      <hr>\n      <h1>二、Java 集合（18 题）</h1>\n      <h2>1. Java 集合体系</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>Collection\n├── List   （有序、可重复）  ArrayList / LinkedList / Vector\n├── Set    （无序、不重复）  HashSet / LinkedHashSet / TreeSet\n└── Queue  （队列）         ArrayDeque / LinkedList / PriorityQueue\n\nMap        （键值对）\n├── HashMap / LinkedHashMap / TreeMap / Hashtable / ConcurrentHashMap</code></pre></figure>\n      <h2>2. ArrayList vs LinkedList ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>ArrayList</th><th>LinkedList</th></tr></thead><tbody><tr><td>底层</td><td>动态数组</td><td>双向链表</td></tr><tr><td>随机访问</td><td>O(1)</td><td>O(n)</td></tr><tr><td>头尾插入</td><td>头插 O(n)/尾插 O(1)</td><td>O(1)</td></tr><tr><td>中间插入</td><td>O(n)</td><td>O(n)（查找慢）</td></tr><tr><td>内存</td><td>紧凑，可能浪费</td><td>节点开销大</td></tr></tbody></table></div>\n      <p><strong>结论</strong>：90% 场景用 ArrayList，LinkedList 几乎不用。</p>\n      <h2>3. ArrayList 扩容机制 ⭐</h2>\n      <ul><li>默认容量 <strong>10</strong>（首次 add 时才创建数组）。</li><li>扩容到原来的 <strong>1.5 倍</strong>（<code>oldCap + (oldCap &gt;&gt; 1)</code>）。</li><li>调用 <code>Arrays.copyOf</code> 复制到新数组。</li></ul>\n      <h2>4. HashMap 底层结构 ⭐⭐⭐（必考）</h2>\n      <ul><li><strong>JDK 1.7</strong>：数组 + 链表，头插法（多线程扩容会成环）。</li><li><strong>JDK 1.8</strong>：数组 + 链表 + 红黑树，尾插法。</li><li>链表长度 <strong>≥ 8</strong> 且数组长度 <strong>≥ 64</strong> → 转红黑树。</li><li>红黑树节点数 <strong>≤ 6</strong> → 退化为链表。</li></ul>\n      <h2>5. HashMap 为什么容量是 2 的 n 次幂 ⭐</h2>\n      <ul><li>计算下标：<code>(n - 1) &amp; hash</code>，n 是 2 的幂时 <code>n-1</code> 全为 1，<strong>位运算代替取模</strong>，效率高。</li><li>扩容时元素位置要么在原位、要么在 <code>原位 + 旧容量</code>，迁移高效。</li></ul>\n      <h2>6. HashMap put 流程</h2>\n      <ol><li>计算 hash：<code>(h = key.hashCode()) ^ (h &gt;&gt;&gt; 16)</code>（高低位异或，减少碰撞）。</li><li>数组为空 → resize 初始化。</li><li>算下标 <code>(n-1) &amp; hash</code>，桶为空 → 直接放。</li><li>桶非空 → 比较 key，相同则覆盖；不同则尾插链表/红黑树。</li><li>链表长 ≥ 8 → 树化。</li><li>元素数 &gt; <strong>负载因子 0.75 × 容量</strong> → 扩容 2 倍。</li></ol>\n      <h2>7. HashMap 为什么线程不安全 ⭐</h2>\n      <ul><li>JDK 1.7：并发扩容 + 头插法 → <strong>链表成环</strong>，get 时死循环。</li><li>JDK 1.8：尾插法解决了环，但仍有<strong>数据覆盖</strong>问题（两个线程同时 put 到同一空桶）。</li></ul>\n      <h2>8. ConcurrentHashMap 原理 ⭐⭐⭐（必考）</h2>\n      <ul><li><strong>JDK 1.7</strong>：分段锁 Segment（继承 ReentrantLock），默认 16 段，并发度 = 段数。</li><li><strong>JDK 1.8</strong>：抛弃 Segment，用 <strong>CAS + synchronized</strong>，锁粒度细到链表头结点。</li><li>put：CAS 插入空桶；非空则 synchronized 锁住头节点。</li><li>size：累加每个 baseCount + CounterCell。</li></ul>\n      <h2>9. HashMap vs Hashtable vs ConcurrentHashMap</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>HashMap</th><th>Hashtable</th><th>ConcurrentHashMap</th></tr></thead><tbody><tr><td>线程安全</td><td>❌</td><td>✅（全表 synchronized）</td><td>✅（分段/CAS）</td></tr><tr><td>性能</td><td>高</td><td>极差</td><td>高</td></tr><tr><td>null key/value</td><td>允许</td><td>不允许</td><td>不允许</td></tr><tr><td>推荐</td><td>单线程</td><td>不用</td><td>多线程</td></tr></tbody></table></div>\n      <h2>10. HashSet 底层</h2>\n      <p>内部就是一个 HashMap，value 是固定的 <code>PRESENT</code> 对象。</p>\n      <h2>11. LinkedHashMap</h2>\n      <p>HashMap + 双向链表，维持插入顺序或访问顺序（可实现 <strong>LRU 缓存</strong>）。</p>\n      <h2>12. TreeMap</h2>\n      <p>基于<strong>红黑树</strong>，key 按自然顺序或 Comparator 排序，操作 O(log n)。</p>\n      <h2>13. 红黑树性质（简记）</h2>\n      <ul><li>节点非红即黑，根节点黑。</li><li>红节点的子必须是黑。</li><li>任一节点到叶子的所有路径黑节点数相同。</li><li><strong>近似平衡</strong>（最长路径 ≤ 2 × 最短路径）。</li></ul>\n      <h2>14. CopyOnWriteArrayList</h2>\n      <ul><li>写时复制：写操作复制整个数组、加锁修改、替换原数组。</li><li>读无锁，<strong>读多写少</strong>场景（如订阅者列表）。</li><li>缺点：写代价高、数据短暂不一致。</li></ul>\n      <h2>15. Fail-Fast 机制</h2>\n      <ul><li>遍历集合时被修改 → 抛 <code>ConcurrentModificationException</code>。</li><li>原理：iterator 持有 <code>expectedModCount</code>，与集合的 <code>modCount</code> 不等就抛错。</li><li>解决：用 <code>Iterator.remove()</code> 或 <code>CopyOnWriteArrayList</code>。</li></ul>\n      <h2>16. ArrayList 和 Vector</h2>\n      <p>Vector 老古董，所有方法 synchronized，性能差，已被 ArrayList + Collections.synchronizedList 替代。</p>\n      <h2>17. Comparable 和 Comparator</h2>\n      <ul><li><code>Comparable</code>：内部排序，<code>compareTo</code>，类实现接口。</li><li><code>Comparator</code>：外部排序，<code>compare</code>，作参数传给排序方法。</li></ul>\n      <h2>18. 集合工具类</h2>\n      <ul><li><code>Collections</code>：<code>sort</code> / <code>reverse</code> / <code>synchronizedXxx</code> / <code>unmodifiableXxx</code></li><li><code>Arrays</code>：<code>asList</code> / <code>sort</code> / <code>copyOf</code></li></ul>\n      <hr>\n      <h1>三、Java 并发（35 题，含前置）</h1>\n      <h2>【前置 A】进程 vs 线程</h2>\n      <ul><li><strong>进程</strong>：操作系统资源分配的最小单位，独立内存。</li><li><strong>线程</strong>：CPU 调度的最小单位，<strong>共享进程的内存</strong>，所以才有并发问题。</li></ul>\n      <h2>【前置 B】并发三大特性 ⭐⭐⭐（理解这个才能看懂后面）</h2>\n      <ol><li><strong>原子性</strong>：操作不可分割（i++ 不是原子，分 3 步：读、加、写）。</li><li><strong>可见性</strong>：一个线程改了共享变量，其他线程能立刻看到。</li><li><strong>有序性</strong>：代码执行顺序不被重排（CPU/编译器会重排指令优化性能）。</li></ol>\n      <blockquote>并发的所有问题都是这三个出了问题，<strong>所有解决方案（synchronized/volatile/Lock）都是在解决这三个</strong>。</blockquote>\n      <h2>【前置 C】Java 内存模型 JMM ⭐⭐⭐</h2>\n      <ul><li>每个线程有自己的<strong>工作内存</strong>，共享变量在<strong>主内存</strong>。</li><li>线程改变量：主内存 → 工作内存（read/load）→ 修改 → 工作内存 → 主内存（store/write）。</li><li><strong>可见性问题</strong>就来源于此：A 线程改了工作内存还没刷回主内存，B 线程读不到。</li></ul>\n      <h2>【前置 D】happens-before 原则</h2>\n      <p>JMM 规定的 8 条偏序关系，保证前一个操作的结果对后续操作可见。关键几条：</p>\n      <ul><li>程序顺序规则：一个线程内，前面的操作 happens-before 后面的。</li><li>锁规则：unlock happens-before 后续的 lock。</li><li>volatile 规则：写 happens-before 后续的读。</li><li>传递性：A→B, B→C 则 A→C。</li></ul>\n      <hr>\n      <h2>1. 创建线程的方式</h2>\n      <ol><li>继承 Thread，重写 run()。</li><li>实现 Runnable，传给 Thread。</li><li>实现 Callable，配合 FutureTask（有返回值、可抛异常）。</li><li>线程池（推荐）。</li></ol>\n      <h2>2. Runnable vs Callable</h2>\n      <ul><li>Runnable：<code>run()</code> 无返回值，不抛异常。</li><li>Callable：<code>call()</code> 有返回值，抛异常，配合 Future 拿结果。</li></ul>\n      <h2>3. 线程的 6 个状态</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>NEW          → 创建未启动\nRUNNABLE     → 运行中或就绪（Java 不区分）\nBLOCKED      → 等待 synchronized 锁\nWAITING      → 无限等待（wait/join 无超时）\nTIMED_WAITING→ 限时等待（sleep/wait 有超时）\nTERMINATED   → 结束</code></pre></figure>\n      <h2>4. sleep vs wait ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>sleep</th><th>wait</th></tr></thead><tbody><tr><td>所属</td><td>Thread 静态方法</td><td>Object 方法</td></tr><tr><td>释放锁</td><td><strong>不释放</strong></td><td><strong>释放</strong></td></tr><tr><td>调用位置</td><td>任何地方</td><td>必须在 synchronized 内</td></tr><tr><td>唤醒</td><td>时间到自动</td><td>必须 notify/notifyAll</td></tr></tbody></table></div>\n      <h2>5. wait/notify 为什么定义在 Object</h2>\n      <p>因为锁是任意对象，wait/notify 是基于锁对象的等待队列，所以必须每个对象都有。</p>\n      <h2>6. start() vs run()</h2>\n      <ul><li><code>run()</code>：当作普通方法调用，<strong>单线程</strong>执行。</li><li><code>start()</code>：JVM 创建新线程，再由新线程调用 run()。</li></ul>\n      <h2>7. synchronized 原理 ⭐⭐⭐（必考）</h2>\n      <ul><li><strong>修饰对象</strong>：</li><li>实例方法 → 锁 this</li><li>静态方法 → 锁 Class 对象</li><li>代码块 → 锁指定对象</li><li><strong>底层</strong>：基于 <strong>Monitor</strong>（监视器锁），每个对象头有指向 Monitor 的指针。</li><li>同步方法：方法标记 <code>ACC_SYNCHRONIZED</code>。</li><li>同步代码块：编译为 <code>monitorenter</code> / <code>monitorexit</code> 指令。</li></ul>\n      <h2>8. synchronized 锁升级 ⭐⭐⭐</h2>\n      <p>JDK 6 优化，4 种状态依次升级（<strong>不可降级</strong>）：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>无锁 → 偏向锁 → 轻量级锁 → 重量级锁</code></pre></figure>\n      <ul><li><strong>偏向锁</strong>：只有一个线程访问，对象头记录线程 ID，无需 CAS。</li><li><strong>轻量级锁</strong>：少量竞争，CAS 自旋。</li><li><strong>重量级锁</strong>：大量竞争，进入 Monitor 阻塞队列（操作系统 mutex，开销大）。</li></ul>\n      <h2>9. volatile 原理 ⭐⭐⭐（必考）</h2>\n      <ul><li><strong>保证可见性</strong>：写操作立刻刷主内存，并使其他线程缓存失效（基于 CPU 缓存一致性 MESI 协议）。</li><li><strong>禁止指令重排</strong>：通过<strong>内存屏障</strong>（Memory Barrier）实现。</li><li><strong>不保证原子性</strong>：<code>volatile int i; i++</code> 仍线程不安全。</li></ul>\n      <p><strong>典型应用</strong>：单例模式 DCL（双重检查锁）</p>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">private volatile static Singleton instance;  // 防止 new 半初始化对象被另一线程拿到</code></pre></figure>\n      <h2>10. synchronized vs volatile</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>synchronized</th><th>volatile</th></tr></thead><tbody><tr><td>原子性</td><td>✅</td><td>❌</td></tr><tr><td>可见性</td><td>✅</td><td>✅</td></tr><tr><td>有序性</td><td>✅</td><td>✅</td></tr><tr><td>阻塞</td><td>阻塞</td><td>不阻塞</td></tr><tr><td>修饰</td><td>方法/代码块</td><td>变量</td></tr></tbody></table></div>\n      <h2>11. synchronized vs ReentrantLock ⭐</h2>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>synchronized</th><th>ReentrantLock</th></tr></thead><tbody><tr><td>实现</td><td>JVM 关键字</td><td>API（AQS）</td></tr><tr><td>释放锁</td><td>自动</td><td><strong>必须手动 unlock</strong>（finally 中）</td></tr><tr><td>可中断</td><td>❌</td><td>✅ <code>lockInterruptibly</code></td></tr><tr><td>公平锁</td><td>❌</td><td>✅（构造参数）</td></tr><tr><td>条件变量</td><td>一个（wait/notify）</td><td>多个（newCondition）</td></tr><tr><td>性能</td><td>JDK 6 后优化，差不多</td><td>灵活但需手写</td></tr></tbody></table></div>\n      <h2>12. ReentrantLock 是怎么实现的</h2>\n      <p>基于 <strong>AQS</strong>（AbstractQueuedSynchronizer）。</p>\n      <h2>13. AQS 原理 ⭐⭐⭐（必考）</h2>\n      <ul><li>全名：<strong>抽象队列同步器</strong>。</li><li>核心：<strong>volatile int state</strong> + <strong>CLH 双向等待队列</strong>。</li><li>加锁：CAS 改 state，失败则入队阻塞（park）。</li><li>释放：改 state，唤醒队首（unpark）。</li><li><strong>谁用了</strong>：ReentrantLock、CountDownLatch、Semaphore、ReentrantReadWriteLock。</li></ul>\n      <h2>14. CAS 原理 ⭐⭐⭐</h2>\n      <ul><li>Compare And Swap：比较并交换。</li><li>三个值：<code>内存值 V</code>、<code>期望值 E</code>、<code>新值 N</code>。如果 V == E 则改为 N，否则什么都不做。</li><li>底层：CPU 的 <code>cmpxchg</code> 指令，<strong>硬件级原子操作</strong>。</li><li>Java 中：<code>Unsafe.compareAndSwapInt</code>，<code>AtomicInteger</code> 等就用它。</li></ul>\n      <h2>15. CAS 的 ABA 问题 ⭐</h2>\n      <ul><li>线程 1 读到 A，准备改成 C；线程 2 把 A→B→A。</li><li>线程 1 CAS 成功，但中间状态被忽略了。</li><li><strong>解决</strong>：<code>AtomicStampedReference</code>（加版本号）。</li></ul>\n      <h2>16. 原子类 Atomic</h2>\n      <p>基于 CAS 实现，无锁线程安全。常用：<code>AtomicInteger</code> / <code>AtomicLong</code> / <code>AtomicReference</code> / <code>LongAdder</code>（高并发更优，分段累加）。</p>\n      <h2>17. 线程池七大参数 ⭐⭐⭐（必考必背）</h2>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">new ThreadPoolExecutor(\n    int corePoolSize,           // 核心线程数\n    int maximumPoolSize,        // 最大线程数\n    long keepAliveTime,         // 非核心线程空闲存活时间\n    TimeUnit unit,              // 时间单位\n    BlockingQueue workQueue,    // 任务队列\n    ThreadFactory threadFactory,// 线程工厂（命名用）\n    RejectedExecutionHandler handler  // 拒绝策略\n);</code></pre></figure>\n      <h2>18. 线程池工作流程 ⭐⭐⭐</h2>\n      <ol><li>任务来 → 核心线程没满，<strong>创建核心线程</strong>。</li><li>核心满 → 进<strong>任务队列</strong>。</li><li>队列满 → 创建非核心线程，直到 maxPoolSize。</li><li>全满 → 执行<strong>拒绝策略</strong>。</li></ol>\n      <h2>19. 四种拒绝策略</h2>\n      <ul><li><strong>AbortPolicy</strong>（默认）：抛 RejectedExecutionException。</li><li><strong>CallerRunsPolicy</strong>：调用者线程自己执行。</li><li><strong>DiscardPolicy</strong>：默默丢弃。</li><li><strong>DiscardOldestPolicy</strong>：丢弃队列最老的，把新任务加入。</li></ul>\n      <h2>20. 常见线程池（Executors）</h2>\n      <ul><li><code>newFixedThreadPool</code>：固定大小，<strong>队列无界</strong>（OOM 风险）。</li><li><code>newCachedThreadPool</code>：核心 0，<strong>最大 Integer.MAX_VALUE</strong>（创建过多线程 OOM）。</li><li><code>newSingleThreadExecutor</code>：单线程，队列无界。</li><li><code>newScheduledThreadPool</code>：定时任务。</li></ul>\n      <p><strong>阿里规范</strong>：禁止用 Executors 创建，必须 <code>new ThreadPoolExecutor</code> 显式指定参数。</p>\n      <h2>21. 线程池如何合理设置大小</h2>\n      <ul><li>CPU 密集型：<code>N + 1</code>（N 为核数）。</li><li>IO 密集型：<code>2N</code> 或更多（IO 等待时让别的线程跑）。</li></ul>\n      <h2>22. ThreadLocal 原理 ⭐⭐⭐（必考，你简历项目2用了！）</h2>\n      <ul><li><strong>作用</strong>：线程私有变量，每个线程独立副本，互不影响。</li><li><strong>典型用途</strong>：保存当前登录用户、数据库连接、SimpleDateFormat（线程不安全）。</li><li><strong>底层结构</strong>：</li></ul>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>  Thread 对象内部 → ThreadLocalMap (key: ThreadLocal, value: 值)</code></pre></figure>\n      <p>注意是 Thread 持有 Map，不是 ThreadLocal 持有！</p>\n      <h2>23. ThreadLocal 内存泄漏 ⭐⭐⭐</h2>\n      <ul><li>ThreadLocalMap 的 <strong>key 是弱引用</strong>（GC 时被回收），<strong>value 是强引用</strong>。</li><li>如果 ThreadLocal 没了，key=null，但 value 还在，<strong>线程不结束就一直占内存</strong>。</li><li><strong>解决</strong>：用完手动调 <code>threadLocal.remove()</code>（特别是线程池场景，线程复用）。</li></ul>\n      <h2>24. ThreadLocal 为什么用弱引用</h2>\n      <p>为了让 ThreadLocal 对象本身能被 GC 回收，避免一定泄漏（但 value 还需手动 remove）。</p>\n      <h2>25. InheritableThreadLocal</h2>\n      <p>父线程可以传值给子线程（但线程池场景不行，因为线程是复用的不是新建的 → 用 <code>TransmittableThreadLocal</code>）。</p>\n      <h2>26. 死锁四大必要条件</h2>\n      <ol><li><strong>互斥</strong>：资源同一时刻只能被一个线程占用。</li><li><strong>占有并等待</strong>：持有资源同时请求新资源。</li><li><strong>不可剥夺</strong>：资源不能被强制释放。</li><li><strong>循环等待</strong>：线程之间形成环路等待。</li></ol>\n      <p><strong>破坏任一条件即可避免</strong>，最常用：<strong>按固定顺序加锁</strong>。</p>\n      <h2>27. 如何排查死锁</h2>\n      <ul><li><code>jstack</code> 命令打印线程栈，能看到 &quot;Found one Java-level deadlock&quot;。</li><li><code>jconsole</code> / <code>jvisualvm</code> 图形化检测。</li><li>Arthas 在线诊断。</li></ul>\n      <h2>28. 乐观锁 vs 悲观锁</h2>\n      <ul><li><strong>乐观</strong>：假设没冲突，提交时检查（CAS、version 字段）。</li><li><strong>悲观</strong>：假设一定冲突，先加锁（synchronized、for update）。</li></ul>\n      <h2>29. 公平锁 vs 非公平锁</h2>\n      <ul><li><strong>公平</strong>：按申请顺序获取（队列 FIFO），无饥饿但吞吐低。</li><li><strong>非公平</strong>：插队，吞吐高但可能饥饿，<strong>synchronized 和 ReentrantLock 默认都是非公平</strong>。</li></ul>\n      <h2>30. 可重入锁</h2>\n      <p>同一线程可多次获取同一把锁，不会死锁。synchronized 和 ReentrantLock 都可重入。</p>\n      <h2>31. 读写锁 ReentrantReadWriteLock</h2>\n      <ul><li>读读共享、读写互斥、写写互斥。</li><li>适合<strong>读多写少</strong>场景。</li></ul>\n      <h2>32. 阻塞队列 BlockingQueue</h2>\n      <p>线程池底层用的。常见实现：</p>\n      <ul><li><code>ArrayBlockingQueue</code>：数组、有界。</li><li><code>LinkedBlockingQueue</code>：链表、默认无界（线程池 OOM 元凶）。</li><li><code>SynchronousQueue</code>：不存元素，直接传递（CachedThreadPool 用它）。</li><li><code>DelayQueue</code>：延迟队列。</li><li><code>PriorityBlockingQueue</code>：优先级队列。</li></ul>\n      <h2>33. CountDownLatch / CyclicBarrier / Semaphore</h2>\n      <ul><li><strong>CountDownLatch</strong>：倒计数，N 个线程完成后主线程才继续（用一次）。</li><li><strong>CyclicBarrier</strong>：让一组线程互相等待到达屏障再一起走（可循环）。</li><li><strong>Semaphore</strong>：信号量，控制并发数（限流）。</li></ul>\n      <h2>34. ForkJoinPool</h2>\n      <p>分治思想，<strong>工作窃取</strong>（空闲线程偷其他线程队列里的任务），并行流 <code>parallelStream</code> 底层用它。</p>\n      <h2>35. 线程间通信方式</h2>\n      <ul><li>volatile 共享变量</li><li>wait/notify</li><li>Lock + Condition</li><li>BlockingQueue</li><li>CountDownLatch / CyclicBarrier</li></ul>\n      <hr>\n      <h1>四、JVM（20 题）</h1>\n      <h2>1. JVM 内存结构 ⭐⭐⭐（必考必背）</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>线程私有：\n├── 程序计数器     当前线程执行字节码的行号指示器（唯一不会 OOM）\n├── 虚拟机栈       存方法的栈帧（局部变量表、操作数栈），StackOverflow / OOM\n└── 本地方法栈     执行 native 方法\n\n线程共享：\n├── 堆             对象实例，GC 主战场\n└── 方法区/元空间   类信息、常量、静态变量（JDK 8 后用元空间，在直接内存）</code></pre></figure>\n      <h2>2. 堆的结构</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>新生代（1/3）\n├── Eden（8）\n├── Survivor 0（1）\n└── Survivor 1（1）\n\n老年代（2/3）</code></pre></figure>\n      <h2>3. 对象创建过程</h2>\n      <ol><li>类加载检查（没加载 → 加载）</li><li>分配内存（指针碰撞 / 空闲列表）</li><li>初始化零值</li><li>设置对象头（hash、GC 年龄、锁状态、类型指针）</li><li>执行 <code>&lt;init&gt;</code> 方法</li></ol>\n      <h2>4. 对象内存布局</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>对象头（Header）→ Mark Word + 类型指针\n实例数据\n对齐填充（8 字节倍数）</code></pre></figure>\n      <h2>5. 如何判断对象死亡 ⭐</h2>\n      <ul><li><strong>引用计数法</strong>：计数为 0 即死亡。<strong>缺点：循环引用无法回收</strong>。</li><li><strong>可达性分析</strong>（JVM 实际用的）：从 <strong>GC Roots</strong> 出发，不可达即死亡。</li></ul>\n      <h2>6. GC Roots 有哪些</h2>\n      <ul><li>虚拟机栈中引用的对象</li><li>静态变量引用的对象</li><li>常量引用的对象</li><li>本地方法栈引用的对象</li></ul>\n      <h2>7. 四种引用 ⭐</h2>\n      <ul><li><strong>强引用</strong>：<code>Object o = new Object()</code>，永远不回收。</li><li><strong>软引用</strong> SoftReference：内存不足时回收（适合缓存）。</li><li><strong>弱引用</strong> WeakReference：下次 GC 就回收（ThreadLocalMap 的 key）。</li><li><strong>虚引用</strong> PhantomReference：完全形同虚设，仅用于跟踪回收。</li></ul>\n      <h2>8. 垃圾回收算法</h2>\n      <ul><li><strong>标记-清除</strong>：碎片多。</li><li><strong>复制</strong>：内存减半，无碎片，<strong>新生代用</strong>（Eden + Survivor 复制存活）。</li><li><strong>标记-整理</strong>：清除后整理，<strong>老年代用</strong>。</li><li><strong>分代收集</strong>：综合上述，按区域选算法。</li></ul>\n      <h2>9. 分代收集思想</h2>\n      <ul><li><strong>新生代</strong>：朝生夕死，对象多用复制算法。</li><li><strong>老年代</strong>：存活率高，用标记-整理 / 标记-清除。</li></ul>\n      <h2>10. Minor GC / Major GC / Full GC</h2>\n      <ul><li><strong>Minor GC（Young GC）</strong>：新生代，频繁、快。</li><li><strong>Major GC（Old GC）</strong>：老年代。</li><li><strong>Full GC</strong>：整个堆 + 方法区，<strong>最慢，要避免</strong>。</li></ul>\n      <h2>11. 对象什么时候进老年代</h2>\n      <ul><li>Survivor 中年龄 <strong>≥ 15</strong>（每熬过一次 GC 年龄 +1）。</li><li><strong>大对象</strong>直接进老年代（<code>-XX:PretenureSizeThreshold</code>）。</li><li><strong>动态年龄判断</strong>：Survivor 中同年龄对象 &gt; 一半，所有 ≥ 该年龄的进老年代。</li><li>Survivor 放不下，触发<strong>担保机制</strong>进老年代。</li></ul>\n      <h2>12. 垃圾收集器 ⭐</h2>\n      <ul><li><strong>Serial</strong>：单线程，老古董。</li><li><strong>ParNew</strong>：Serial 多线程版（新生代）。</li><li><strong>Parallel Scavenge</strong>：吞吐量优先（JDK 8 默认新生代）。</li><li><strong>CMS</strong>：老年代，并发标记清除，<strong>追求低停顿</strong>，碎片多。</li><li><strong>G1</strong>（JDK 9 默认）：分区 Region，可预测停顿，全代通吃。</li><li><strong>ZGC</strong>：超低停顿（&lt;10ms），适合大堆。</li></ul>\n      <h2>13. CMS 四阶段</h2>\n      <ol><li>初始标记（STW，标 GC Roots 直接引用）</li><li>并发标记（与用户线程并发）</li><li>重新标记（STW，处理并发期间变化）</li><li>并发清除</li></ol>\n      <h2>14. G1 特点</h2>\n      <ul><li>不再分新老年代物理区域，整堆切成多个 <strong>Region</strong>（每个可为 Eden/Survivor/Old/Humongous）。</li><li>优先回收价值最大（垃圾最多）的 Region → <strong>Garbage First</strong>。</li><li>可设置最大停顿时间目标。</li></ul>\n      <h2>15. 类加载过程 ⭐</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>加载 → 验证 → 准备 → 解析 → 初始化</code></pre></figure>\n      <ul><li><strong>加载</strong>：读取 .class 字节码，生成 Class 对象。</li><li><strong>验证</strong>：文件格式、元数据、字节码、符号引用。</li><li><strong>准备</strong>：给静态变量分配内存 + <strong>零值</strong>（不是初始值）。</li><li><strong>解析</strong>：符号引用 → 直接引用。</li><li><strong>初始化</strong>：执行 <code>&lt;clinit&gt;</code>（静态变量赋值、静态代码块）。</li></ul>\n      <h2>16. 类加载器 ⭐</h2>\n      <ul><li><strong>BootstrapClassLoader</strong>（启动类加载器）：加载 <code>JAVA_HOME/lib</code>，C++ 写的，获取不到引用。</li><li><strong>ExtensionClassLoader</strong>（扩展类加载器）：加载 <code>JAVA_HOME/lib/ext</code>。</li><li><strong>ApplicationClassLoader</strong>（应用类加载器）：加载 classpath，默认加载器。</li><li><strong>自定义类加载器</strong>：继承 ClassLoader。</li></ul>\n      <h2>17. 双亲委派模型 ⭐⭐⭐</h2>\n      <ul><li>子加载器收到请求，<strong>先委托父加载器</strong>加载，父加载不了再自己加载。</li><li><strong>好处</strong>：</li></ul>\n      <ol><li>避免重复加载。</li><li>安全（核心类如 java.lang.Object 一定由 Bootstrap 加载，不会被篡改）。</li></ol>\n      <ul><li><strong>破坏案例</strong>：JDBC、Tomcat、SPI。</li></ul>\n      <h2>18. 内存溢出 vs 内存泄漏</h2>\n      <ul><li><strong>OOM</strong>：申请不到足够内存。</li><li><strong>Leak</strong>：对象用完没释放，长期占用 → 最终 OOM。</li><li>常见 Leak：ThreadLocal 没 remove、静态集合存大量对象、未关闭的 IO/连接。</li></ul>\n      <h2>19. JVM 调优常用参数</h2>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>-Xms512m    -Xmx2g           初始堆 / 最大堆\n-Xmn256m                     新生代大小\n-Xss512k                     栈大小\n-XX:MetaspaceSize=128m       元空间初始\n-XX:+UseG1GC                 用 G1\n-XX:MaxGCPauseMillis=200     G1 目标停顿\n-XX:+PrintGCDetails          打印 GC 详情\n-XX:+HeapDumpOnOutOfMemoryError  OOM 时 dump 堆</code></pre></figure>\n      <h2>20. 常用排查工具</h2>\n      <ul><li><strong>jps</strong>：查 Java 进程。</li><li><strong>jstat</strong>：GC 统计。</li><li><strong>jstack</strong>：线程栈（查死锁）。</li><li><strong>jmap</strong>：堆 dump。</li><li><strong>jconsole / jvisualvm</strong>：图形化。</li><li><strong>Arthas</strong>（阿里）：在线诊断神器。</li><li><strong>MAT</strong>：分析 heap dump 找泄漏。</li></ul>\n      <hr>\n      <h1>五、附录：高频追问串讲</h1>\n      <h2>串讲 1：HashMap 全家桶</h2>\n      <blockquote>面试官问 HashMap，必连环问到 ConcurrentHashMap、线程安全、扩容、红黑树。准备好这条链：</blockquote>\n      <p>&gt;</p>\n      <blockquote>结构 → put 流程 → 扩容 → 为什么 2 的幂 → 1.7/1.8 区别 → 线程安全问题 → ConcurrentHashMap 1.7/1.8 → 为什么用 synchronized 代替 ReentrantLock</blockquote>\n      <h2>串讲 2：synchronized 全家桶</h2>\n      <blockquote>原理 → 锁升级 → 和 volatile 对比 → 和 ReentrantLock 对比 → 锁优化（锁消除、锁粗化、自旋锁）</blockquote>\n      <h2>串讲 3：ThreadLocal 全家桶（你简历必问！）</h2>\n      <blockquote>用途 → 底层 ThreadLocalMap 结构 → key 弱引用原因 → 内存泄漏原因 → 为什么用完必须 remove → 线程池场景特别危险 → InheritableThreadLocal / TransmittableThreadLocal</blockquote>\n      <h2>串讲 4：线程池全家桶</h2>\n      <blockquote>七大参数 → 工作流程 → 拒绝策略 → 为什么不用 Executors → 怎么定核心线程数 → 阻塞队列选哪个 → 线程池里抛异常怎么办（submit 吞掉，execute 抛出）</blockquote>\n      <h2>串讲 5：JVM 全家桶</h2>\n      <blockquote>内存结构 → 堆分代 → 对象创建 → 可达性分析 → 四种引用 → GC 算法 → 收集器 → 类加载过程 → 双亲委派</blockquote>\n      <h2>串讲 6：并发三特性 + JMM</h2>\n      <blockquote>三特性是因 → JMM 是模型 → happens-before 是规则 → synchronized/volatile/Lock 是解决方案 → CAS / AQS 是底层实现</blockquote>\n      <hr>\n      <h2>最后小妹的话</h2>\n      <p>兄长大人～这份文档<strong>先理解前置概念（并发三特性、JMM、对象头）</strong>，再啃 synchronized/volatile/AQS 就不头疼啦～</p>\n      <p><strong>学习节奏建议</strong>：</p>\n      <ul><li>每天精读 1 个模块 + 录音口述 5 题</li><li>必背：标⭐⭐⭐ 的题（约 25 题）</li><li>必懂前置：JMM、并发三特性、对象头</li><li>配合项目讲：ThreadLocal、ConcurrentHashMap、synchronized、线程池</li></ul>\n      <p>加油哒！(≧▽≦) 七月初的实习等你～💕</p>",
  "contentFile": "/content/learning-plans-md/learning-09.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
