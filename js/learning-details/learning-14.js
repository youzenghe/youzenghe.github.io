(function(){
  const detail = {
  "id": 14,
  "content": "<h1>尤赠贺 - 简历项目面试追问全集</h1>\n      <blockquote>基于你简历两个项目（ClassicSage / PoetryNest）整理</blockquote>\n      <blockquote>每个技术点都按 <strong>「介绍 → 为什么用 → 怎么实现 → 难点 → 优化」</strong> 五步追问准备</blockquote>\n      <blockquote>面试官 80% 的时间会问项目，把这份吃透 = 拿到实习</blockquote>\n      <hr>\n      <h2>目录</h2>\n      <ul><li><a href=\"#总览面试官提问套路\">总览：面试官提问套路</a></li><li><a href=\"#项目一classicsage重点\">项目一：ClassicSage（重点）</a></li><li><a href=\"#项目二poetrynest\">项目二：PoetryNest</a></li><li><a href=\"#简历通用追问\">简历通用追问</a></li><li><a href=\"#自我介绍--项目介绍话术模板\">自我介绍 / 项目介绍话术模板</a></li></ul>\n      <hr>\n      <h2>总览：面试官提问套路</h2>\n      <p>面试官看到你简历的固定动作：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>1. 让你 2 分钟讲项目          → 考表达和架构感\n2. 挑一个技术点深挖           → &quot;你说用了 Redisson，讲讲原理&quot;\n3. 追问&quot;为什么用 XX 不用 YY&quot;   → 考技术选型思考\n4. 追问难点和踩坑             → 考真实性（撒谎的项目这一步必崩）\n5. 假设场景题                 → &quot;如果 QPS 翻 10 倍你怎么改&quot;</code></pre></figure>\n      <p><strong>核心原则</strong>：每个简历词都要能答出 <strong>「这是什么 / 为什么用它 / 怎么用的 / 踩过什么坑」</strong> 四件套。</p>\n      <hr>\n      <h1>项目一：ClassicSage（重点）</h1>\n      <blockquote>技术栈：Spring Boot, MyBatis-Plus, Redis, MySQL, Neo4j, Redisson, RabbitMQ, TestNG, RestAssured, JMeter</blockquote>\n      <h2>0. 项目整体追问</h2>\n      <h3>Q0.1 介绍一下 ClassicSage 这个项目</h3>\n      <p><strong>回答模板</strong>（30 秒版）：</p>\n      <blockquote>ClassicSage 是一个基于 Spring Boot 的国学知识图谱平台，核心是把人物、典籍、流派之间的复杂关系建模并提供查询。我用 MySQL 存基础数据，Neo4j 存关系图谱，Redis 做多级缓存，Redisson 解决并发幂等问题，RabbitMQ 做 MySQL 到 Neo4j 的异步同步。项目还集成了 TestNG + RestAssured 做接口自动化测试，已获得软著。</blockquote>\n      <h3>Q0.2 项目背景 / 为什么做这个</h3>\n      <ul><li>真实回答：个人技术学习项目 + 软著</li><li>包装回答：传统国学查询场景关系网络复杂，普通 SQL 难以表达多跳关系（比如&quot;谁是李白的师爷的弟子&quot;），所以引入图数据库</li></ul>\n      <h3>Q0.3 项目架构图你能画一下吗</h3>\n      <p>准备一张架构图，包含：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>[前端] → [Nginx] → [Spring Boot]\n                       ↓\n        ┌──────────────┼──────────────┐\n        ↓              ↓              ↓\n     [Redis]      [MySQL]        [Neo4j]\n                       ↓\n                  [RabbitMQ] → 异步同步到 Neo4j</code></pre></figure>\n      <h3>Q0.4 项目难点是什么</h3>\n      <p>准备 3 个真实难点（<strong>面试官最爱问</strong>）：</p>\n      <ol><li><strong>MySQL 和 Neo4j 数据一致性</strong>：双写无法用本地事务，用 RabbitMQ + 重试 + 死信队列 + 幂等消费保证最终一致。</li><li><strong>缓存击穿</strong>：热点人物（如李白）TTL 到期瞬间高并发 → 用 Redisson 互斥锁 / 逻辑过期解决。</li><li><strong>图谱构建幂等性</strong>：异步任务可能重复消费 → Redisson 分布式锁 + Redis 标记位双保险。</li></ol>\n      <hr>\n      <h2>1. Spring Boot 相关</h2>\n      <h3>Q1.1 SpringBoot 自动装配原理 ⭐⭐⭐</h3>\n      <p><strong>回答主线</strong>：</p>\n      <ol><li>启动类 <code>@SpringBootApplication</code> = <code>@SpringBootConfiguration</code> + <code>@EnableAutoConfiguration</code> + <code>@ComponentScan</code></li><li><code>@EnableAutoConfiguration</code> 通过 <code>@Import(AutoConfigurationImportSelector.class)</code> 引入选择器</li><li>选择器读取 <code>META-INF/spring.factories</code>（2.7 后是 <code>META-INF/spring/...imports</code>）</li><li>加载里面所有 <code>xxxAutoConfiguration</code> 类</li><li>这些类用 <code>@Conditional</code> 系列注解判断是否生效（如 <code>@ConditionalOnClass</code>、<code>@ConditionalOnMissingBean</code>）</li></ol>\n      <h3>Q1.2 IOC 和 AOP</h3>\n      <ul><li><strong>IOC</strong>：控制反转。对象的创建和依赖关系由容器管理，开发者只声明不创建。</li><li><strong>DI</strong>：依赖注入，IOC 的实现方式（@Autowired / 构造器注入 / setter 注入）。</li><li><strong>AOP</strong>：面向切面。把日志、事务、权限等横切逻辑从业务代码抽出来。底层：JDK 动态代理（接口）+ CGLib（无接口）。</li></ul>\n      <h3>Q1.3 你写过自定义 Starter 吗（简历提到&quot;SpringBoot DIY&quot;）</h3>\n      <p><strong>话术</strong>：</p>\n      <blockquote>我封装过一个 Redis 缓存工具的 starter，里面包含 RedisTemplate 配置、缓存穿透/击穿的通用方法、自定义注解 <code>@SafeCache</code>。流程是：</blockquote>\n      <blockquote>1. 创建 <code>xxx-spring-boot-autoconfigure</code> 模块</blockquote>\n      <blockquote>2. 写 <code>@Configuration</code> + <code>@ConditionalOnXxx</code></blockquote>\n      <blockquote>3. 在 <code>META-INF/spring.factories</code> 注册</blockquote>\n      <blockquote>4. 创建 <code>xxx-spring-boot-starter</code> 模块依赖前者</blockquote>\n      <blockquote>5. 业务项目引入 starter 即用</blockquote>\n      <h3>Q1.4 Bean 的生命周期</h3>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>实例化 → 属性赋值 → BeanNameAware/BeanFactoryAware →\nBeanPostProcessor.before → @PostConstruct → InitializingBean.afterPropertiesSet →\ninit-method → BeanPostProcessor.after → 使用 →\n@PreDestroy → DisposableBean.destroy → destroy-method</code></pre></figure>\n      <h3>Q1.5 Bean 的作用域</h3>\n      <ul><li><strong>singleton</strong>（默认）：单例</li><li><strong>prototype</strong>：每次获取新实例</li><li><strong>request</strong> / <strong>session</strong>：Web 环境，每个请求/会话一个</li></ul>\n      <h3>Q1.6 循环依赖怎么解决</h3>\n      <p>Spring 用<strong>三级缓存</strong>：</p>\n      <ul><li>一级 <code>singletonObjects</code>：完整 Bean</li><li>二级 <code>earlySingletonObjects</code>：半成品 Bean（已实例化未填充属性）</li><li>三级 <code>singletonFactories</code>：Bean 工厂（支持 AOP 代理）</li></ul>\n      <p><strong>只能解决单例 + setter 注入的循环依赖</strong>，构造器注入或 prototype 解决不了。</p>\n      <hr>\n      <h2>2. Redis 相关（你简历核心）</h2>\n      <h3>Q2.1 Redis 在你项目里缓存了什么</h3>\n      <ul><li>人物详情（String 存 JSON）</li><li>人物关系（List / Set）</li><li>热点 Top 排行（ZSet）</li><li>限流计数（String + INCR）</li></ul>\n      <h3>Q2.2 为什么用 Redis 不用 Caffeine 等本地缓存</h3>\n      <ul><li>本地缓存：单机快，但<strong>多节点数据不一致</strong></li><li>Redis：集群共享，可以做<strong>分布式锁、限流、计数器</strong></li><li>实际项目常<strong>多级缓存</strong>：Caffeine（一级） + Redis（二级） + MySQL（兜底）</li></ul>\n      <h3>Q2.3 缓存穿透 ⭐⭐⭐</h3>\n      <ul><li><strong>是什么</strong>：查一个<strong>根本不存在</strong>的 key，每次都打到数据库。</li><li><strong>危害</strong>：恶意攻击导致 DB 雪崩。</li><li><strong>解决</strong>：</li></ul>\n      <ol><li><strong>缓存空值</strong>（简单但占内存，要加短 TTL）</li><li><strong>布隆过滤器</strong>（位图判断 key 是否存在，有少量误判但永不漏）</li></ol>\n      <h3>Q2.4 缓存击穿 ⭐⭐⭐</h3>\n      <ul><li><strong>是什么</strong>：<strong>热点 key</strong> 过期瞬间，大量并发请求同时穿透到 DB。</li><li><strong>解决</strong>：</li></ul>\n      <ol><li><strong>互斥锁</strong>（Redisson lock，只让一个线程查 DB 重建缓存）</li><li><strong>逻辑过期</strong>（key 永不过期，value 里存逻辑过期时间，过期后异步重建）</li></ol>\n      <h3>Q2.5 缓存雪崩 ⭐⭐⭐</h3>\n      <ul><li><strong>是什么</strong>：大量 key <strong>同时过期</strong> 或 Redis <strong>整体宕机</strong>，请求全打 DB。</li><li><strong>解决</strong>：</li></ul>\n      <ol><li><strong>TTL 加随机值</strong>（如 30 分钟 ± 5 分钟）</li><li><strong>Redis 高可用</strong>（哨兵 / 集群）</li><li><strong>限流降级</strong>（Sentinel / Hystrix）</li></ol>\n      <h3>Q2.6 你说 Redis 用了 JSON / 字符串 / TTL，什么场景用什么</h3>\n      <ul><li><strong>String（JSON）</strong>：人物详情等大对象</li><li><strong>String（普通）</strong>：计数器、Token、简单状态</li><li><strong>Hash</strong>：可能要改其中某个字段的对象（避免反序列化整对象）</li><li><strong>TTL 设置</strong>：高频查询 1h、低频 5min、绝对不变的 24h</li></ul>\n      <h3>Q2.7 Redis 持久化 RDB vs AOF ⭐⭐⭐</h3>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>RDB</th><th>AOF</th></tr></thead><tbody><tr><td>原理</td><td>全量快照</td><td>增量命令追加</td></tr><tr><td>文件</td><td>二进制</td><td>文本</td></tr><tr><td>速度</td><td>恢复快</td><td>恢复慢</td></tr><tr><td>数据安全</td><td>可能丢几分钟</td><td>最多丢 1 秒</td></tr><tr><td>文件大小</td><td>小</td><td>大</td></tr></tbody></table></div>\n      <p><strong>生产</strong>：两个一起开，AOF 主，RDB 备。</p>\n      <h3>Q2.8 Redis 为什么这么快</h3>\n      <ol><li>纯<strong>内存</strong>操作</li><li><strong>单线程</strong>避免上下文切换和锁竞争</li><li><strong>IO 多路复用</strong>（epoll）</li><li>高效的<strong>数据结构</strong>（SDS、跳表、压缩列表）</li></ol>\n      <h3>Q2.9 Redis 单线程为什么不慢</h3>\n      <p>瓶颈在 IO 不在 CPU。单线程 + epoll 同时处理大量连接，CPU 没在等 IO。</p>\n      <h3>Q2.10 Redis 内存淘汰策略 ⭐</h3>\n      <p>8 种，记住分类：</p>\n      <ul><li><strong>不淘汰</strong>：noeviction（默认，OOM 报错）</li><li><strong>淘汰过期</strong>：volatile-lru / volatile-lfu / volatile-random / volatile-ttl</li><li><strong>淘汰所有</strong>：allkeys-lru / allkeys-lfu / allkeys-random</li></ul>\n      <p><strong>生产推荐</strong>：allkeys-lru（最近最少使用）</p>\n      <h3>Q2.11 Redis 过期 key 怎么删除</h3>\n      <ul><li><strong>惰性删除</strong>：访问时检查，过期了删（节省 CPU，浪费内存）</li><li><strong>定期删除</strong>：每 100ms 抽样删一批（折中）</li></ul>\n      <hr>\n      <h2>3. Redis + Lua（你简历高亮点）⭐⭐⭐</h2>\n      <h3>Q3.1 为什么用 Lua 不用 Java 代码多次操作 Redis</h3>\n      <ol><li><strong>原子性</strong>：Lua 脚本在 Redis 端整体执行，不会被打断</li><li><strong>减少网络往返</strong>：多个命令一次发送</li><li><strong>可复用</strong>：脚本缓存（EVALSHA）</li></ol>\n      <h3>Q3.2 你的 Lua 脚本做了什么</h3>\n      <p><strong>典型场景</strong>：</p>\n      <ul><li><strong>限流</strong>：滑动窗口 / 令牌桶</li><li><strong>秒杀扣库存</strong>：判断库存 + 扣减 + 记录用户 一气呵成</li><li><strong>分布式锁释放</strong>：判断锁主 + 删除（防误删）</li></ul>\n      <p><strong>示例脚本</strong>（限流）：</p>\n      <figure class=\"code-block\" data-lang=\"lua\"><figcaption><span>lua</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-lua\">-- KEYS[1]: 限流 key,  ARGV[1]: 阈值, ARGV[2]: 窗口秒数\nlocal count = redis.call(&#x27;incr&#x27;, KEYS[1])\nif tonumber(count) == 1 then\n    redis.call(&#x27;expire&#x27;, KEYS[1], ARGV[2])\nend\nif tonumber(count) &gt; tonumber(ARGV[1]) then\n    return 0\nend\nreturn 1</code></pre></figure>\n      <h3>Q3.3 Lua 脚本会不会很慢</h3>\n      <p><strong>会！</strong>单线程 Redis 中 Lua 长时间执行会<strong>阻塞所有其他请求</strong>。所以：</p>\n      <ul><li>脚本要短</li><li>不要循环过多</li><li>别在脚本里做复杂计算</li></ul>\n      <h3>Q3.4 Lua 执行原子性如何保证</h3>\n      <p>Redis 单线程模型 + 脚本不可被打断（其他命令必须等脚本执行完）。</p>\n      <hr>\n      <h2>4. Redisson 分布式锁（你简历强项）⭐⭐⭐</h2>\n      <h3>Q4.1 什么场景需要分布式锁</h3>\n      <ul><li><strong>多节点</strong>部署时（单机 synchronized 失效）</li><li>需要<strong>互斥访问共享资源</strong>（库存、订单号、幂等）</li></ul>\n      <h3>Q4.2 Redisson 加锁原理</h3>\n      <ol><li>用 <strong>Lua 脚本</strong>保证加锁原子性：<code>hset key threadId 1</code> + <code>pexpire key 30000</code></li><li>加锁成功 → 启动 <strong>WatchDog</strong>（看门狗），每 10 秒续期一次到 30 秒</li><li>加锁失败 → 通过 Redis 发布订阅（<code>Subscribe</code>）监听锁释放，避免轮询</li></ol>\n      <h3>Q4.3 WatchDog 看门狗机制 ⭐</h3>\n      <ul><li><strong>作用</strong>：防止业务还没执行完锁就过期了</li><li><strong>触发条件</strong>：调用 <code>lock()</code> 不传超时时间才会启用（传了就不开看门狗）</li><li><strong>续期周期</strong>：默认 <code>internalLockLeaseTime / 3</code> = 10 秒一次</li><li><strong>续期到</strong>：默认 30 秒</li><li><strong>客户端宕机</strong>：看门狗停了，30 秒后锁自动过期，不会死锁</li></ul>\n      <h3>Q4.4 Redisson 可重入原理</h3>\n      <p>锁结构是 <strong>Hash</strong>：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>key:   lock_name\nfield: threadId (UUID + 线程ID)\nvalue: 重入次数</code></pre></figure>\n      <p>同线程再来加锁 → value +1；释放 → value -1；为 0 时删除 key。</p>\n      <h3>Q4.5 锁误删问题怎么解决</h3>\n      <p><strong>问题</strong>：A 加锁后业务超时，锁过期；B 拿到锁；A 业务完了去 release，删了 B 的锁。</p>\n      <p><strong>解决</strong>：</p>\n      <ol><li>加锁时 value 存 <strong>当前线程唯一标识</strong></li><li>释放时 <strong>Lua 脚本</strong> 先判断 value == 自己才删</li></ol>\n      <h3>Q4.6 MultiLock（联锁）</h3>\n      <p>锁多个 Redis 实例，全部加锁成功才算成功。用于 RedLock 算法，跨实例避免单点失效。</p>\n      <h3>Q4.7 你项目里分布式锁锁的是什么</h3>\n      <p><strong>幂等控制</strong>：图谱构建任务可能被重复触发，用锁保证同一资源同一时刻只有一个任务在构建。</p>\n      <h3>Q4.8 用 Redisson 而不是自己写 setnx + expire 的原因</h3>\n      <ol><li>setnx + expire <strong>不是原子操作</strong>（虽然 Redis 2.6.12 后 SET 支持 NX EX 解决了）</li><li>Redisson 帮你解决了：可重入、续期、公平锁、读写锁、信号量、误删</li><li>不需要重复造轮子</li></ol>\n      <hr>\n      <h2>5. RabbitMQ（你简历强项）⭐⭐⭐</h2>\n      <h3>Q5.1 你的项目为什么用 RabbitMQ</h3>\n      <p><strong>核心场景</strong>：MySQL → Neo4j 数据异步同步</p>\n      <ul><li>MySQL 写入后立刻同步 Neo4j 会拖慢主流程</li><li>双写无法事务，需要<strong>最终一致性</strong></li><li>MQ 解耦：MySQL 写完发消息就返回，Neo4j 慢慢消费</li></ul>\n      <h3>Q5.2 MQ 的三大作用</h3>\n      <ol><li><strong>异步</strong>：耗时操作不阻塞主流程</li><li><strong>解耦</strong>：生产消费独立部署</li><li><strong>削峰</strong>：流量洪峰先入队列慢慢处理</li></ol>\n      <h3>Q5.3 RabbitMQ 核心概念</h3>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>Producer → Exchange → (RoutingKey) → Queue → Consumer\n                ↑\n            通过 Binding 关联</code></pre></figure>\n      <ul><li><strong>Exchange 类型</strong>：</li><li><code>Direct</code>：精确匹配 RoutingKey</li><li><code>Fanout</code>：广播给所有绑定队列</li><li><code>Topic</code>：通配符匹配（<code>order.*.created</code>）</li><li><code>Headers</code>：按消息 header 匹配（少用）</li></ul>\n      <h3>Q5.4 你简历说&quot;5 个队列&quot;，怎么设计的</h3>\n      <p><strong>话术示例</strong>：</p>\n      <blockquote>我按业务领域拆了 5 个队列：</blockquote>\n      <blockquote>1. <code>sage.entity.queue</code> - 人物/作品创建同步</blockquote>\n      <blockquote>2. <code>sage.relation.queue</code> - 关系图谱构建</blockquote>\n      <blockquote>3. <code>sage.update.queue</code> - 数据更新同步</blockquote>\n      <blockquote>4. <code>sage.delete.queue</code> - 删除同步</blockquote>\n      <blockquote>5. <code>sage.dlx.queue</code> - 死信队列（兜底）</blockquote>\n      <h3>Q5.5 消息可靠性怎么保证 ⭐⭐⭐</h3>\n      <p><strong>三个环节都要保</strong>：</p>\n      <div class=\"md-table\"><table><thead><tr><th>环节</th><th>风险</th><th>解决</th></tr></thead><tbody><tr><td>生产端 → MQ</td><td>消息没到 MQ</td><td><code>confirm</code> 机制 + <code>return</code> 回调</td></tr><tr><td>MQ 内部</td><td>MQ 宕机消息丢</td><td>队列持久化 + 消息持久化（<code>deliveryMode=2</code>）</td></tr><tr><td>MQ → 消费端</td><td>消费失败</td><td>手动 ACK + 重试 + 死信队列</td></tr></tbody></table></div>\n      <h3>Q5.6 死信队列 DLX</h3>\n      <p><strong>触发死信的 3 种情况</strong>：</p>\n      <ol><li>消息被 reject / nack 且不重回队列</li><li>消息 TTL 过期</li><li>队列达到最大长度</li></ol>\n      <p><strong>用途</strong>：</p>\n      <ul><li>失败重试（消费失败 → 死信 → 延迟重新投递）</li><li><strong>延迟队列</strong>（TTL + DLX 模拟）</li></ul>\n      <h3>Q5.7 幂等消费怎么做 ⭐</h3>\n      <p><strong>问题</strong>：网络问题导致消息可能被消费多次（at-least-once）</p>\n      <p><strong>方案</strong>：</p>\n      <ol><li><strong>数据库唯一约束</strong>（最简单可靠）</li><li><strong>Redis SETNX</strong>（基于消息 ID 标记）</li><li><strong>状态机</strong>（订单只能从 PENDING → PAID，第二次消费跳过）</li></ol>\n      <h3>Q5.8 消息堆积怎么办</h3>\n      <ol><li><strong>加消费者</strong>（水平扩展）</li><li><strong>批量消费</strong>（<code>prefetch</code> 调大）</li><li><strong>临时转移</strong>：把消息转到临时队列分批处理</li><li><strong>升级硬件</strong></li></ol>\n      <h3>Q5.9 消息顺序性怎么保证</h3>\n      <ul><li><strong>单队列单消费者</strong>：天然有序</li><li><strong>多消费者</strong>：根据业务 key 哈希分发到固定队列</li></ul>\n      <h3>Q5.10 RabbitMQ vs Kafka vs RocketMQ</h3>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>RabbitMQ</th><th>Kafka</th><th>RocketMQ</th></tr></thead><tbody><tr><td>吞吐</td><td>万级</td><td>百万级</td><td>十万级</td></tr><tr><td>延迟</td><td>微秒</td><td>毫秒</td><td>毫秒</td></tr><tr><td>功能</td><td>丰富</td><td>简单</td><td>丰富</td></tr><tr><td>适合</td><td>业务 MQ</td><td>日志/大数据</td><td>金融业务</td></tr></tbody></table></div>\n      <hr>\n      <h2>6. MySQL（必问）⭐⭐⭐</h2>\n      <h3>Q6.1 索引底层为什么用 B+ 树</h3>\n      <ul><li><strong>B 树</strong>：每个节点存 key 和 data</li><li><strong>B+ 树</strong>：内节点只存 key，叶子节点存所有 data 且<strong>叶子用链表相连</strong></li><li><strong>优势</strong>：</li></ul>\n      <ol><li>内节点不存 data → 一个节点能容纳更多 key → <strong>树更矮 → IO 更少</strong></li><li>范围查询：叶子链表直接遍历，无需回溯</li></ol>\n      <h3>Q6.2 聚簇索引 vs 非聚簇索引</h3>\n      <ul><li><strong>聚簇</strong>：叶子节点存<strong>整行数据</strong>（InnoDB 主键索引）</li><li><strong>非聚簇（二级索引）</strong>：叶子存<strong>主键值</strong>，要拿全部数据要<strong>回表</strong></li></ul>\n      <h3>Q6.3 回表 / 覆盖索引</h3>\n      <ul><li><strong>回表</strong>：通过二级索引拿到主键，再去聚簇索引查整行</li><li><strong>覆盖索引</strong>：查询字段都在索引里，不用回表（用 EXPLAIN 看到 <code>Using index</code>）</li></ul>\n      <h3>Q6.4 最左前缀原则</h3>\n      <p>联合索引 <code>(a, b, c)</code>：</p>\n      <ul><li>命中：<code>a</code> / <code>a,b</code> / <code>a,b,c</code> / <code>a,c</code>（c 不用索引但 a 用了）</li><li>不命中：<code>b</code> / <code>c</code> / <code>b,c</code></li></ul>\n      <h3>Q6.5 索引失效场景</h3>\n      <ol><li>不满足最左前缀</li><li>函数 / 计算：<code>WHERE YEAR(date) = 2025</code></li><li>隐式类型转换：字段是 varchar，传了 int</li><li><code>!=</code> / <code>NOT IN</code></li><li><code>LIKE &#x27;%xx&#x27;</code>（前模糊）</li><li><code>OR</code> 两边不都是索引</li></ol>\n      <h3>Q6.6 事务的四大特性 ACID</h3>\n      <ul><li><strong>A 原子性</strong>：要么全成要么全败（undo log 回滚）</li><li><strong>C 一致性</strong>：数据合法（业务保证 + 其他三特性保证）</li><li><strong>I 隔离性</strong>：并发事务互不干扰（锁 + MVCC）</li><li><strong>D 持久性</strong>：提交后永久（redo log）</li></ul>\n      <h3>Q6.7 事务隔离级别 ⭐⭐⭐</h3>\n      <div class=\"md-table\"><table><thead><tr><th>级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>READ UNCOMMITTED</td><td>✅</td><td>✅</td><td>✅</td></tr><tr><td>READ COMMITTED</td><td>❌</td><td>✅</td><td>✅</td></tr><tr><td>REPEATABLE READ（MySQL 默认）</td><td>❌</td><td>❌</td><td>✅*</td></tr><tr><td>SERIALIZABLE</td><td>❌</td><td>❌</td><td>❌</td></tr></tbody></table></div>\n      <p>*MySQL 的 RR 通过 MVCC + 间隙锁基本解决了幻读</p>\n      <h3>Q6.8 MVCC 原理 ⭐⭐⭐（必考）</h3>\n      <ul><li>每行隐藏字段：<code>DB_TRX_ID</code>（事务ID）、<code>DB_ROLL_PTR</code>（指向 undo log）</li><li>事务开启时生成 <strong>ReadView</strong>，里面有 <code>m_ids</code>（活跃事务列表）、<code>min_trx_id</code>、<code>max_trx_id</code>、<code>creator_trx_id</code></li><li>读取时根据可见性算法决定看哪个版本</li><li><strong>RC</strong>：每次 select 生成新 ReadView</li><li><strong>RR</strong>：事务第一次 select 生成 ReadView，后续复用</li></ul>\n      <h3>Q6.9 快照读 vs 当前读</h3>\n      <ul><li><strong>快照读</strong>：普通 <code>SELECT</code>，走 MVCC，不加锁</li><li><strong>当前读</strong>：<code>SELECT ... FOR UPDATE</code> / <code>LOCK IN SHARE MODE</code> / <code>UPDATE</code> / <code>DELETE</code>，读最新版本并加锁</li></ul>\n      <h3>Q6.10 MySQL 锁</h3>\n      <ul><li><strong>全局锁</strong>：FTWRL，备份用</li><li><strong>表锁</strong>：<code>LOCK TABLES</code></li><li><strong>行锁</strong>：</li><li><strong>Record Lock</strong>：锁单行</li><li><strong>Gap Lock</strong>：锁间隙（RR 才有）</li><li><strong>Next-Key Lock</strong>：行锁 + 间隙锁（解决幻读）</li></ul>\n      <h3>Q6.11 redo log / undo log / binlog</h3>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>redo log</th><th>undo log</th><th>binlog</th></tr></thead><tbody><tr><td>层级</td><td>InnoDB</td><td>InnoDB</td><td>Server</td></tr><tr><td>作用</td><td>崩溃恢复（持久性）</td><td>回滚（原子性）+ MVCC</td><td>主从复制、数据恢复</td></tr><tr><td>写入</td><td>物理日志</td><td>逻辑日志</td><td>逻辑日志</td></tr></tbody></table></div>\n      <h3>Q6.12 防超卖怎么实现 ⭐</h3>\n      <ul><li><strong>方案 1</strong>：<code>UPDATE stock SET num = num - 1 WHERE id = ? AND num &gt; 0</code>，判断影响行数</li><li><strong>方案 2</strong>：乐观锁 <code>WHERE version = ?</code> + 重试</li><li><strong>方案 3</strong>：Redis 预减库存 + MQ 异步落库</li><li><strong>方案 4</strong>：分布式锁</li></ul>\n      <hr>\n      <h2>7. Neo4j（你简历亮点 + 面试官好奇点）⭐⭐⭐</h2>\n      <h3>Q7.1 为什么用 Neo4j 不用 MySQL</h3>\n      <p><strong>核心理由</strong>：<strong>多跳关系查询性能</strong>。</p>\n      <ul><li>MySQL 查&quot;谁是李白的师爷的弟子&quot;要多次 JOIN，性能差，SQL 难写</li><li>Neo4j 是图结构，关系即指针，<strong>多跳查询是常数时间</strong> + Cypher 语法表达直观</li></ul>\n      <h3>Q7.2 Cypher 基础语法</h3>\n      <figure class=\"code-block\" data-lang=\"cypher\"><figcaption><span>cypher</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-cypher\">// 创建节点\nCREATE (p:Person {name: &#x27;李白&#x27;, dynasty: &#x27;唐&#x27;})\n\n// 创建关系\nMATCH (a:Person {name: &#x27;李白&#x27;}), (b:Person {name: &#x27;杜甫&#x27;})\nCREATE (a)-[:FRIEND_OF]-&gt;(b)\n\n// 查询：李白的朋友的朋友\nMATCH (a:Person {name: &#x27;李白&#x27;})-[:FRIEND_OF*2]-&gt;(b)\nRETURN b</code></pre></figure>\n      <h3>Q7.3 MySQL 数据怎么同步到 Neo4j</h3>\n      <p><strong>你的方案</strong>：MySQL 写完发 RabbitMQ → 消费者解析消息 → Cypher 写 Neo4j。</p>\n      <p><strong>为什么不用 Canal 监听 binlog</strong>：</p>\n      <ul><li>项目轻量，引入 Canal 部署复杂</li><li>业务上能在 Service 层主动控制时机，更可控</li></ul>\n      <h3>Q7.4 一致性怎么保证</h3>\n      <p><strong>最终一致性</strong>：</p>\n      <ul><li>消息发送：本地消息表 / RabbitMQ confirm</li><li>消费失败：重试 + 死信兜底</li><li>不一致兜底：定时对账任务</li></ul>\n      <h3>Q7.5 Neo4j 适合什么场景</h3>\n      <ul><li>社交关系（六度人脉）</li><li>知识图谱</li><li>推荐系统</li><li>风控反欺诈（关联账户排查）</li></ul>\n      <hr>\n      <h2>8. TestNG + RestAssured + JMeter</h2>\n      <h3>Q8.1 TestNG vs JUnit</h3>\n      <ul><li>TestNG 支持<strong>依赖测试、参数化、分组、并行</strong>，比 JUnit 更适合大型测试</li><li>注解：<code>@Test</code>、<code>@BeforeMethod</code>、<code>@DataProvider</code></li></ul>\n      <h3>Q8.2 RestAssured 做什么的</h3>\n      <p><strong>REST API 自动化测试</strong>框架，语法链式：</p>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">given().param(&quot;id&quot;, 1)\n.when().get(&quot;/api/person&quot;)\n.then().statusCode(200).body(&quot;name&quot;, equalTo(&quot;李白&quot;));</code></pre></figure>\n      <h3>Q8.3 JMeter 压测你做了什么</h3>\n      <ul><li>模拟 N 个并发用户访问接口</li><li>关注指标：<strong>TPS、平均响应时间、99 分位、错误率</strong></li><li>你的项目典型数据：单机 QPS XXX、加 Redis 缓存后提升 X 倍</li></ul>\n      <hr>\n      <h1>项目二：PoetryNest</h1>\n      <blockquote>技术栈：Spring Boot, MyBatis, Redis, MySQL, Spring Cache, WebSocket, OSS, TestNG, Selenium</blockquote>\n      <h2>0. 项目介绍</h2>\n      <h3>Q0.1 这个项目是做什么的</h3>\n      <p><strong>模板</strong>：</p>\n      <blockquote>PoetryNest 是一个诗词社交分享平台，用户可以发布诗词作品、互相关注、实时聊天。重点用到了 JWT 做登录认证、Spring Cache + AOP 做缓存自动管理、WebSocket 实现在线聊天、ThreadLocal 透传用户上下文。</blockquote>\n      <hr>\n      <h2>1. JWT（必问！）⭐⭐⭐</h2>\n      <h3>Q1.1 JWT 是什么 / 组成</h3>\n      <ul><li><strong>Header</strong>：算法（HS256 等）+ 类型（JWT）</li><li><strong>Payload</strong>：业务数据（userId、过期时间）</li><li><strong>Signature</strong>：签名 = HMAC(Header + Payload, 密钥)</li></ul>\n      <p>格式：<code>xxxxx.yyyyy.zzzzz</code></p>\n      <h3>Q1.2 JWT 流程</h3>\n      <ol><li>用户登录，服务端校验密码，生成 JWT 返回</li><li>客户端存储（localStorage / cookie）</li><li>后续请求带 <code>Authorization: Bearer xxx</code></li><li>服务端<strong>验签</strong>解析出 userId</li></ol>\n      <h3>Q1.3 JWT vs Session ⭐⭐⭐</h3>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>JWT</th><th>Session</th></tr></thead><tbody><tr><td>存储</td><td>客户端</td><td>服务端</td></tr><tr><td>扩展性</td><td>无状态，易水平扩展</td><td>需共享 session（Redis）</td></tr><tr><td>失效控制</td><td>难（除非加 Redis 黑名单）</td><td>容易（删 session）</td></tr><tr><td>体积</td><td>大（每次请求都带）</td><td>小（只带 sessionId）</td></tr><tr><td>安全</td><td>容易泄漏，无法主动作废</td><td>服务端控制</td></tr></tbody></table></div>\n      <h3>Q1.4 JWT 怎么续签</h3>\n      <p><strong>方案 1</strong>：双 Token（accessToken + refreshToken），accessToken 短期，refresh 来换新的 <strong>方案 2</strong>：滑动过期，每次请求检查若剩余时间 &lt; N，颁发新 Token 通过响应头返回</p>\n      <h3>Q1.5 怎么强制下线（JWT 无状态的痛点）</h3>\n      <p><strong>方案</strong>：Redis 维护<strong>黑名单</strong>或<strong>白名单</strong></p>\n      <ul><li>黑名单：登出时把 jti 写入 Redis（TTL = JWT 剩余有效期）</li><li>白名单：登录时颁发，下线时删除（推荐）</li></ul>\n      <h3>Q1.6 JWT 安全问题</h3>\n      <ul><li>HTTPS 传输（防中间人）</li><li>不放敏感信息（Payload 是 Base64，可解码）</li><li>密钥要长且保密</li><li>过期时间合理（短一点）</li></ul>\n      <hr>\n      <h2>2. Spring Cache + AOP ⭐⭐⭐</h2>\n      <h3>Q2.1 Spring Cache 原理</h3>\n      <p><strong>基于 AOP</strong> 的声明式缓存：</p>\n      <ul><li>启动加 <code>@EnableCaching</code></li><li>方法上加 <code>@Cacheable</code> / <code>@CacheEvict</code> / <code>@CachePut</code></li><li>Spring 用 AOP 拦截方法调用，先查缓存命中则返回，不命中执行方法并写缓存</li></ul>\n      <h3>Q2.2 三大注解</h3>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@Cacheable(value=&quot;person&quot;, key=&quot;#id&quot;)    // 查询，命中直接返回\n@CachePut(value=&quot;person&quot;, key=&quot;#p.id&quot;)   // 更新，方法执行后把返回值写入缓存\n@CacheEvict(value=&quot;person&quot;, key=&quot;#id&quot;)   // 删除，方法执行后清除缓存</code></pre></figure>\n      <h3>Q2.3 你的项目&quot;AOP 自动管理&quot;具体怎么做</h3>\n      <p><strong>话术</strong>：</p>\n      <blockquote>默认 Spring Cache 太死板（key 单一、不支持复杂 TTL）。我自定义注解 <code>@SmartCache(key, ttl, type)</code>，用 AOP 切面拦截：</blockquote>\n      <blockquote>1. 查 Redis，命中返回</blockquote>\n      <blockquote>2. 不命中执行方法</blockquote>\n      <blockquote>3. 写 Redis（带随机 TTL 防雪崩）</blockquote>\n      <blockquote>4. 配合 <code>@SmartEvict</code> 在更新方法上自动清理相关 key</blockquote>\n      <h3>Q2.4 Spring Cache 失效场景</h3>\n      <ul><li><strong>同类内部调用</strong>：AOP 代理失效（this 调用不走代理）</li><li><strong>private 方法</strong>：不能被代理</li><li><strong>异常</strong>：默认抛异常也回滚缓存</li></ul>\n      <h3>Q2.5 先更新 DB 还是先删缓存 ⭐⭐⭐</h3>\n      <p><strong>经典问题，标准答案</strong>： <strong>先更新 DB，再删缓存</strong>（Cache Aside Pattern）</p>\n      <p><strong>为什么不先删缓存再更 DB</strong>：</p>\n      <ul><li>A 删缓存 → A 还没更新 DB → B 查到旧值写入缓存 → A 更新 DB → 缓存永远是旧值</li></ul>\n      <p><strong>为什么不&quot;双删&quot;</strong>：</p>\n      <ul><li>延迟双删可以更可靠，但不是必须</li></ul>\n      <hr>\n      <h2>3. WebSocket ⭐⭐⭐</h2>\n      <h3>Q3.1 WebSocket 是什么</h3>\n      <p><strong>全双工</strong>通信协议，建立连接后服务端和客户端可以<strong>互相主动推送</strong>消息，基于 TCP，初次握手用 HTTP 升级（<code>Upgrade: websocket</code>）。</p>\n      <h3>Q3.2 vs HTTP / 轮询</h3>\n      <div class=\"md-table\"><table><thead><tr><th></th><th>HTTP 轮询</th><th>长轮询</th><th>WebSocket</th></tr></thead><tbody><tr><td>连接</td><td>每次新建</td><td>阻塞等待</td><td>一次建立长连</td></tr><tr><td>服务端推送</td><td>❌</td><td>慢</td><td>✅</td></tr><tr><td>性能</td><td>差</td><td>中</td><td>好</td></tr></tbody></table></div>\n      <h3>Q3.3 你的项目用 WebSocket 干啥</h3>\n      <ul><li>在线聊天 / 通知推送</li><li>在线人数实时更新</li></ul>\n      <h3>Q3.4 SpringBoot 集成 WebSocket</h3>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@ServerEndpoint(&quot;/chat/{userId}&quot;)\n@Component\npublic class ChatServer {\n    @OnOpen public void onOpen(Session session, @PathParam String userId) {...}\n    @OnMessage public void onMessage(String msg) {...}\n    @OnClose public void onClose() {...}\n}</code></pre></figure>\n      <h3>Q3.5 心跳机制</h3>\n      <ul><li>客户端定时（30s）发 ping</li><li>服务端回 pong</li><li>长时间无心跳则关闭连接</li></ul>\n      <h3>Q3.6 多机部署 WebSocket 怎么办</h3>\n      <p><strong>问题</strong>：用户 A 连服务器 1，用户 B 连服务器 2，A 给 B 发消息怎么转发？</p>\n      <p><strong>方案</strong>：</p>\n      <ul><li><strong>Redis Pub/Sub</strong> 广播给所有节点</li><li>引入 <strong>MQ</strong></li><li>使用 <strong>STOMP + Spring Message Broker</strong></li></ul>\n      <hr>\n      <h2>4. ThreadLocal（你简历有用！必问！）⭐⭐⭐</h2>\n      <h3>Q4.1 你 ThreadLocal 用在哪</h3>\n      <p><strong>透传当前登录用户</strong>：JWT 拦截器解析出 userId 后存入 ThreadLocal，业务层任意位置 <code>UserContext.get()</code> 直接拿，不用层层传参。</p>\n      <h3>Q4.2 ThreadLocal 原理</h3>\n      <ul><li>Thread 对象内部有个 <code>ThreadLocalMap</code></li><li>key 是 ThreadLocal 实例（<strong>弱引用</strong>）</li><li>value 是你存的值（强引用）</li><li>不同线程读写互不干扰</li></ul>\n      <h3>Q4.3 内存泄漏 ⭐⭐⭐</h3>\n      <ul><li>key 弱引用 → GC 时被回收 → key=null 但 value 还在</li><li>线程不结束（<strong>线程池场景！</strong>）→ value 永远占内存</li><li><strong>解决</strong>：用 <code>try-finally</code> 在请求结束时调 <code>threadLocal.remove()</code></li></ul>\n      <h3>Q4.4 拦截器 / 过滤器里怎么用</h3>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">public class JwtInterceptor implements HandlerInterceptor {\n    public boolean preHandle(...) {\n        Long userId = parseJwt(token);\n        UserContext.set(userId);\n        return true;\n    }\n    public void afterCompletion(...) {\n        UserContext.remove();   // 必须！\n    }\n}</code></pre></figure>\n      <h3>Q4.5 父子线程传值（线程池场景的坑）</h3>\n      <ul><li><code>ThreadLocal</code> 不行</li><li><code>InheritableThreadLocal</code> 新建线程时拷贝，但<strong>线程池线程是复用的</strong>，仍不行</li><li>解决：阿里的 <strong>TransmittableThreadLocal（TTL）</strong></li></ul>\n      <hr>\n      <h2>5. SpringTask 定时任务</h2>\n      <h3>Q5.1 用法</h3>\n      <figure class=\"code-block\" data-lang=\"java\"><figcaption><span>java</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code class=\"language-java\">@EnableScheduling   // 启动类\n@Scheduled(cron = &quot;0 0 2 * * ?&quot;)  // 每天 2 点\npublic void cleanExpired() {...}</code></pre></figure>\n      <h3>Q5.2 分布式部署问题</h3>\n      <p><strong>多节点会重复执行</strong>！解决：</p>\n      <ul><li><strong>分布式锁</strong>（Redisson）抢占</li><li><strong>XXL-Job / Quartz 集群</strong>专业调度框架</li><li>业务层加幂等</li></ul>\n      <hr>\n      <h2>6. OSS（对象存储）</h2>\n      <h3>Q6.1 OSS 是什么 / 为什么用</h3>\n      <ul><li>阿里云对象存储服务，存图片、视频、文件</li><li><strong>为什么不存数据库</strong>：DB 存大文件性能差、备份慢、CDN 难</li><li><strong>为什么不存服务器</strong>：本地存储扩展难，CDN 加速</li></ul>\n      <h3>Q6.2 上传流程</h3>\n      <ol><li>前端拿 STS 临时凭证（避免 AccessKey 泄漏）</li><li>前端直传 OSS（不走后端节省带宽）</li><li>上传成功后端只存 URL</li></ol>\n      <hr>\n      <h2>7. Selenium UI 自动化</h2>\n      <h3>Q7.1 是什么</h3>\n      <p>浏览器 UI 自动化测试框架，模拟用户点击/输入。</p>\n      <h3>Q7.2 元素定位</h3>\n      <ul><li><code>By.id</code> / <code>By.name</code> / <code>By.className</code> / <code>By.xpath</code> / <code>By.cssSelector</code></li></ul>\n      <h3>Q7.3 等待</h3>\n      <ul><li>隐式等待：全局等</li><li>显式等待：等特定条件（<code>WebDriverWait + ExpectedConditions</code>）</li></ul>\n      <hr>\n      <h1>简历通用追问</h1>\n      <h2>测试相关</h2>\n      <h3>你为什么这么注重测试</h3>\n      <p><strong>回答</strong>：测试可以让我对自己的代码有信心，重构时不怕改坏，也是工程质量的体现。</p>\n      <h3>项目测试覆盖率多少</h3>\n      <p><strong>话术</strong>：核心模块（缓存、锁、消息）80% 以上，整体 60% 左右。</p>\n      <h2>项目周期 / 工作量</h2>\n      <h3>一个人多久做完的</h3>\n      <p><strong>ClassicSage 3 个月、PoetryNest 2 个月</strong> → 时间合理，不要吹&quot;3 天做完&quot;。</p>\n      <h3>遇到最大的坑</h3>\n      <p><strong>准备 2-3 个真实的踩坑</strong>：</p>\n      <ol><li>RabbitMQ 消息丢失，没开 confirm</li><li>Redisson 锁误删，没用 Lua</li><li>ThreadLocal 没 remove 内存泄漏</li><li>Neo4j 多跳查询节点数据爆炸，加了深度限制</li></ol>\n      <h2>软著</h2>\n      <ul><li>不要主动吹软著很厉害，但提到可以</li><li>软著证明项目是你的、合规可发布</li></ul>\n      <h2>GitHub</h2>\n      <ul><li><strong>面试前一定要把代码 README 写好，commit 历史看起来正常</strong></li><li>面试官真的会去看，看到混乱的代码会减分</li></ul>\n      <h2>学历 / 大二在校</h2>\n      <h3>你大二就做了这些项目？</h3>\n      <p><strong>话术</strong>：是的，我从大一暑假开始系统学 Java，大二上学期开始做项目，每天投入 6 小时以上。我对找一份实习提升工程能力非常迫切。</p>\n      <h3>期望薪资</h3>\n      <p><strong>实习日薪 200-300 / 月薪 4-6K</strong>（一线城市），按公司层级调整。</p>\n      <hr>\n      <h1>自我介绍 / 项目介绍话术模板</h1>\n      <h2>2 分钟自我介绍</h2>\n      <blockquote>您好面试官，我叫尤赠贺，目前是 XXX 大学计算机相关专业大二学生，预计 2028 年毕业。</blockquote>\n      <p>&gt;</p>\n      <blockquote>我学习 Java 后端方向，技术栈包括 Spring Boot、MyBatis、MySQL、Redis、RabbitMQ，对 JVM、并发编程有一定研究。</blockquote>\n      <p>&gt;</p>\n      <blockquote>我独立完成过两个项目：第一个是 ClassicSage，一个国学知识图谱平台，重点用 Redis 做多级缓存、Redisson 做分布式锁、RabbitMQ 实现 MySQL 到 Neo4j 的异步同步；第二个是 PoetryNest 诗词社交平台，用了 JWT 认证、WebSocket 实时通信、Spring Cache + AOP 的智能缓存。两个项目都获得了软件著作权。</blockquote>\n      <p>&gt;</p>\n      <blockquote>我还熟悉测试体系，用 TestNG + RestAssured + JMeter 做接口和性能测试。</blockquote>\n      <p>&gt;</p>\n      <blockquote>希望能加入贵公司实习，进一步学习成长，谢谢。</blockquote>\n      <h2>项目介绍 STAR 模板</h2>\n      <p>每个项目按 <strong>背景 → 你做了什么 → 难点 → 结果</strong> 讲：</p>\n      <figure class=\"code-block\"><figcaption><span>代码</span><button type=\"button\" class=\"copy-code-btn\">复制</button></figcaption><pre><code>S（Situation）：项目背景一句话\nT（Task）：你的角色和目标\nA（Action）：核心做法（3 个亮点）\nR（Result）：结果（QPS / 数据量 / 用户量 / 代码量 / 软著）</code></pre></figure>\n      <hr>\n      <h1>最后小妹的话</h1>\n      <p>兄长大人～(◕ᴗ◕✿) 这份是真正会被面试官追问的东西，<strong>比八股更重要 100 倍</strong>，因为你的简历项目就是面试官的提问目录！</p>\n      <p><strong>复习节奏</strong>：</p>\n      <ol><li>第一遍：从头到尾通读，标记不会的</li><li>第二遍：每天精读 1 个模块，<strong>录音口述</strong></li><li>第三遍：找朋友 / AI 模拟面试，对方按这份问你</li><li>临场前：背 STAR 模板和自我介绍</li></ol>\n      <p><strong>重点关注的红色警报区</strong>（你不熟一定挂）：</p>\n      <ul><li>⚠️ MySQL + Neo4j 一致性方案</li><li>⚠️ Redisson 的看门狗和误删</li><li>⚠️ JWT 强制下线方案</li><li>⚠️ ThreadLocal 内存泄漏</li><li>⚠️ Spring Cache 失效场景</li></ul>\n      <p>加油哒！💕 七月 offer 在等你～(≧▽≦)</p>",
  "contentFile": "/content/learning-plans-md/learning-14.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
