(function(){
  const detail = {
  "id": 19,
  "content": "<h1>Day8：SQL 执行与 EXPLAIN 高频面试题</h1>\n      <blockquote>主题：SQL 执行与 EXPLAIN</blockquote>\n      <blockquote>目标：能通过执行计划判断 SQL 是否可能慢，能说清“全表扫 vs 走索引”，并能落到项目优化场景。</blockquote>\n      <hr>\n      <h2>1. 一条 SQL 从发送到 MySQL 后，大致经历了哪些步骤？</h2>\n      <p><strong>面试官想考：</strong> 你是不是只会背 SQL，还是理解 MySQL 执行链路。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>客户端发送 SQL 到 MySQL Server。</li><li>经过解析器做语法解析。</li><li>优化器选择执行计划，比如决定用哪个索引、表连接顺序。</li><li>执行器调用存储引擎接口读取数据。</li><li>存储引擎返回数据，Server 层再做过滤、排序、聚合等处理。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>如果接口查询慢，你会从哪里开始排查？</li><li>为什么同一条 SQL 有时快、有时慢？</li></ul>\n      <hr>\n      <h2>2. 你平时怎么判断一条 SQL 慢不慢？</h2>\n      <p><strong>面试官想考：</strong> 你有没有真实排查慢 SQL 的思路。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>先看接口耗时和慢查询日志，确认是不是数据库阶段慢。</li><li>对 SQL 执行 <code>EXPLAIN</code>，重点看 <code>type</code>、<code>key</code>、<code>rows</code>、<code>Extra</code>。</li><li>看是否走了合适索引，扫描行数是否过大。</li><li>看是否出现 <code>Using filesort</code>、<code>Using temporary</code>、全表扫描等风险。</li><li>结合业务数据量判断，不只看 SQL 写法。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>如果一个接口 2 秒，其中 SQL 执行 1.5 秒，你怎么优化？</li></ul>\n      <hr>\n      <h2>3. EXPLAIN 里你最关注哪些字段？分别怎么看？</h2>\n      <p><strong>面试官想考：</strong> EXPLAIN 是否真正会用。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>type</code>：访问类型，常见从好到差大致是 <code>const</code>、<code>ref</code>、<code>range</code>、<code>index</code>、<code>ALL</code>。</li><li><code>key</code>：实际使用的索引，如果是 <code>NULL</code>，通常说明没走索引。</li><li><code>rows</code>：预估扫描行数，越大越要警惕。</li><li><code>Extra</code>：额外信息，重点关注 <code>Using index</code>、<code>Using where</code>、<code>Using filesort</code>、<code>Using temporary</code>。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>如果 <code>possible_keys</code> 有索引，但 <code>key</code> 是 <code>NULL</code>，可能是什么原因？</li><li>如果 <code>rows</code> 很大，但 <code>type</code> 不是 <code>ALL</code>，还需要优化吗？</li></ul>\n      <hr>\n      <h2>4. EXPLAIN 里的 <code>type = ALL</code> 代表什么？一定不能接受吗？</h2>\n      <p><strong>面试官想考：</strong> 你是否能结合业务判断，而不是死背。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>ALL</code> 通常表示全表扫描，会扫描整张表。</li><li>大表、高频接口、在线业务里要重点警惕。</li><li>小表、配置表、一次性离线统计不一定是问题。</li><li>是否要优化，要结合数据量、访问频率、响应时间、并发量判断。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>人物表、文章表、订单表这类大表出现 <code>ALL</code> 怎么处理？</li><li>什么场景下你能接受全表扫描？</li></ul>\n      <hr>\n      <h2>5. <code>key = NULL</code> 一定代表没有索引吗？</h2>\n      <p><strong>面试官想考：</strong> 你对“有索引”和“用索引”的区别是否清楚。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>不一定是表上没有索引，而是优化器这次没有选择索引。</li><li>可能原因包括：条件字段没建索引、索引选择性太差、函数操作导致索引失效、隐式类型转换、查询返回数据比例太高。</li><li>优化器认为全表扫描更划算时，也可能不用索引。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li><code>where status = 1</code> 建索引一定有用吗？</li><li><code>where DATE(create_time) = ?</code> 为什么可能不走索引？</li></ul>\n      <hr>\n      <h2>6. <code>rows</code> 字段很大说明什么？你会怎么处理？</h2>\n      <p><strong>面试官想考：</strong> 你能不能从扫描成本角度分析性能。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>rows</code> 是优化器预估要扫描的行数，不是最终返回行数。</li><li><code>rows</code> 很大说明过滤不够精准，I/O 和 CPU 成本可能高。</li><li>可以考虑补充合适索引、优化查询条件、减少返回字段、分页限制、改造业务查询方式。</li><li>也要配合真实执行耗时和数据分布判断。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>列表分页越往后越慢，和扫描行数有什么关系？</li><li>如果人物关系查询扫描行数很大，你会考虑 MySQL 优化还是 Neo4j 承接？</li></ul>\n      <hr>\n      <h2>7. <code>Using filesort</code> 是什么意思？一定很慢吗？</h2>\n      <p><strong>面试官想考：</strong> 你对排序成本和索引排序是否理解。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>Using filesort</code> 表示 MySQL 需要额外排序，不一定真的落磁盘。</li><li>数据量小不一定慢，但大数据量排序会明显增加成本。</li><li>常见优化方式：让 <code>where</code> 和 <code>order by</code> 尽量匹配联合索引，减少排序数据量。</li><li>不要盲目为了消灭 <code>Using filesort</code> 建很多索引，要看接口频率和数据量。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li><code>order by create_time desc limit 20</code> 应该怎么设计索引？</li><li>查询文章列表按发布时间倒序，为什么要关注排序字段？</li></ul>\n      <hr>\n      <h2>8. <code>Using temporary</code> 代表什么？哪些 SQL 容易出现？</h2>\n      <p><strong>面试官想考：</strong> 你是否知道分组、排序、去重的代价。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>Using temporary</code> 表示 MySQL 执行过程中需要临时表。</li><li>常见于 <code>group by</code>、<code>distinct</code>、复杂排序、子查询或无法利用索引完成分组排序的场景。</li><li>临时表数据量大时，可能带来内存和磁盘开销。</li><li>优化方向是减少参与计算的数据量，必要时增加合适索引或改造统计逻辑。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>统计类接口要不要实时查大表？</li><li>高频统计能不能用缓存、异步汇总表或定时任务？</li></ul>\n      <hr>\n      <h2>9. <code>Using index</code> 和 <code>Using where</code> 有什么区别？</h2>\n      <p><strong>面试官想考：</strong> 覆盖索引和过滤逻辑。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li><code>Using index</code> 通常表示使用了覆盖索引，可以直接从索引拿到需要字段，减少回表。</li><li><code>Using where</code> 表示 MySQL 还需要根据 <code>where</code> 条件继续过滤。</li><li>两者可能同时出现，不是互斥关系。</li><li>如果查询字段都在索引里，通常比还要回表更省 I/O。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>列表页只展示 <code>id、title、create_time</code>，为什么可能适合覆盖索引？</li><li>为什么 <code>select *</code> 容易破坏覆盖索引？</li></ul>\n      <hr>\n      <h2>10. 你会如何给一个慢查询设计优化方案？</h2>\n      <p><strong>面试官想考：</strong> 是否具备企业项目排查闭环。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>先确认慢在哪里：接口、网络、数据库、缓存、下游服务。</li><li>拿到真实 SQL 和参数，查看慢查询日志或监控。</li><li>用 <code>EXPLAIN</code> 看执行计划，关注是否全表扫、扫描行数、排序、临时表。</li><li>根据业务访问模式设计索引，不是看到字段就建索引。</li><li>优化 SQL 写法，避免函数操作索引列、隐式转换、无意义 <code>select *</code>。</li><li>压测或对比执行耗时，确认优化有效。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>如果一个热门接口被频繁访问，除了加索引还能怎么优化？</li><li>数据量继续增长后，现在的索引方案会不会失效？</li></ul>\n      <hr>\n      <h2>11. 如果 SQL 明明建了索引，为什么还是慢？</h2>\n      <p><strong>面试官想考：</strong> 你能不能从“走索引不等于快”继续分析。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>可能扫描行数仍然很大，比如索引区分度差。</li><li>可能需要大量回表。</li><li>可能排序、分组、临时表成本高。</li><li>可能分页偏移量太大。</li><li>可能锁等待、事务太长、并发竞争导致慢。</li><li>也可能是缓存失效或数据库资源瓶颈。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li><code>status</code> 这种低区分度字段为什么不一定适合单独建索引？</li><li>深分页怎么优化？</li></ul>\n      <hr>\n      <h2>12. 项目中遇到 SQL 慢，你会怎么向面试官讲一个完整案例？</h2>\n      <p><strong>面试官想考：</strong> 你能不能把八股和项目经验串起来。</p>\n      <p><strong>答题抓手：</strong></p>\n      <ul><li>先说业务场景：哪个接口、查什么数据、为什么高频。</li><li>再说问题表现：接口耗时升高、慢查询日志出现、扫描行数大。</li><li>接着说排查过程：抓 SQL、带参数执行 <code>EXPLAIN</code>、分析 <code>type/key/rows/Extra</code>。</li><li>然后说优化动作：建联合索引、改 SQL、减少字段、避免深分页、加缓存等。</li><li>最后说结果：扫描行数下降、响应时间下降、接口稳定性提升。</li></ul>\n      <p><strong>项目追问：</strong></p>\n      <ul><li>这个优化有没有副作用，比如写入变慢、索引占空间？</li></ul>\n      <hr>\n      <h1>今日重点口述模板</h1>\n      <p>如果面试官问：“你怎么看一条 SQL 有没有走索引、有没有性能问题？”</p>\n      <p>可以这样答：</p>\n      <blockquote>我会先用慢查询日志或接口耗时定位到具体 SQL，然后带真实参数执行 EXPLAIN。重点看几个字段：<code>type</code> 判断访问方式，<code>key</code> 看实际用了哪个索引，<code>rows</code> 看预估扫描行数，<code>Extra</code> 看有没有 <code>Using filesort</code>、<code>Using temporary</code> 这类额外成本。如果 <code>type</code> 是 <code>ALL</code>、<code>key</code> 是 <code>NULL</code>、<code>rows</code> 很大，通常说明有全表扫描或过滤效率差的风险。然后我会结合业务数据量和访问频率判断是否需要优化，比如补联合索引、调整查询条件、减少查询字段、避免函数操作索引列，必要时加缓存或改造统计方式。</blockquote>",
  "contentFile": "/content/learning-plans-md/learning-19.md"
};
  const learningDetails = { ...(window.SITE_DATA?.learningDetails || {}), [String(detail.id)]: detail };
  window.SITE_DATA = Object.freeze({ ...(window.SITE_DATA || {}), learningDetails });
})();
